# Git Notes
- `2026-05-29` pending commit `feat: streamline discord grok commands`: Reworked Discord Slash Commands to keep an explicit project allowlist of common CowCli commands (`help`, `status`, `backend`, `config`, `skill`, `memory`, `knowledge`, `voice`, `updates`, `tokens`, `ledger`, etc.) while filtering unrelated historical commands such as `codex-app` and replacing old Grok shortcuts with `/grok-gen-image`, `/grok-gen-video`, `/grok-direct-gen-image`, and `/grok-direct-gen-video`. The four Grok commands all accept an optional image attachment: no image means text-to-image/text-to-video, with an image means image-to-image/image-to-video. Image commands expose `speed|quality`; video commands expose `6s|10s` and `480p|720p`. Direct commands still route through `/grok-direct image|video`; enhanced image generation submits `runtime=grok` with `prompt_enhancement=True`, and non-direct video submits to the Grok video job queue. Added `discord_prune_global_commands_on_startup` config/template default so guild-based Discord startup clears stale global commands before syncing guild commands. Validation: `.\.venv\Scripts\python.exe -m py_compile channel\discord\discord_channel.py tests\test_discord_channel.py config.py` passed; `.\.venv\Scripts\python.exe -m pytest tests\test_discord_channel.py -q` -> 14 passed; `.\.venv\Scripts\python.exe -m pytest tests\test_grok_direct_cli.py tests\test_grok_video_skill.py -q` -> 32 passed; `.\.venv\Scripts\python.exe -m pytest tests\test_grok_config_template.py -q` -> 6 passed; `.\.venv\Scripts\python.exe -m pytest tests\test_web_admin_privacy.py::WebAdminPrivacyTest::test_channels_handler_exposes_discord_config_fields tests\test_discord_channel.py -q` -> 15 passed; `.\.venv\Scripts\python.exe -m pytest tests\test_grok_image_gen.py tests\test_web_grok_image_to_image.py -q` -> 13 passed with non-fatal background logging after test completion from missing local Codex vision auth; runtime restarted with `.\.venv\Scripts\cow.exe restart`, new PID 29076, and Discord logged global prune count 0 plus `synced 19 slash command(s)`. Rollback: revert `channel/discord/discord_channel.py`, Discord tests, config/template, README, and this note.
- `2026-05-29` pending commit `fix: use grok image edit endpoint`: Fixed Grok single-image image-to-image so requests with a reference image continue to use the existing Grok login/OAuth credentials but POST to xAI `/images/edits` with the documented JSON `image` envelope, while plain text-to-image remains on `/images/generations`. Added Discord `/image-to-image` slash shortcut with an image attachment, while keeping `/imagine` as text-to-image and `/image-to-video` for video; README and `docs/grok.md` now document the endpoint split and Discord shortcut. Validation: `.\.venv\Scripts\python.exe -m py_compile integrations\hermes_xai\image_gen.py channel\discord\discord_channel.py tests\test_grok_image_gen.py tests\test_discord_channel.py` passed; `.\.venv\Scripts\python.exe -m pytest tests\test_grok_image_gen.py tests\test_discord_channel.py tests\test_grok_direct_cli.py tests\test_web_grok_image_to_image.py -q` -> 44 passed; live Grok OAuth image-edit smoke used `/images/edits` and saved `tmp\grok_media\xai_grok-imagine-image_z427ye7p.jpeg` (337789 bytes). Runtime was restarted with `.\.venv\Scripts\cow.exe restart`, now running PID 6272; Discord logged `synced 20 slash command(s)` after restart. Rollback: revert `integrations/hermes_xai/image_gen.py`, `channel/discord/discord_channel.py`, the two focused tests, README, `docs/grok.md`, and this note.
- `2026-05-29` pending commit `fix: route grok through proxy`: Diagnosed Web Grok login failing at xAI OIDC discovery because direct IPv4 access to `auth.x.ai:443` timed out while the local HTTP proxy `http://127.0.0.1:7897` succeeded. Added `grok_proxy` config/template/Web editing support and shared xAI proxy resolution, then routed OAuth discovery/token exchange/refresh, Grok Chat HTTP client, TTS, image/video generation, generated media downloads, and Grok video worker subprocess env through the resolved proxy; empty `grok_proxy` falls back to global `proxy` or `discord_proxy`. Web Grok login now surfaces start errors instead of leaving an empty authorization link. Local ignored `config.json` was updated to `grok_proxy=http://127.0.0.1:7897` and must not be staged. Validation: runtime restart loaded `grok_proxy`, proxy discovery smoke returned HTTP 200 and `get_xai_oauth_status()` reported `logged_in=True`; safe upload preflight passed before and after staging; focused py_compile passed for touched Grok/Web/config/test modules; `tests\test_grok_proxy.py tests\test_grok_config_template.py tests\test_grok_regression.py tests\test_grok_web_gray.py -q` -> 27 passed; `node --check channel\web\static\js\grok.js` passed; `git diff --cached --check` passed.
- `2026-05-29` pending commit `fix: proxy discord gateway connection`: Diagnosed live Discord Bot offline status after the App was installed into `OpenClaw_Company`. The IDs in `config.json` were corrected to numeric Guild/User/Channel IDs and `discord.py` was installed into the project venv, but Discord startup still failed with `Cannot connect to host discord.com:443` because Python/aiohttp was direct-connecting while Chrome used the local Clash proxy. Added `discord_proxy` config/template/Web field and `DISCORD_PROXY` env support, falling back to global `proxy`/standard proxy env vars, then passes the proxy into `discord.py` `commands.Bot(proxy=...)`. Added proxy URL normalization tests and Web field coverage. Local runtime config was set to `http://127.0.0.1:7897` without staging `config.json`; after restart Discord logged `using configured proxy`, `synced 19 slash command(s)`, and `logged in as 1506139597535711242`. Followed up on the first Discord `/video` failure: job `0b7e907ce5ed` was created at `2026-05-29 18:08:31` and failed before `data/auth/grok_auth.json` was created at `18:13:52`; a same-entry child-process status probe after login now reports `logged_in=true`, so no video auth code change was needed. Validation: `py_compile` passed for Discord/config/Web/test files; `tests\test_discord_channel.py tests\test_web_admin_privacy.py::WebAdminPrivacyTest::test_channels_handler_exposes_discord_config_fields -q` -> 11 passed.
- `2026-05-29` pending commit `feat: route grok direct image refs`: Added PR2 entry routing for Grok single-image image-to-image without changing the provider payload. `/grok-direct image` now accepts one explicit `[图片: ...]` / `[image: ...]` reference or one same-session recent image when the prompt clearly asks to edit/reference an image, preserves `prompt_enhancement=False`, keeps speed/quality and explicit `--size`/`--ar`, and leaves plain text-to-image without `image_url`. Added `common/image_reference_routing.py` for shared image-reference intent and text-to-image opt-out detection. Web single image uploads now register recent image refs for later direct image commands; Web same-message attachments still append image markers. WeCom and Weixin now route active-Grok natural image-edit prompts to `IMAGE_CREATE` and attach one fresh recent image only for explicit image-to-image intent, while plain "generate an image" and "text-to-image/no image" prompts do not auto-reference prior images. Updated config/template with `image_recognition_image_create_auto_ref_window_seconds=180`, CowCli help examples, README, and focused tests. Validation: `.\.venv\Scripts\python.exe -m py_compile plugins\cow_cli\cow_cli.py channel\chat_channel.py channel\wecom_bot\wecom_bot_channel.py channel\weixin\weixin_channel.py channel\web\web_channel.py common\image_generation_routing.py common\image_reference_routing.py config.py` passed; `.\.venv\Scripts\python.exe -m pytest tests\test_grok_direct_cli.py tests\test_grok_image_trigger.py tests\test_web_grok_image_to_image.py tests\test_wecom_grok_image_to_image.py tests\test_weixin_grok_image_to_image.py tests\test_grok_config_template.py -q` -> 48 passed; `.\.venv\Scripts\python.exe -m pytest tests\test_grok_video_trigger.py -q` -> 21 passed with one non-fatal expected environment warning about no vision model. Rollback: revert PR2 routing/helper/config/docs/test files; no service restart was performed.
- `2026-05-29` pending commit `fix: show discord in web channel connect list`: Confirmed `/api/channels` already returned the tracked `discord` channel definition from the main `D:\CowAgent` repository, but the Web console connect dropdown filtered it out with the front-end `CONNECTABLE_CHANNEL_NAMES` whitelist. Added `discord` to that whitelist and a regression that reads `channel/web/static/js/console.js` to prevent the UI filter from hiding Discord again. Also verified `D:\CowAgent-discord-worktree` is not present and not a runtime dependency; all deployment-required Discord files are tracked in the main repo. Validation: safe upload preflight passed before staging; `node --check channel\web\static\js\console.js` passed; `py_compile` passed for `tests\test_web_admin_privacy.py`; grouped `tests\test_web_admin_privacy.py tests\test_discord_channel.py -q` hit the known Windows `TemporaryDirectory` cleanup race after assertions; targeted rerun `tests\test_web_admin_privacy.py::WebAdminPrivacyTest::test_web_uploaded_images_are_cached_for_later_video_refs tests\test_web_admin_privacy.py::WebAdminPrivacyTest::test_console_allows_discord_in_connect_dropdown tests\test_discord_channel.py -q` -> 11 passed.
- `2026-05-29` pending commit `feat: add grok image-to-image backend`: Added Grok/xAI single-reference image-to-image support without changing the text-to-image payload shape. `XAIImageGenProvider.generate()` now accepts `image_url`, normalizes local paths/`file://` references into data URIs with MIME detection and size checks, accepts HTTP/HTTPS/data URI references, centralizes the provisional image-to-image payload in `_build_image_to_image_payload()`, and keeps HTTP/token/cookie error text sanitized. Added shared `models/grok/grok_image_options.py` for Grok speed/quality model selection, prompt/argument size parsing, `[图片: ...]` extraction, and reference-dimension aspect/resolution inference. The `image-generation` skill now passes Grok `image_url` instead of rejecting it, avoids double prompt enhancement by calling the provider with `prompt_enhancement=False` after script-layer enhancement, and preserves explicit false for `/grok-direct` background paths. `models/grok/grok_image.py` now handles natural-language image-to-image with the first `[图片: ...]` reference and returns a clear upload-first error for edit intent without an image. No real Grok online calls were made; all payload and routing validation used mocks/unit tests. Validation: `py -3 -m py_compile integrations\hermes_xai\image_gen.py skills\image-generation\scripts\generate.py models\grok\grok_image.py models\grok\grok_image_options.py` passed; `py -3 -m pytest tests\test_grok_image_gen.py tests\test_grok_image_skill.py tests\test_grok_sync_image_model.py -q` -> 28 passed; related regression `py -3 -m pytest tests\test_image_generation_skill_script.py tests\test_grok_image_trigger.py tests\test_grok_direct_cli.py -q` -> 40 passed; provider/config regression `py -3 -m pytest tests\test_grok_image_download.py tests\test_grok_regression.py tests\test_grok_config_template.py -q` -> 24 passed. Rollback: revert `integrations/hermes_xai/image_gen.py`, `skills/image-generation/scripts/generate.py`, `models/grok/grok_image.py`, `models/grok/grok_image_options.py`, the three focused test files, and README/docs updates.
- `2026-05-29` pending commit `fix: stop repeated grok video completion recovery`: Fixed a restart-loop notification bug in Grok video background recovery. Root cause: `delivery_failed` was included in video `RECOVERABLE_STATUSES`, so a job whose text completion notice had been sent but whose MP4 delivery failed was treated as unfinished on every startup; because the output MP4 still existed, recovery called `_send_completion()` again and re-sent `Grok video generation finished (task ...)`. `delivery_failed` is now treated as a terminal state for Grok video recovery, while queued/running jobs are still recovered. Added `tests/test_grok_video_recovery.py` to lock this behavior. The already noisy runtime task `c64d27f56bc0` was also locally moved from `delivery_failed` to `failed` in its runtime `job_state.json` with a `.before-codex-suppress-repeat` backup, so it will not be retried by the old running code before deployment. Validation: `py -3 -m py_compile agent/tools/video_generation/job_manager.py tests/test_grok_video_recovery.py` passed; `py -3 -m pytest -q tests/test_grok_video_recovery.py` -> 1 passed; `py -3 -m pytest -q tests/test_grok_video_recovery.py tests/test_grok_video_skill.py` -> 13 passed.
- `2026-05-29` pending commit `fix: document and parse grok video options`: Added `/help` and Web slash-menu examples for `/grok-direct video --resolution 720p --duration 10s -- <prompt>`, plus uploaded-image video usage. Added a shared local parser for explicit video options in the original user text (`720p`, `480p`, `10s`, and simple aspect ratios) and applied it to plain Grok `VIDEO_CREATE` and the background `grok_video_generation_task`. The video tool now prefers the original current-context text, strips internal `[image: ...]` reference markers from the submitted prompt, and only carries parsed parameters/reference images forward, so video generation does not use local prompt polishing; image generation remains the only non-direct path with hidden prompt enhancement. Validation: `py_compile` passed for touched Grok/video/CowCli/test files; focused pytest `tests\test_grok_direct_cli.py tests\test_grok_video_trigger.py tests\test_grok_video_skill.py -q` -> 48 passed; `tests\test_web_admin_privacy.py tests\test_grok_video_gen.py tests\test_image_generation_intent_and_send_limits.py -q` -> 32 passed.
- `2026-05-29` pending commit `feat: add discord channel`: Added a native Python Discord channel instead of reusing the OpenClaw Node relay as the runtime path. Discord is independent from Weixin/WeCom, requires exactly one configured `discord_admin_user_id`, optionally restricts `discord_guild_id` and `discord_allowed_channel_ids`, and exposes CowCli chat commands as native Discord Slash Commands while keeping terminal-only lifecycle commands out of Discord. Added `/imagine` as a convenience wrapper for `/grok-direct image -- <prompt>`, Web channel configuration fields for token/guild/admin/channel ids, `discord.py` dependency metadata, ChannelFactory/ChannelManager lifecycle wiring, and Discord reply-target preservation in image/video background job snapshots so Grok media completion can return to the original Discord channel. Optional message-content handling can cache Discord image attachments when `discord_message_content_enabled=true`; the default remains Slash Commands only. Validation: `py -3 -m py_compile channel\discord\discord_channel.py channel\discord\discord_message.py channel\channel_factory.py app.py agent\tools\image_generation\job_manager.py agent\tools\video_generation\job_manager.py` passed; `py -3 -m pytest tests\test_discord_channel.py tests\test_web_admin_privacy.py -q` -> 20 passed. A first test run exposed CowCli plugin top-level import registration (`Plugin path not set`), fixed by lazy-loading plugin constants/suggestions and recorded in self-evolution memory. Rollback: remove `channel/discord/`, the `discord` factory/singleton mapping, Discord config/template/Web fields, `discord.py` dependency lines, and the Discord snapshot keys in image/video job managers.
- `2026-05-29` pending commit `fix: align grok direct refs and web commands`: Finalized the Grok direct/Web follow-up after rebasing onto `origin/main@c7b466e`. Fixed the image-generation script boolean parser so `prompt_enhancement=false` truly preserves the user's raw `/grok-direct image` prompt; default Grok video requests now use same-session recent images unless the user explicitly asks for text-to-video, pick the newest one by default or newest N when an explicit count is requested, infer video aspect ratio from local reference image dimensions, and run plain/direct Grok video submissions through detached background jobs with same-actor parallelism controlled by `grok_video_generation_actor_workers`. Web slash suggestions now read the loaded CowCli plugin command surface so `/grok-direct` appears in Web. Validation: `py_compile` passed for touched source/tests; `node --check channel\web\static\js\console.js` passed; focused pytest grouped run reached 218 passed with two Windows temp-dir cleanup races, and rerunning those two tests separately passed.
- `2026-05-29` pending commit `fix: auto attach fresh image for grok video`: Diagnosed the live WeCom sequence at 10:51 where an image was downloaded and registered, but the following `生成视频` request submitted to Grok with `has_image=False reference_count=0`, so it behaved as text-to-video. Updated `ChatChannel._append_recent_image_refs_for_video_create()` so generic `VIDEO_CREATE` prompts automatically attach same-session recent images inside the new short `image_recognition_video_create_auto_ref_window_seconds` window (default 120s), while explicit `文生视频` / `不参考图` phrasing stays text-to-video and existing explicit reference prompts keep the longer recent-ref window. Added config/template defaults and WeCom Bot regressions for fresh-image auto-attach and text-to-video opt-out. Validation: `py -3 -m py_compile channel\chat_channel.py channel\wecom_bot\wecom_bot_channel.py tests\test_grok_video_trigger.py config.py` passed; `.venv\Scripts\python.exe -m pytest tests\test_grok_video_trigger.py tests\test_image_recognition_background.py -q` -> 27 passed, 3 warnings.
- `2026-05-29` pending commit `fix: focus replies on latest context`: Diagnosed the follow-up after the progress-report fix where a user replied `没有` to the latest bot prompt asking whether to add harvest/notes, but Grok continued an older UCIe knowledge answer from conversation history. The existing Agent context logic kept recent turns chronologically but did not explicitly weight the latest assistant prompt, so a salient old protocol answer could dominate ambiguous follow-ups. Added per-request conversation-focus context that tells the model to interpret the newest user message against the latest exchange first and use older knowledge/tool results only when explicitly referenced. Added a request-copy history focus for short contextual replies (`没有`, `不用`, `好的`, etc.) that keeps only the latest configurable number of real user turns (`short_contextual_reply_keep_turns`, default 2), preserving in-memory/persisted history while preventing old protocol evidence from entering ambiguous reply requests. Knowledge auto-injection is now gated: progress updates and short confirmations skip retrieval; explicit knowledge/protocol questions still retrieve; contextual follow-ups after a knowledge answer build a query from the latest knowledge question plus the follow-up. Validation: `py_compile` passed for touched Agent/config/test files; focused pytest `tests\test_agent_task_limits.py tests\test_agent_stream_knowledge_context.py -q` -> 43 passed, 6 subtests.
- `2026-05-29` pending commit `fix: report personal grok backend status`: Fixed CowCli backend status/current-quota queries after personal Grok switching. Root cause: `set_user_backend_override()` stored the admin/whitelist backend on the actor profile, but `/backend status` and natural-language current-backend quota queries still called global `get_current_backend()`, so they reported the shared GPT route such as `capi_monthly`. `describe_status(profile)` now renders the actor-effective backend first and keeps the shared GPT backend as a separate line for admin/whitelist users; ordinary users resolve to the shared GPT route. `/backend` status and `quota-current` are public read-only/self-scoped queries, while backend switching and explicit card-specific quota commands stay admin-only; `quota-current` avoids CAPI/Codex quota calls when the effective backend is Grok or another non-quota backend. Validation: `py_compile` passed for `common\llm_backend_router.py`, `plugins\cow_cli\cow_cli.py`, `tests\test_cow_cli_backend_nl.py`, and `tests\test_codex_backend_router.py`; focused pytest `tests\test_cow_cli_backend_nl.py tests\test_codex_backend_router.py -q` -> 102 passed, 1 warning.
- `2026-05-29` pending commit `fix: align grok direct refs and web commands`: Synced to `origin/main@64562f8` after the user's remote update, preserving that commit's quiet non-bill image auto-reply changes. Reviewed `origin/main@66af513` and found it preserved recent image refs but did not fully match the desired semantics: prompts without an explicit count could still pass all already-attached `[image: ...]` refs, so Grok received multiple references instead of only the latest one. Added shared parsing for explicit `参考上面 N 张` / `last N images` counts, capped at the xAI 7-reference limit, and applied the selection rule at all Grok video submission layers: plain Grok video, background `grok_video_generation_task`, and `/grok-direct video`. If refs are present and the prompt does not explicitly request a count, the final payload now uses only the newest image; explicit counts use the newest N images. Web uploads are cached into the same recent-image store so upload-first-then-command works, Web chat context is resolved as the configured administrator, and `/api/commands` now lets the Web slash menu mirror the CowCli command surface including `/grok-direct`, backend, voice, memory, knowledge, config, skill, token, ledger, updates, and install-browser commands. Grok direct image generation now bypasses the provider-level YouMind enhancer entirely when `prompt_enhancement=false`, not just the skill-layer enhancer. Validation so far: `py_compile` passed for touched Grok/video/Web/CowCli/image files; `node --check channel\web\static\js\console.js` passed; focused pytest `tests\test_grok_direct_cli.py tests\test_grok_video_trigger.py tests\test_grok_video_skill.py tests\test_image_recognition_background.py tests\test_grok_image_gen.py tests\test_web_admin_privacy.py -q` -> 62 passed, 2 warnings.
- `2026-05-29` pending commit `fix: quiet non-bill image auto replies`: Fixed an Agent routing false positive where plain work-progress snapshots such as `Feature list完成90% tc_list完成30%` were treated like protocol/knowledge questions. Added progress-snapshot detection to task-budget classification, protocol-question detection, deep knowledge routing, and knowledge auto-injection. Added `single_chat_image_recognition_auto_reply=false` as the default switch for proactive private image-recognition replies: background recognition still stores image facts for later follow-up, and bill-like private screenshots still run the existing auto-ledger reply path, but non-bill images stay silent unless the switch is enabled. Stabilized image-recognition regressions by keeping async recognition under the intended mocks and using the current date for duplicate-bill checks. Validation: `py_compile` passed for touched Agent/image/config/test files; focused pytest `tests\test_agent_stream_knowledge_context.py tests\test_agent_task_limits.py tests\test_image_recognition_background.py tests\test_wecom_social_bridge.py -q` -> 79 passed, 5 warnings, 6 subtests.
- `2026-05-29` pending commit `fix: preserve grok image-to-video refs`: Synced the checkout to `origin/main@5349f6f8` before editing, then verified the live WeCom quoted-old-image case was delivered as plain text with no image payload, so the bot cannot identify that old referenced image unless the platform callback includes media content or a resolvable id. Fixed Grok direct image mode so `prompt_enhancement=false` reaches `XAIImageGenProvider` and skips the provider-level YouMind hidden prompt enhancer instead of only skipping the skill layer. Added same-session recent-image fallback for `VIDEO_CREATE` and `/grok-direct video` prompts such as `参考上图` / `参考上面几张` / `image to video`, so a user can send images first and immediately ask Grok to animate them; direct video still rejects image-to-video prompts when no usable image can be found. Added safe Grok video submit diagnostics that log only whether an image/reference count is present. Validation: safe upload preflight passed before and after staging; `git diff --cached --name-status` reviewed 17 intentional files; `git diff --cached --check` passed; `py_compile` passed for touched Grok/video/image/channel/CowCli/test files; focused pytest `tests\test_grok_direct_cli.py tests\test_grok_image_gen.py tests\test_grok_image_skill.py tests\test_grok_video_trigger.py tests\test_image_generation_skill_script.py` plus targeted image-recognition regressions -> 50 passed, 1 warning.
- `2026-05-29` pending commit `feat: add local grok backend switch command`: Added CowCli natural-language local switching for the restricted Grok backend. `切换后端到 Grok/xAI` now resolves to a backend command before Agent execution; admins and restricted-backend whitelist users receive a personal Grok override via `set_user_backend_override`, while ordinary users are denied and the shared GPT backend remains unchanged. `切回 GPT 后端` clears the personal override back to the shared GPT route. Direct `execute()` calls without chat context refuse personal Grok switching instead of mutating global state. Validation: safe upload preflight passed before and after staging; `git diff --cached --name-status` reviewed 5 intentional files; `git diff --cached --check` passed; `py -3 -m py_compile plugins\cow_cli\cow_cli.py plugins\cow_cli\backend_nl.py tests\test_cow_cli_backend_nl.py` passed; `py -3 -m pytest tests\test_cow_cli_backend_nl.py tests\test_codex_backend_router.py tests\test_grok_agent_model_routing.py tests\test_grok_image_trigger.py -q` -> 115 passed.
- `2026-05-29` pending commit `fix: route grok reasoning and image defaults`: Follow-up to the standalone Grok backend work. Grok-backed actor routes now explicitly report that reasoning effort is unsupported, so Agent policy selection, knowledge-task effort fallback, inline thinking display, and model call kwargs do not send `thinking`, `reasoning_effort`, or `reasoning_effort_locked` when the active/request backend is Grok. Added shared image-generation routing so an admin/whitelisted actor with active Grok backend defaults background image jobs and direct `IMAGE_CREATE` to Grok, while prompts that explicitly ask for GPT/OpenAI/Codex image generation route back to the GPT/Codex image runtime. Updated the `image-generation` and `grok-image-generation` skill docs in both repo and runtime skill copies. Validation: safe upload preflight passed before and after staging; `git diff --cached --check` passed; `py_compile` passed for touched reasoning/image/bridge/test files; focused pytest `tests\test_agent_stream_capi_fallback.py tests\test_reasoning_effort_policy.py tests\test_codex_backend_router.py tests\test_grok_agent_model_routing.py tests\test_grok_web_gray.py tests\test_cow_cli_backend_nl.py tests\test_grok_image_skill.py tests\test_grok_image_trigger.py -q` -> 169 passed, 1 warning, 15 subtests passed.
- `2026-05-29` pending commit `feat: isolate grok model backend`: Split Grok into a restricted per-actor model backend under `llm_backend.providers.grok` with default whitelist `山海入梦来`. Global `current_backend` remains GPT-only (`capi`, `capi_monthly`, `codex`), and midnight auto-switch continues to update only that shared GPT route. Admin and whitelist users can receive personal overrides for Grok, GPT sub-backends, or saved custom OpenAI-compatible backend profiles without changing ordinary users. Web config now returns sanitized backend profiles and admin-available backend options, accepts `llm_backend_provider` and `actor_backend` saves without a normal `updates` payload, supports typed custom backend IDs, and saves actor backend overrides into router state instead of `config.json`. Selecting Codex in the Web model selector now switches only the Web admin profile instead of mutating the global route. Agent, plain Bridge chat, reasoning-effort metadata, and CAPI runtime fallback now resolve the active backend from the current actor profile where available; personal CAPI fallback writes back to the privileged actor override instead of flipping the ordinary-user backend. CowCli `/backend`, backend quota aliases, and backend status are now admin-only so ordinary users cannot inspect or switch model backend state through chat help or natural-language shortcuts. Validation completed during development: `py_compile` passed for touched router/bridge/agent/Web/CowCli/Grok/test files; `node --check channel\web\static\js\console.js` passed; focused pytest `tests\test_codex_backend_router.py tests\test_grok_agent_model_routing.py tests\test_grok_web_gray.py tests\test_cow_cli_backend_nl.py tests\test_agent_stream_capi_fallback.py tests\test_reasoning_effort_policy.py -q` passed with 148 tests, 15 subtests, and 2 expected dependency deprecation warnings.
- `2026-05-28` pending commit `feat: adapt image prompts with full youmind library`: Added hidden prompt enhancement for GPT/Codex and Grok image generation using the full local YouMind Nano Banana Pro prompt reference library (`skills/image-generation/references/nano-banana-pro/`, source commit `d66923c7fed464395fed76c782588305cb777f8b`, manifest count 13,842 prompts). The enhancer routes simple user prompts into portrait, poster, infographic/flowchart, product, UI, thumbnail, comic/storyboard, game assets, and general image use cases; Grok gets portrait-forward high-aesthetic photo phrasing by default, while GPT/Codex gets all-purpose structured prompt enrichment. Enhancement happens after model/provider selection so Grok speed/quality routing is not changed by inserted wording. Normal chat/job state does not expose the hidden prompt; output-local `prompt_metadata.json` and scoped runtime prompt history allow revealing the latest hidden prompt only when the user explicitly asks to see it. Error/log paths redact any upstream echo of the hidden prompt marker so provider failures do not leak the enhanced prompt. `gpt-imagine2`/`gpt-imagine` aliases normalize to `gpt-image-2`; direct Grok/xAI, OpenAI image, Codex-auth image skill, and legacy ChatGPT image paths all pass through the enhancer. Runtime skill copies for `image-generation` and `grok-image-generation` were synchronized. Validation so far: focused `py_compile` passed for prompt enhancer, prompt-history tool, job manager, skill script, Grok provider, OpenAI image provider, ChatGPT image provider, and Grok model wrapper; `.venv\Scripts\python.exe -m pytest tests/test_image_prompt_enhancer.py tests/test_grok_image_gen.py tests/test_grok_image_skill.py tests/test_image_generation_skill_script.py -q` -> 30 passed in 4.93s.
- `2026-05-28` pending commit `fix: harden grok media delivery`: Addressed PR3-PR5 review follow-ups without reimplementing Hermes/xAI protocol code. Added `integrations/hermes_xai/media_download.py` for shared safe downloads: HTTPS-only URLs, required hostname, DNS resolution with all A/AAAA addresses checked for public/global IPs, localhost/private/link-local/multicast/reserved/unspecified rejection, redirect-by-redirect revalidation, connect/read timeout, streaming writes, size cap abort with temp deletion, allowed Content-Type enforcement, and MP4 magic validation for `application/octet-stream`. `image_gen.py` and `video_gen.py` now save b64/URL outputs under `tmp/grok_media/`; `models/grok/grok_image.py` and `models/grok/grok_video.py` mark generated replies for cleanup. `ChatChannel`, WeCom Bot, and WechatCom app clean marked generated files after send success/failure/fallback; WeCom Bot and WechatCom app remote URL send branches now use the safe downloader or refuse non-video remote files. `WecomBotChannel._compose_context()` now checks `video_create_prefix` before `image_create_prefix` and strips leading punctuation, so `生成视频：...` routes to `ContextType.VIDEO_CREATE`. Voice conversation mode was preserved and documented as expected for `wechatcom_app`/`wecom_bot` voice inputs only; docs/tests now emphasize that text input and personal WeChat do not trigger new voice sends, TTS/send failures fall back to text, and final text is suppressed only after at least one successful voice segment. Test dependency manifests now expose `web.py`/`pycryptodome` for regression imports and align optional `websocket-client` with the main requirement. Validation so far: targeted `py_compile` for touched source/tests passed; `.venv\Scripts\python.exe -m pytest tests\test_grok_image_download.py tests\test_grok_video_gen.py tests\test_grok_video_trigger.py tests\test_grok_image_trigger.py tests\test_grok_video_send.py tests\test_grok_voice_conversation_mode.py -q` -> 61 passed, 2 warnings.
- `2026-05-29` pending commit `feat: add grok direct generation command`: Added the admin-only `/grok-direct` CowCli command for direct Grok image/video submissions that bypass Agent/LLM prompt analysis and prompt polishing while still using existing background queues and Grok/xAI service-side checks. `/grok-direct image` submits to `ImageGenerationJobManager` with `runtime=grok`, default `quality=speed`, and `prompt_enhancement=false` so the newer YouMind hidden image prompt enhancer is also skipped; `/grok-direct video` submits to `GrokVideoGenerationJobManager` with defaults `resolution=480p`, `aspect_ratio=16:9`, and `duration=6s`. The command parser preserves raw prompt text after `--`, accepts concise options such as `--ar`, `--size`, `--quality`, `--resolution`, and `--duration`, and uses `[图片: ...]` references from Weixin, WeCom Bot, and Web uploads as video reference images up to the existing seven-image xAI limit. Image direct mode rejects reference images with a user-facing hint to use video. CowCli command access marks `grok-direct` as admin-only and role-aware `/help` only shows it to admins; authenticated Web sessions count as admin context. Video resolution now flows from tool schema to job manager args, Grok video skill script, and `XAIVideoGenProvider.generate()`. Web channel polling/SSE media replies now register local result files as temporary `/api/file?token=...` links for image/video/file rendering while existing HTTP media suppression remains in place. Image/video job managers now preserve `request_id` in context snapshots so background completions can return to the original Web request and persist terminal state before exposing terminal in-memory status; `shutdown(wait=False)` waits when all known jobs are already terminal to avoid Windows teardown races. The image script accepts `prompt_enhancement=false` across provider adapters for direct-mode compatibility. Tests added/updated: `tests/test_grok_direct_cli.py`, `tests/test_grok_video_skill.py`, `tests/test_grok_image_skill.py`, and `tests/test_web_admin_privacy.py`. Validation after rebasing onto `origin/main@e66ba57`: `node --check channel\web\static\js\console.js` passed; `py -3 -m py_compile plugins\cow_cli\cow_cli.py agent\tools\image_generation\job_manager.py agent\tools\video_generation\job_manager.py agent\tools\video_generation\grok_video_generation_task.py skills\grok-video-generation\scripts\generate.py skills\image-generation\scripts\generate.py channel\web\web_channel.py tests\test_grok_direct_cli.py tests\test_grok_image_skill.py tests\test_grok_video_skill.py tests\test_web_admin_privacy.py` passed; `py -3 -m pytest tests\test_grok_direct_cli.py tests\test_grok_video_skill.py tests\test_web_admin_privacy.py tests\test_grok_video_send.py tests\test_grok_image_skill.py tests\test_image_generation_background_jobs.py tests\test_cow_cli_backend_nl.py -q` -> 119 passed; `py -3 -m pytest tests\test_image_prompt_enhancer.py tests\test_grok_image_trigger.py -q` -> 14 passed. Two wide-suite attempts hit Windows `TemporaryDirectory` teardown WinError 145 after test assertions had passed; each failed test passed when rerun individually. One isolated run of `test_different_actors_run_concurrently` exceeded its strict 0.65s timing threshold by about 1 ms, but the full grouped suite immediately passed with the same test included. During validation, the broader suite exposed an existing Windows background-worker race where `delivery_failed` was visible in memory before terminal state hit disk and TemporaryDirectory cleanup could fail with WinError 145; fixed in both image and video job managers and recorded a self-evolution lesson. Rollback: revert the CowCli command block plus the prompt-enhancement flag, resolution/request_id/media-return changes if direct Grok submission needs to be disabled; no schema migration or credential changes were introduced.
- `2026-05-29` final validation for `feat: add grok direct generation command`: After remote sync, the commit was rebased onto `origin/main@5a09b54`. Post-rebase validation passed: safe upload preflight OK with no staged files, `node --check channel\web\static\js\console.js`, focused `py_compile` for touched CowCli/job manager/skill/Web/test files, and `py -3 -m pytest tests\test_grok_direct_cli.py tests\test_grok_video_skill.py tests\test_web_admin_privacy.py tests\test_grok_video_send.py tests\test_grok_image_skill.py tests\test_image_generation_background_jobs.py tests\test_cow_cli_backend_nl.py tests\test_image_prompt_enhancer.py tests\test_grok_image_trigger.py tests\test_grok_video_trigger.py tests\test_image_generation_intent_and_send_limits.py -q` -> 152 passed in 8.23s.
- `2026-05-28` pending commit `docs: complete grok pr5 hardening`: Closed PR 5 documentation, migration, config, Web status, and regression coverage without reimplementing Grok/xAI protocol flows. Added `docs/grok.md` with OAuth login/manual paste, Hermes auth read-only import, PR1-4 config table, voice rules, WeCom AMR limits, image/video flows, Web/token safety, common errors, and regression matrix. `integrations/hermes_xai/auth.py` now supports read-only import of Hermes `providers.xai-oauth` from `~/.hermes/auth.json`/`HERMES_HOME/auth.json` into CowWeCom's auth store by reusing the existing `_load_auth_store`, `_save_auth_store`, and `save_xai_oauth_tokens` path; it never writes back to Hermes and returns only token-free status. Added reusable redaction helpers and wired Web Grok errors plus the Grok login page output through defensive redaction; `/api/grok/status`, `/api/grok/test`, `/api/grok/login/manual`, and `/api/grok/logout` remain token-free. `config.py` and `config-template.json` now default `grok_import_hermes_auth=true` with `grok_import_hermes_auth_overwrite=false`. Added `tests/test_grok_regression.py` and extended auth/Web/config tests for import behavior, non-overwrite, logout-without-reimport, redaction, config completeness, docs completeness, and plain OpenAI text path regression. Hermes/frontier reuse note: PR5 continues to rely on `integrations/hermes_xai/xai_http.py`, `auth.py`, `tts.py`, `image_gen.py`, and `video_gen.py` from PR1-4; no OAuth/TTS/image/video protocol implementation was duplicated. Validation: safe upload preflight OK before staging and after staging; `git diff --check` and `git diff --cached --check` passed; `.venv\Scripts\python.exe -m pytest -q tests\test_grok_auth.py tests\test_grok_web_gray.py tests\test_grok_config_template.py tests\test_grok_regression.py` -> 40 passed, 2 warnings, 6 subtests; PR1 chat/Web suite -> 61 passed, 2 warnings, 8 subtests; PR2 voice suite -> 38 passed, 2 warnings; PR3/PR4 image-video suite -> 46 passed, 2 warnings; explicit PowerShell-expanded `test_grok*.py` run before and after staging -> 119 passed, 2 warnings, 8 subtests; post-staging voice focused regression -> 22 passed, 2 warnings; OpenAI/Grok regression -> 13 passed; `node --check channel\web\static\js\grok.js` and py_compile for touched auth/Web/test files passed.
- `2026-05-28` pending commit `feat: add grok video generation`: Implemented PR 4 Grok/xAI video generation. Hermes direct reuse targets from `plugins/video_gen/xai/__init__.py`: `DEFAULT_MODEL`, `DEFAULT_DURATION`, `DEFAULT_ASPECT_RATIO`, `DEFAULT_RESOLUTION`, `DEFAULT_TIMEOUT_SECONDS`, `DEFAULT_POLL_INTERVAL_SECONDS`, `VALID_ASPECT_RATIOS`, `VALID_RESOLUTIONS`, `MAX_REFERENCE_IMAGES`, `_normalize_reference_images`, `_clamp_duration`, submit request shape, poll status handling, and `XAIVideoGenProvider`; CowWeCom reuses PR 1 `integrations.hermes_xai.xai_http.resolve_xai_http_credentials()` and adds local MP4 download, 401 forced-refresh retry, local-image data URI conversion, `VIDEO_CREATE` routing, WeCom/Weixin/WechatCom video delivery, and the `grok-video-generation` background Skill/job manager. User-visible flows now cover `生成视频 ...`, quoted/sent single-image video, and “参考上面发的 X 张图片生成 ... 视频” with up to 7 references. Outputs are saved as local MP4 files and sent as `ReplyType.VIDEO`, never as remote xAI URLs. Validation completed before staging: safe upload preflight OK; runtime `skills/grok-video-generation` copy synced to `C:\Users\RondleLiu\cow\skills\grok-video-generation`; `.venv\Scripts\python.exe -m pytest -q tests/test_grok_video_gen.py tests/test_grok_video_trigger.py tests/test_grok_video_send.py tests/test_grok_video_skill.py tests/test_app_scheduler_startup.py tests/test_grok_config_template.py` passed with 33 tests; Grok auth/image/TTS regression passed with 46 tests and 6 subtests; image background/script plus WeCom voice regression passed with 34 tests; targeted `py_compile` passed for the video provider, routing, channel, task, script, app, and tests. Post-staging checks: second safe upload preflight OK, `git diff --cached --check` passed, focused Grok video pytest repeated with 33 tests passing, and targeted `py_compile` repeated successfully before commit/push.
- `2026-05-28` pending commit `fix: cover grok video channel sends`: Added the Grok video channel/test slice without touching provider routing. `WechatComAppChannel` now handles `ReplyType.VIDEO`/`VIDEO_URL` by opening local `file://`/HTTP video sources, uploading as WeCom `video`, calling `send_video`, and sending an explicit text fallback if the file is missing or upload/send fails. Added `tests/test_grok_video_send.py` to lock WeCom Bot video replies to `media_type="video"`, Enterprise WeChat app video upload/send/fallback behavior, and Weixin video uploads through CDN `media_type=2` plus `send_video_item`. Validation: `.venv\Scripts\python.exe -m pytest -q tests/test_grok_video_send.py tests/test_wecom_voice_send.py` passed with 7 tests and 2 warnings; `.venv\Scripts\python.exe -m py_compile channel\wechatcom\wechatcomapp_channel.py tests\test_grok_video_send.py` passed. Concurrent untracked Grok video implementation files were left untouched.
- `2026-05-28` pending commit `fix: make grok image worker use project auth`: Fixed the post-PR3 Grok image worker path issue reported from live WeCom use. `integrations/hermes_xai/auth.py` now resolves the default `data/auth/grok_auth.json` and relative `grok_auth_file` values from the CowWeCom project root instead of the worker process current directory, so the background image-generation skill sees the same logged-in Grok OAuth store as the main service. `skills/image-generation/scripts/generate.py` now ignores non-Grok configured models when `runtime=grok`, preserving fast-model auto selection, and reroutes CowWeCom console logs to stderr while retaining the old stdout wrapper so successful worker stdout remains pure JSON. Added regression coverage for project-root auth path resolution, Grok runtime model cleanup, stdout-safe log rerouting, and a subprocess-level Grok runtime script boundary. Validation: `py_compile` passed for the touched auth/script/test files; focused Grok image/OAuth/worker pytest passed with 73 tests, 1 warning, and 6 subtests; a real runtime worker smoke using the synced `C:\Users\RondleLiu\cow\skills\image-generation\scripts\generate.py` generated a local Grok image (`tmp\grok-worker-smoke-20260528_181728\grok-image-bdeb49fc.jpg`, 376514 bytes) with stdout containing only the JSON result and logs on stderr.
- `2026-05-28` pending commit `feat: add grok image generation`: Implemented PR 3 Grok/xAI image generation by migrating Hermes `plugins/image_gen/xai` provider behavior and `agent.image_gen_provider` b64/URL local-save semantics into `integrations/hermes_xai/image_gen.py`, while continuing to resolve credentials through PR 1 `resolve_xai_http_credentials()`. The provider posts to `/images/generations`, retries once after 401 with forced refresh, accepts `grok-imagine-image` and `grok-imagine-image-quality`, maps Grok aspect ratios/resolutions, saves b64 responses and downloaded URL responses to local files, refuses to send OAuth tokens to non-xAI origins, and sanitizes Authorization/token/callback-like values in user-facing errors. `ContextType.IMAGE_CREATE` now routes to Grok only when `text_to_image` is explicitly `xai`/`grok`; WeCom Bot and Enterprise WeChat app image send paths reuse existing `ReplyType.IMAGE` local-file delivery. Added `grok-image-generation` and extended the existing image-generation background job so ordinary image requests still default to Codex, while explicit Grok/xAI requests pass `runtime=grok`; quality/speed wording alone does not switch provider; inside Grok runtime, quality mode is explicit-only (high quality/quality mode/高清/高质量/精细), and all other Grok image jobs default to the fast model. Runtime skill copies for `image-generation` and `grok-image-generation` were synchronized. Validation: PR3 image/skill tests passed with 25 tests; PR1 Grok/Web regression passed with 41 tests and 8 subtests; PR2 voice regression passed with 38 tests; `py_compile` passed for the Grok image provider, routing, channels, image-generation job manager/tool/script, and skill cache. Live OAuth smoke generated a standard Grok image (`tmp\xai_grok-imagine-image_20260528_174943_0aef0f27.jpg`, 355762 bytes) and a quality-model Grok image (`tmp\xai_grok-imagine-image-quality_20260528_180007_b40cac83.jpg`, 179256 bytes); both tmp outputs remain untracked.
- `2026-05-28` pending commit `fix: gate legacy visual pollution repair`: Added the legacy public protocol close-the-loop flow. `visual_extractors.py` and `text_sanitizer.py` now treat lowercase/snake_case UCIe signals, stacked bit ranges, MsgInfo/MsgCode/MsgSubcode fields, bit-field rows, sheet markers, and signal/list/register/message encoding headers as table/encoding negatives instead of formula candidates, while preserving UCIe CRC/VTF/loss formula candidates. `scripts/repair_knowledge_text_chunks.py` now reports `visual_replacement_*`, `*_before`, candidate strip counts, skipped pages/chunks, examples, backup path and dry-run state; apply always backs up SQLite, strips only ordinary chunks on pages covered by high-confidence retrievable visual_analysis replacements and only with explicit `strip_completed_visual_regions`, preserves visual_analysis chunks, and refreshes FTS. Added service/admin entry `complete_and_repair_legacy_visual_knowledge` / `repair_legacy_visual_pollution` and CLI `--repair-legacy-visual-pollution`, `--repair-dry-run`, `--repair-apply`, `--strip-completed-visual-regions`, `--document-id`, `--kb-id`, `--all`, `--visual-analysis-backend`, `--visual-force-prepare`.
  Validation: `PYTHONPATH=. .\.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_repair_chunks.py` -> 174 passed in 21.49s. `node --check channel\web\static\js\console.js` -> passed. `py -3 -m py_compile agent\knowledge\backend\visual_extractors.py agent\knowledge\backend\visual_analyzer.py agent\knowledge\backend\text_sanitizer.py agent\knowledge\backend\service.py scripts\repair_knowledge_text_chunks.py scripts\build_knowledge_backend.py` -> passed. Copy-library prepare smoke in `C:\Users\Hi\AppData\Local\Temp\cowwecom-legacy-smoke-tfiwmzm9`: 3 source PDFs done, 405 artifacts, generated artifact/chunk count 0, zero bbox page-size count 0, AMBA AXI pages 26/29 no formula, AXI4-Stream pages 18/24 no formula, UCIe pages 204/205/288/302/303 no formula and have table, UCIe pages 73/99/161/162/178/189 still have formula, UCIe Figure 5-10 found on page 154 as figure, Figure 5-15 bad step-caption count 0. Repair dry-run smoke on the copy reported 31 formula-garble chunks and 32 large-table-like chunks, skipped 55 polluted chunks without visual replacements, no backup, DB size unchanged. Apply smoke with synthetic high/low visual chunks created backup, stripped only the high-confidence replacement page, preserved low-confidence pollution, preserved visual chunks, and refreshed FTS.
  Notes: no real visual model was invoked; validation covers candidate extraction, visual-gated repair safety, reports, backups, and FTS behavior, not final answer quality improvements from model-produced visual summaries.
- `2026-05-28` pending commit `fix: close grok pr2 review follow-ups`: Closed the PR 2 fixed-version review blockers. `grok_voice_conversation_mode_enabled=true` now independently enables conversation-mode voice replies for original voice input on allowed WeCom channels even when the low-gated `grok_voice_reply_enabled` switch is false; text input still returns `input_is_not_voice` before any voice-mode override. `grok_voice_low_latency_backend`/`grok_voice_low_latency_model` now populate the current `LLMRequest` as request-level routing fields, so voice requests can use a configured backend/model without mutating global backend state, while text requests receive no backend/model override. `ChatChannel._send_reply()` now suppresses final text after a successful voice stream, but leaves full text fallback intact when every voice segment fails. WeCom Bot voice message objects now carry `origin_ctype=ContextType.VOICE`, and `pyproject.toml` now includes the runtime/test dependencies already required by the review path (`web.py`, `pycryptodome`, `pydub`, `wechatpy`, `PyYAML`, `python-dotenv`, and related base deps). Benchmark notes/config suggestions now reflect request-level backend override instead of claiming voice mode cannot cross backend. Validation: PR1 Grok/Web regression passed with 45 tests and 8 subtests; PR2 voice regression passed with 38 tests; compileall passed for integrations/hermes_xai, voice, channel, bridge, agent, common, models, config.py, and scripts; benchmark runs for capi_monthly, capi_quota, and codex all completed successfully with include-tts=false and 0 failure rate.
- `2026-05-28` pending commit `fix: enforce grok voice conversation gates`: Implemented the PR 2 review follow-up dual Grok/xAI voice rule. Added `VoiceModeDecision` and `voice_mode_decision` events so voice streaming is enabled only for conversation-mode voice input on allowed WeCom channels or non-conversation local `low_*` voice input; text input and disallowed channels stay text. Legacy `always_reply_voice`/`voice_reply_voice` no longer route `text_to_voice=xai/grok` through `build_text_to_voice`, keeping Grok voice behind the controlled streamer. Conversation mode forces low reasoning, short spoken answers, request-level backend/model selection, `max_output_tokens`, and streaming delta segmentation before TTS; non-conversation mode preserves the local-low gate. Added `scripts/benchmark_voice_mode_models.py`; CAPI quota and Codex benchmark runs both succeeded 15/15 with complex prompt output constrained to ~96-141 chars after stricter voice prompts. CAPI quota `gpt-5.5`: p50 first_voice_ready 2855ms, p95 5605.8ms, quality 2.48; Codex `gpt-5.5` low run: p50 2214ms, p95 3481ms, quality 2.48. A separate Codex reasoning-depth comparison confirmed the user's latency concern: Codex `low` was p50 2613.5ms / p95 4028.15ms with one transient provider network error, while Codex `xhigh` was p50 6206ms / p95 8785.2ms with 15/15 success; quality was effectively tied, so `grok_voice_force_reasoning_effort=low` is required for voice conversations. The benchmark recommendation can now be applied by leaving `grok_voice_low_latency_backend` empty for the current backend or by setting `grok_voice_low_latency_backend` plus `grok_voice_low_latency_model` for a single voice-request override; this does not change the global backend and text requests do not receive the override. Follow-up template/default update: `config-template.json` now defaults new deployments to `llm_backend.current_backend=codex`, Codex `gpt-5.5`, Grok voice reply enabled, and conversation mode low reasoning, while Codex provider default reasoning remains `xhigh` for non-voice requests. Validation: PR2 voice pytest passed with 32 tests; PR1 Grok/OAuth/Responses/Web regression passed with 41 tests and 8 subtests; focused new tests passed with 17 tests after the stricter prompt/max-output and benchmark reasoning-effort update; `compileall` passed for integrations/hermes_xai, voice, channel, bridge, agent, common, models, config.py, and scripts.
- `2026-05-28` pending commit `fix: harden visual prepare extraction`: Closed the follow-up blockers for public protocol visual prepare/candidate extraction. `visual_extractors.py` now treats PyMuPDF `page.find_tables()` as an explicitly controlled capability: disabled by default, optional per-document call/page budget, optional subprocess isolation with per-page timeout, and report counters for disabled/skipped/timeout/error/budget-exhausted paths; heuristic caption/dense/signal-table table candidates remain active when PyMuPDF layout support is absent or disabled. Signal-table negative guards now run before formula detection, so AMBA AXI signal rows such as `AWLEN[3:0]`/`ARLEN[3:0]` and AXI4-Stream rows such as `TDATA[(8n-1):0]`, `TSTRB[(n-1):0]`, `TKEEP[(n-1):0]`, `TID[(i-1):0]`, `TDEST[(d-1):0]`, and `TUSER[(u-1):0]` become table/signal context instead of formula candidates, while UCIe CRC/VTF/loss formula pages still produce formula candidates. `service.py` now splits a large `prepare_visual_artifacts(force=True, max_pages=1000)` request into configurable sub-batches, clears stale/forced visual cache once per document, writes artifacts/state after each sub-batch, advances `next_page`/`prepared_pages`/`prepared_artifacts`, refreshes grouping windows incrementally, and records batch failures as failed state instead of leaving `running` with 0 artifacts. Added regression tests for sub-batch checkpointing/failure state, find_tables budget behavior including explicit zero budget, AMBA AXI/AXI4-Stream signal-table formula false positives, and UCIe real formula preservation. Validation: `$env:PYTHONUTF8='1'; $env:PYTHONPATH='.'; .\.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_repair_chunks.py` passed with 167 tests in 21.87s; `node --check channel\web\static\js\console.js` passed; `$env:PYTHONUTF8='1'; .\.venv\Scripts\python.exe -m py_compile agent\knowledge\backend\visual_extractors.py agent\knowledge\backend\visual_analyzer.py agent\knowledge\backend\text_sanitizer.py agent\knowledge\backend\service.py scripts\repair_knowledge_text_chunks.py scripts\build_knowledge_backend.py` passed. Copy-library smoke used temp copy `C:\Users\Hi\AppData\Local\Temp\cowwecom-visual-smoke-bkkypy4t\public_protocol_knowledge` and `prepare_visual_artifacts(force=True, max_pages=1000)` completed in 25.39s without polluting the source library: 512 prepared pages, 412 prepared artifacts, `prepare_report={find_tables_calls:0, find_tables_skipped:149, find_tables_disabled:356, find_tables_timeouts:0, find_tables_errors:0, find_tables_budget_exhausted:0}`; all three source documents were `done` (`AXI4-Stream`: 42 pages/14 artifacts, `AMBA AXI`: 132 pages/53 artifacts, `UCIe 1.1`: 338 pages/345 artifacts); generated docs (`codex_analysis`, `llm_study`) had 0 visual artifacts and 0 visual chunks; zero bbox page-size count was 0; UCIe Figure 5-10 was `figure`, Figure 5-15 bad step-caption count was 0; AMBA AXI pages 26/29 and AXI4-Stream pages 18/24 had no formula candidates; UCIe pages 73/99/161/162/178/189 retained formula candidates. No real vision model was invoked, so this validates prepare/candidate/group-ready behavior rather than final formula/table QA quality.
- `2026-05-28` pending commit `fix: merge local token usage aliases`: Local token usage queries now read CowAgent runtime `llm_cache_usage.jsonl` directly, pass configured `llm_usage_user_aliases` into the token-usage skill, and expand admin replies with per-user request/input/output/total/cache/reasoning details. The reply counts only identified human labels as users and folds unlabeled/system identities into a separate bucket. `common.llm_usage_tracker` now applies the same alias map for Web/report aggregation. Added local ignored config aliases to merge `Rondo0323` and old duplicate hashes into `LiuHao`/`riko` without rewriting source JSONL. Bill screenshot handling now strengthens the image prompt, parses `成交价/实付/合计` before rounded summaries, and only lets follow-up text override amount when the user explicitly mentions an amount. Runtime copies of `token-usage-tracker` and `china-expense-ledger` scripts were synchronized. Validation: focused pytest for token usage, CowCli natural language, LLM usage report, and China expense ledger passed with 97 tests; targeted `py_compile` passed; live local summary for 2026-05-28 showed 2 identified users (`LiuHao`, `riko`) plus 2 unattributed/system identities after alias merge.
- `2026-05-28` pending commit `fix: harden visual prepare candidates`: Closed the local knowledge visual-completion PR blockers in the prepare/candidate path after first fast-forward merging remote `origin/main` from `207d1e5` to `eb3d104`. `visual_extractors.py` now parses caption label kind before keyword fallback, so `Figure 5-10 ... for Table 5-6` remains a figure; label-only caption stitching only accepts short caption-title-like next blocks and rejects list/step/body sentences; all visual candidates receive real `page_width`/`page_height`; PyMuPDF `find_tables()` is gated by table/caption/dense/continuation hints and reports call/skip counts; formula candidate extraction skips dense table blocks, signal-list rows, table headers, and table-overlapped blocks unless there is formula context plus formula structure or strong garble evidence. `text_sanitizer.py` reuses the stricter label-only caption stitching guard. `service.py` surfaces `prepare_report.find_tables_calls/find_tables_skipped` through prepare/build results for review. Added regressions for Figure title Table references, label-only step rejection, legal ARM/AMBA multiline captions, signal table no-formula candidates, formula candidate preservation, bbox page size, and `find_tables()` precheck stats. Validation: with the real Python path prepended because bare `python` is the WindowsApps alias, `$env:PYTHONPATH='.'; python -m pytest -q tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_repair_chunks.py` passed with 160 tests; `node --check channel/web/static/js/console.js` passed; `python -m py_compile agent/knowledge/backend/visual_extractors.py agent/knowledge/backend/visual_analyzer.py agent/knowledge/backend/text_sanitizer.py agent/knowledge/backend/service.py scripts/repair_knowledge_text_chunks.py scripts/build_knowledge_backend.py` passed. Manual caption matrix matched the requested True/False cases. Copy-library smoke used temp copy `C:\Users\Hi\AppData\Local\Temp\cowwecom-visual-smoke-w063emq9`, `workspace_root` pointed at the temp parent, and no original public protocol DB was modified. `prepare_visual_artifacts(force=True, max_pages=1000)` completed in 59.36s over 512 pages and 3 source PDFs, produced 524 pending artifacts, `prepare_report={find_tables_calls:353, find_tables_skipped:152}`, and all 3 source prepare states were `done`; generated docs (`llm_study`, `codex_analysis`) had 0 visual artifacts and 0 visual chunks; source visual chunks after prepare remained 0 because no vision model was called; zero bbox page-size count was 0. Smoke spot checks: UCIe `Figure 5-10.\nReceiver termination map for Table 5-6...` was `artifact_type=figure`; UCIe Figure 5-15 bad step-caption count was 0; AMBA signal pages 14/15/17/18/19/20/21/22 had 0 formula candidates; AMBA formula candidates remained only 3 pages outside those signal pages; UCIe formula candidates were still present. No real vision model was invoked, so this only validates prepare/candidate/group-ready data, not final formula/table QA quality.
- `2026-05-28` pending commit `fix: tune native wecom voice quality`: Kept Grok TTS replies on native Enterprise WeChat voice bubbles while improving the AMR path. `voice/audio_convert.py` now prepares WeCom voice audio through mono downmix, configurable loudness normalization (`wecom_voice_normalize_target_dbfs=-18.0`) with peak headroom (`wecom_voice_normalize_headroom_db=1.0`), and deterministic AMR-NB export at `wecom_voice_amr_bitrate=12.2k`; invalid bitrate config falls back to `12.2k`. Grok voice streaming defaults now favor fewer fragments (`grok_voice_max_segment_chars=180`, `grok_voice_min_segment_chars=18`, `grok_voice_flush_idle_ms=1500`) while retaining the native AMR bubble send path. README and quickstart document that this improves consistency but cannot exceed the WeCom AMR telephone-quality ceiling. Validation: `.venv\Scripts\python.exe -m pytest -q tests/test_wecom_voice_split.py tests/test_voice_reply_streamer.py tests/test_grok_config_template.py tests/test_wecom_voice_send.py tests/test_voice_mode_reasoning_policy.py tests/test_wecom_bot_voice_marker.py` passed with 20 tests; `.venv\Scripts\python.exe -m py_compile voice\audio_convert.py channel\voice_streamer.py config.py` passed; a real pydub/ffmpeg smoke generated a valid `#!AMR` file through `split_audio_by_wecom_voice_limits()`.
- `2026-05-28` pending commit `fix: smooth grok oauth voice setup`: Diagnosed local Grok login and WeCom voice reply failures after the Grok TTS update. The local callback server was receiving loopback callbacks with code/state, but `/grok` required a manual poll and manual paste attempts without both `code` and `state` failed before token exchange. The Grok gray-login page now auto-polls after login start, the backend exposes a pending-callback completion helper, and the manual endpoint falls back to an already-received loopback callback when pasted input is incomplete. Live WeCom voice testing then showed Grok TTS generated MP3 files but WeCom voice upload fell back to text because the runtime venv lacked `pydub`; installed `pydub` locally, verified `ffmpeg`/`ffprobe` are on PATH, added `pydub` and Python 3.13 `audioop-lts` to base `requirements.txt`, documented the ffmpeg deployment requirement, and ignored local `data/auth/` so Grok OAuth tokens cannot be staged accidentally. Validation: `.venv\Scripts\python.exe -m pytest -q tests/test_grok_auth.py tests/test_grok_web_gray.py tests/test_voice_mode_reasoning_policy.py tests/test_voice_reply_streamer.py tests/test_wecom_voice_send.py tests/test_wecom_voice_split.py tests/test_wecom_bot_voice_marker.py` passed with 33 tests and 6 subtests; `node --check channel\web\static\js\grok.js` passed; `.venv\Scripts\python.exe -m py_compile integrations\hermes_xai\auth.py channel\web\web_channel.py voice\audio_convert.py channel\wecom_bot\wecom_bot_channel.py` passed; `.venv\Scripts\python.exe -m pip check` passed; CowAgent restarted as PID `40952` and `/chat` returned HTTP 200. Live retest note: the post-restart phrase `今天太热了。` routed as `effort=xhigh/local`, so it correctly did not enter Grok voice streaming while `grok_voice_require_low_reasoning=true`; the phrase `你好。` routed as `effort=low/local`, generated Grok TTS files, and the user confirmed hearing the voice reply. Audio-quality follow-up: Grok source TTS is requested as 24kHz/128kbps MP3, but WeCom voice bubbles are converted to 8kHz mono AMR; a local ffmpeg probe showed AMR export already uses about 12.4kbps, so the remaining telephone-like quality is mainly the WeCom AMR voice-message limit rather than a low conversion setting.
- `2026-05-28` pending commit `feat: add grok tts wecom voice streaming`: Implemented PR 2 Grok TTS + Enterprise WeChat voice mode. xAI TTS is adapted from Hermes `tools/tts_tool.py` protocol logic and uses CowWeCom PR 1 `resolve_xai_http_credentials()` OAuth/API-key resolver without any TTS-only token path; `/tts` retries once after 401 with forced credential refresh and sanitizes Authorization/access/refresh/code fields in errors. Added `text_to_voice=xai/grok`, voice-origin markers across WechatCom app voice ASR and WeCom Bot voice-text input, low reasoning only for original voice short/simple local rules, a token-free `reasoning_effort_decision` event before LLM calls, segmented model-delta TTS streaming, per-session TTS worker/queue, AMR conversion/splitting to <=55s and <=1.9MB, Enterprise WeChat app voice upload/send, and WeCom Bot voice upload/send. Text inputs never trigger voice reply; voice inputs that route to medium/xhigh stay text; voice stream success suppresses the final duplicate full text, while full stream failure leaves the final text fallback intact. Config defaults added for Grok TTS, voice streaming gates, WeCom voice limits, and `reasoning_effort_policy_low_effort`. Key call chain: incoming voice sets `context["input_is_voice"]=True`, `source_msgtype="voice"`, `origin_ctype=ContextType.VOICE`; AgentBridge copies `input_is_voice` to the model; local policy emits `low_*` only for short simple voice tasks; `AgentStreamExecutor` emits `reasoning_effort_decision`; `AgentEventHandler` creates `VoiceReplyStreamer`; streamer splits LLM deltas by sentence/max chars/idle/end, calls `generate_xai_tts()`, channel VOICE send converts/splits AMR, uploads `media type=voice`, sends voice, then suppresses final text only after a voice segment succeeds. Validation: `py -3 -m pytest tests/test_grok_auth.py tests/test_grok_http_credentials.py tests/test_grok_bot_factory.py tests/test_grok_responses_adapter.py tests/test_openai_responses_api.py tests/test_grok_agent_model_routing.py tests/test_grok_config_template.py tests/test_grok_web_gray.py -q` passed with 38 tests; `py -3 -m pytest tests/test_grok_tts.py tests/test_voice_mode_reasoning_policy.py tests/test_wecom_voice_split.py tests/test_wecom_bot_voice_marker.py tests/test_voice_reply_streamer.py tests/test_wecom_voice_send.py -q` passed with 17 tests; `py -3 -m pytest tests/test_grok_tts.py tests/test_voice_mode_reasoning_policy.py tests/test_wecom_voice_split.py tests/test_wecom_bot_voice_marker.py tests/test_voice_reply_streamer.py tests/test_wecom_voice_send.py tests/test_grok_config_template.py -q` passed with 21 tests after the WeCom Bot VOICE support regression; `py -3 -m compileall -q integrations/hermes_xai voice channel bridge agent common models config.py` passed. Manual validation still needed with a real Grok OAuth account and live Enterprise WeChat app/Bot to verify actual xAI audio bytes, WeCom AMR playback, multi-segment ordering, and platform behavior for repeated voice sends on live chats.
- `2026-05-28` pending commit `fix: harden grok pr1 review blockers`: Closed PR 1 review blockers for Grok/xAI text chat. Agent model routing now resolves `bot_type=grok`/`xai` to `grok_model` (default `grok-4.3`) instead of the generic `model`, and `GrokBot.call_with_tools()` overwrites any incoming non-Grok `model` kwarg before building the xAI Responses payload. Manual OAuth paste now requires a full callback URL or `code`+`state` query string by default; bare authorization code compatibility is gated by `grok_oauth_accept_bare_code=true`, requires an active PKCE login session, and never logs the pasted code or verifier. Web Grok copy now asks for callback URL/query string, status/start/test responses remain token-free, and `config-template.json` exposes `bot_type` plus a Grok enable note without changing the default backend. Validation: `py -3 -m pytest tests/test_grok_auth.py tests/test_grok_web_gray.py tests/test_grok_agent_model_routing.py tests/test_grok_config_template.py -q` passed with 21 tests; `py -3 -m pytest tests/test_grok_agent_model_routing.py -q` passed with 4 tests; `py -3 -m compileall -q integrations/hermes_xai models/grok common/llm_backend_router.py config.py bridge/agent_bridge.py channel/web/web_channel.py` passed; `node --check channel/web/static/js/grok.js` passed. No PR 2/3/4 TTS/image/video capability was added.
- `2026-05-28` pending commit `fix: harden visual knowledge repair boundaries`: Closed the PR follow-up gaps for visual knowledge repair. Strict caption parsing now rejects label-only lines such as `Figure 5-19.` and `Table 3-9.` without losing ARM/AMBA punctuationless captions or label-line-plus-next-title blocks. Visual completion scope now honors document-only, KB-only, or all-source-documents-across-KB selection and keeps generated docs (`llm_study`, `codex_analysis`, etc.) out; `analyze_visual_artifact_group()` returns resolved `analysis_backend`, requested backend, and model metadata. Formula/equation artifacts are now a canonical visual type with prompt/schema/search-term support, and PyMuPDF preparation adds guarded formula candidates only on formula context/garble evidence without relaxing low-confidence gates. Table preparation now uses guarded `find_tables()`, dense table block detection, caption-table rect preference, and continuation hints for multi-page tables. The repair script reports `formula_garble_*` and `large_table_like_*` quality stats, defaults to report-only for formula/table noise, and adds `--strip-completed-visual-regions` so ordinary chunk cleanup occurs only when the same page already has high-confidence retrievable visual chunks. Validation: `.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_repair_chunks.py` passed with 156 tests; `node --check channel/web/static/js/console.js` passed; `.venv\Scripts\python.exe -m py_compile agent/knowledge/backend/visual_extractors.py agent/knowledge/backend/visual_analyzer.py agent/knowledge/backend/text_sanitizer.py agent/knowledge/backend/service.py scripts/repair_knowledge_text_chunks.py scripts/build_knowledge_backend.py` passed. Copy-library smoke used a temp copy of `public_protocol_knowledge` at `C:\Users\Hi\AppData\Local\Temp\cowagent-visual-smoke-4d4be6ba3ae34eeb9951b0293f063c93`, prepared 706 visual artifacts over 512 pages for 3 source documents, confirmed 3 generated documents had 0 artifacts/chunks, confirmed label-only caption checks are false, and confirmed all-source completion would target 3 source documents instead of the default KB only. No real vision model was called in that smoke, so formula QA success is limited to candidate/report/fake-analyzer coverage.
- `2026-05-28` pending commit `fix: harden visual knowledge completion`: Finished the follow-up visual knowledge repair pass. PDF caption detection now rejects body Figure/Table reference sentences before accepting captions, including single references, comma/and clusters, mixed Figure/Table references, parenthesized qualifiers, and `on page ... shows/lists` forms, while still accepting ARM/AMBA punctuationless captions such as `Figure 2-1 TVALID before TREADY handshake`; TOC/List pages remain skipped. Source-document selection is now a shared whitelist of `(doc_type or "document") == "document"` across service and Web console, so full-KB visual completion processes only original source documents and explicitly rejects selected generated docs such as `llm_study`, `codex_analysis`, and `visual_analysis`. The main Web button now calls `/api/knowledge/admin/visual/complete` with `analysis_backend=current`, retry accepts explicit analysis backends and records the resolved model, page fallback was made reachable without relaxing confidence gates, and the CLI preserves existing KB config when `--kb-id` is used while surfacing visual errors in JSON. Validation: `PYTHONPATH=. .\.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_repair_chunks.py` passed with 140 tests; `node --check channel/web/static/js/console.js` passed; `PYTHONPATH=. .\.venv\Scripts\python.exe -m py_compile agent\knowledge\backend\visual_extractors.py agent\knowledge\backend\service.py scripts\build_knowledge_backend.py` passed.
- `2026-05-28` pending commit `docs: note public knowledge export cleanup`: Documented the post-pull migration for repaired public protocol knowledge. Retrieval/deep query reads the committed `public_protocol_knowledge/indexes/kb.sqlite`, so remote machines that pull this update use the fixed UCIe/AMBA AXI/AXI4-Stream chunks for actual knowledge queries. The Web-readable Markdown library must be refreshed with `scripts/export_knowledge_backend_docs.py` into `knowledge/documents/<kb_id>/`, and any legacy `~/cow/knowledge/protocols/` export should be deleted or it can keep stale Figure/Table/word-break artifacts visible in the Web console list. Local verification before this note: `/api/knowledge/list` showed `protocol_paths_count=0` after deleting `C:\Users\Hi\cow\knowledge\protocols`; `/api/knowledge/read` for `documents/ucie_1_1/ucie-1.1-specification-9c80c7e4.md` returned success without old span `030936c179e2933d7502dd37` or `ble 1-2`, while the old `protocols/...` path returned `file not found`.
- `2026-05-28` pending commit `feat: add grok oauth chat integration`: Added CowWeCom Grok/xAI PR1 text-chat integration by source-level migrating Hermes xAI OAuth constants, PKCE/login/session handling, discovery validation, token exchange/refresh, refresh skew, xAI credential resolution, Responses compatibility, and Grok reasoning-effort allowlist into a CowWeCom-owned `integrations/hermes_xai` adapter. Tokens now persist atomically to `data/auth/grok_auth.json` or configured `grok_auth_file` with chmod best effort and process locks; Web APIs expose `/api/grok/status`, `/api/grok/login/start`, `/api/grok/login/poll`, `/api/grok/login/manual`, `/api/grok/logout`, and `/api/grok/test` without returning access tokens, refresh tokens, authorization codes, code verifiers, or full callback URLs. `bot_type=grok` and `bot_type=xai` create `GrokBot`, use OAuth credentials before `XAI_API_KEY`/`grok_api_key`, force xAI OAuth bearers to xAI origins, and send text via Responses API with `x-grok-conv-id`, raw session `prompt_cache_key`, service-tier removal, xAI tool-schema sanitization, and model allowlisted `reasoning.effort`. Updated config template/Web provider metadata/README. Validation: `py -3 -m pytest tests/test_grok_auth.py tests/test_grok_http_credentials.py tests/test_grok_bot_factory.py tests/test_grok_responses_adapter.py tests/test_openai_responses_api.py -q` passed with 22 tests; safe token/code search over Grok touched paths found no Web response/log path returning secrets.
- `2026-05-28` pending commit `fix: keep grok login gray-only`: Added a hidden authenticated `/grok` gray-login page backed by the existing Grok OAuth APIs, and added `grok_gray_enabled=false` so the normal Web config provider selector hides Grok by default. This lets admins log in and test `/api/grok/test` without changing the currently selected production backend; Grok only becomes a selectable provider when `grok_gray_enabled=true`, and chat only routes to Grok when `bot_type` is explicitly set to `grok` or `xai`. Installed missing global-test dependencies `web.py` and `legacy-cgi` for Python 3.13; `requirements.txt` already lists both runtime dependencies. Validation: `py -3 -c "import web; print('web import ok')"` passed; `.venv\Scripts\python.exe -c "import web; print('venv web import ok')"` passed; `py -3 -m pytest tests/test_grok_auth.py tests/test_grok_http_credentials.py tests/test_grok_bot_factory.py tests/test_grok_responses_adapter.py tests/test_grok_web_gray.py tests/test_openai_responses_api.py -q` passed with 24 tests; `py -3 -m compileall -q channel/web/web_channel.py integrations/hermes_xai models/grok config.py` passed; `node --check channel/web/static/js/grok.js` passed.
- `2026-05-28` pending commit `fix: accept grok build manual auth code`: Updated the Grok OAuth manual path to accept either a full loopback callback URL/query string or the bare authorization code shown by Grok Build when the browser cannot connect to `127.0.0.1:56121`. Bare codes are only accepted while a current PKCE login session exists, and they reuse that session's state, redirect URI, PKCE verifier, and token exchange path; full callback URLs still validate host, path, scheme, port, and state. `/grok` now advertises callback URL or code paste and cache-busts its JS. Validation: `py -3 -m pytest tests/test_grok_auth.py tests/test_grok_http_credentials.py tests/test_grok_bot_factory.py tests/test_grok_responses_adapter.py tests/test_grok_web_gray.py tests/test_openai_responses_api.py -q` passed with 26 tests; `py -3 -m compileall -q integrations/hermes_xai/auth.py channel/web/web_channel.py` passed; `node --check channel/web/static/js/grok.js` passed.
- `2026-05-28` pending commit `fix: clarify grok gray login status`: Hid stale Grok authorize/manual-paste controls after OAuth status becomes logged-in, while keeping credential test/logout visible. The gray page now renders missing email as `xAI 未返回` instead of a generic dash, and auth storage extracts `profile.email` from `id_token` when xAI supplies it without exposing tokens in status responses. Validation: `py -3 -m pytest tests/test_grok_auth.py tests/test_grok_http_credentials.py tests/test_grok_bot_factory.py tests/test_grok_responses_adapter.py tests/test_grok_web_gray.py tests/test_openai_responses_api.py -q` passed with 27 tests; `py -3 -m compileall -q integrations/hermes_xai/auth.py` passed; `node --check channel/web/static/js/grok.js` passed.
- `2026-05-28` pending commit `fix: repair public protocol text chunks`: Applied `scripts/repair_knowledge_text_chunks.py` to the committed public protocol SQLite. Flow: UCIe-scoped dry-run first returned `ok=true`, `failed=0`, `old_text_chunks=632`, `new_text_chunks=575`, `old_total_chars=858429`, `new_total_chars=757699`, and only skipped the non-PDF Codex analysis document; full `--all --dry-run` returned `selected_documents=5`, `processed=3`, `skipped=2`, `failed=0`, `old_text_chunks=879`, `new_text_chunks=821`, `old_total_chars=1139933`, `new_total_chars=1038711`; full `--all --apply` produced the same summary and backup `public_protocol_knowledge/indexes/kb.sqlite.backup-20260528-093518`. Manual before/after spot checks against the backup showed UCIe Figure/Table bad-marker chunks dropping from 6 to 0, AMBA AXI v2.0 Figure/Table bad-marker chunks dropping from 3 to 0, and AXI4-Stream staying at 0; sampled pages confirmed Figure 4-2/4-3/4-4, Figure 5-34, Table 7-2, AXI Figure 1-4/1-5/1-6, and AXI Table 5-1 no longer contain the old lane/byte/timing/cache-table noise. SQLite `PRAGMA integrity_check` returned `ok`; UCIe retained query hits for `D2D Adapter`, `UCIe Link Training`, `D2D Adapter`, `PHYRETRAIN`, `Table 4-11`, and `Sideband`. Validation reports refreshed to pass for `ucie_1_1`, `amba_axi_v2_0`, and `axi4_stream`; focused repair tests passed with 34 tests and compileall passed for the repair/storage/service modules. Backup and older failed/diagnostic repair reports are intentionally left untracked.
- `2026-05-28` pending commit `fix: harden visual chunk append ids`: Closed the remaining `append_visual_chunks()` chunk-ID boundary. Visual chunk append now runs inside a savepoint, deletes same-artifact/group visual chunks before idempotent reinsert, remaps incoming visual chunk IDs that collide with ordinary chunks, other artifacts, groups, or other documents, uses plain `INSERT INTO chunks`, defensively removes stale FTS rows for inserted chunk IDs, and keeps `source_span_ids` unchanged when only the chunk ID is remapped. `append_visual_group_chunks()` now writes base group chunks and group/member mappings in one savepoint instead of committing halfway through. Added regressions for repeat append without orphan spans or duplicate FTS, ordinary chunk collision remap, other-document chunk collision remap, source-span rollback on chunk insert failure, and group append rollback on member mapping failure. Validation: `PYTHONPATH=. pytest -q tests/test_knowledge_backend_repair_chunks.py` passed with 34 tests; `PYTHONPATH=. pytest -q tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py` passed with 86 passed and 6 skipped; `PYTHONPATH=. python -m compileall -q scripts/repair_knowledge_text_chunks.py agent/knowledge/backend/storage.py agent/knowledge/backend/service.py` passed; `PYTHONPATH=. python scripts/repair_knowledge_text_chunks.py --db public_protocol_knowledge/indexes/kb.sqlite --document-id 9c80c7e49f1cf343c0309aad --dry-run --workspace-root .` returned `ok=true`, `selected_documents=1`, `old_text_chunks=632`, `new_text_chunks=623`, `preserved_visual_chunks=0`, and wrote `public_protocol_knowledge/reports/repair-text-chunks-20260528-080512.json`.
- `2026-05-28` pending commit `test: harden visual group retry regressions`: Added non-blocking visual group regression coverage without changing production behavior beyond short storage docstrings. New tests assert group member retry success removes old `visual_artifact_chunks` mappings, retry low-confidence removes old group rows from `chunks`, `chunks_fts`, `source_spans`, and `visual_artifact_chunks`, group success sets `group_retrievable=1` only for real current members, stale group-member cleanup deletes multiple stale rows in one call, and missing raw group `source_pages` falls back to group/member pages while preserving the metadata reason `source_pages missing in model output; filled from group metadata`. Added docstrings for `_mark_visual_group_not_retrievable()`, `invalidate_visual_group_for_member_analysis_change()`, and `resolve_visual_group_id_for_members()` to document stale-membership filtering and chunk cleanup boundaries. Validation: `.venv\Scripts\python.exe -m py_compile agent\knowledge\backend\visual_grouping.py agent\knowledge\backend\visual_analyzer.py agent\knowledge\backend\service.py agent\knowledge\backend\storage.py agent\knowledge\backend\models.py agent\knowledge\backend\visual_extractors.py` passed; `PYTHONPATH=. .venv\Scripts\python.exe -m pytest -q tests\test_knowledge_backend_visual.py` passed with 68 passed and 3 skipped; `node --check channel\web\static\js\console.js` passed; safe-upload preflight passed before and after staging; staged files were `GIT_NOTES.md`, `README.md`, `agent/knowledge/backend/storage.py`, and `tests/test_knowledge_backend_visual.py`; `git diff --cached --check` passed.

- `2026-05-28` pending commit `fix: harden visual source span cleanup`: Finished the KnowledgeStorage source-span integrity closure for visual append/delete paths. `append_visual_chunks()` now checks incoming visual span IDs against existing global `source_spans`, remaps conflicts into unique IDs, rewrites the appended chunk `source_span_ids`, and uses plain `INSERT` instead of `INSERT OR REPLACE`. Visual chunk deletion now flows through a shared helper that deletes chunks, `visual_artifact_chunks`, and FTS rows, rescans remaining chunks, prunes entity/relation graph references only for unreferenced spans, and preserves spans still used by ordinary or visual chunks; `reset_visual_cache()` now reports actual deleted source-span count. Added regressions for cross-document visual append conflicts, artifact deletion preserving shared ordinary spans, artifact deletion pruning unreferenced visual span graph refs, and reset preserving shared spans with accurate counts. Validation: `.venv\Scripts\python.exe -m pytest -q tests\test_knowledge_backend_repair_chunks.py` passed with 29 tests; `.venv\Scripts\python.exe -m pytest -q tests\test_knowledge_backend_pdf_sanitizer.py tests\test_knowledge_backend_visual.py tests\test_knowledge_backend_service.py` passed with 77 passed and 6 skipped; `.venv\Scripts\python.exe -m compileall -q scripts\repair_knowledge_text_chunks.py agent\knowledge\backend\storage.py agent\knowledge\backend\service.py` passed; `.venv\Scripts\python.exe scripts\repair_knowledge_text_chunks.py --db public_protocol_knowledge\indexes\kb.sqlite --document-id 9c80c7e49f1cf343c0309aad --dry-run --workspace-root .` returned `ok=true`, `selected_documents=1`, `old_text_chunks=632`, `new_text_chunks=623`, `preserved_visual_chunks=0`; safe-upload preflight passed before and after staging; staged files were `GIT_NOTES.md`, `README.md`, `agent/knowledge/backend/storage.py`, and `tests/test_knowledge_backend_repair_chunks.py`; `git diff --cached --check` passed.

- `2026-05-28` pending commit `fix: compact duplicate tool results`: Compacted same-turn duplicate tool-call reuse in `AgentStreamExecutor`. Identical same-turn calls still execute only once and still emit a protocol-compatible duplicate `tool_result`, but the duplicate block now contains only an original tool-use id, result hash, and character count instead of repeating the full result text. This keeps the first real read/bash/edit result available to the model while reducing context growth and cache perturbation from duplicate same-turn tool calls. Added regressions for duplicate `read`, `bash`, and `edit` calls. Validation: `.venv\Scripts\python.exe -m pytest -q tests\test_agent_stream_tool_attempt_memory.py` passed with 14 tests; `.venv\Scripts\python.exe -m py_compile agent\protocol\agent_stream.py tests\test_agent_stream_tool_attempt_memory.py` passed; safe-upload preflight passed before and after staging; `git diff --check` and `git diff --cached --check` passed.

- `2026-05-27` pending commit `fix: harden save document span cleanup`: Hardened `KnowledgeStorage.save_document()` and stale visual span cleanup. Incoming source spans now classify conflicts before `INSERT`: preserved visual/current remaining chunk spans and other-document span ids are protected and force incoming remap across chunks, entities, relations, and relation ids; current-document non-preserved conflicting spans, including orphan spans, are pruned from graph references and deleted before the ordinary insert. `_delete_stale_visual_chunks()` now deletes stale visual chunks first, recomputes remaining chunk references, prunes graph references only for unreferenced stale visual spans, and preserves spans still used by another chunk. Added regressions for current-document orphan span replacement, other-document global span remap, stale visual span graph pruning, and shared stale visual spans that remain referenced. Validation: `PYTHONPATH=. pytest -q tests/test_knowledge_backend_repair_chunks.py` passed with 25 tests; `PYTHONPATH=. pytest -q tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py` passed with 77 passed, 6 skipped; `PYTHONPATH=. python -m compileall -q scripts/repair_knowledge_text_chunks.py agent/knowledge/backend/storage.py agent/knowledge/backend/service.py` passed. The requested global-`python` repair dry-run failed because that interpreter lacks `pypdf`; rerunning the same dry-run with `.venv\Scripts\python.exe` succeeded with `ok=true`, `selected_documents=1`, `old_text_chunks=632`, `new_text_chunks=623`, `preserved_visual_chunks=0`.

- `2026-05-27` pending commit `fix: block low confidence visual group indexing`: Closed the latest visual knowledge review gaps. Page and group validation now require explicit model `should_index=true` plus zero validation reasons, so `visual_analysis.index_low_confidence=true` may preserve low-confidence JSON for admin review but cannot write low-confidence chunks/FTS rows. Group validation now derives multipage status from model, group metadata, and members; explicit `source_pages=[]` hard-fails while stored result JSON still carries sorted unique page metadata for inspection. Group member reanalysis through build force or direct retry now invalidates the parent group, deletes old group/member visual chunks, clears retrievable flags, and leaves the group pending or skipped until it is remerged. Resolver queries and cleanup now ignore/delete legacy stale membership rows whose artifact no longer points at the group. Validation: `python -m py_compile agent/knowledge/backend/visual_grouping.py agent/knowledge/backend/visual_analyzer.py agent/knowledge/backend/service.py agent/knowledge/backend/storage.py agent/knowledge/backend/models.py agent/knowledge/backend/visual_extractors.py` passed; `PYTHONPATH=. pytest -q tests/test_knowledge_backend_visual.py` passed with 63 passed, 3 skipped; `node --check channel/web/static/js/console.js` passed. Optional full `PYTHONPATH=. pytest -q` failed during collection because this global interpreter is missing optional dependencies `pandas`, `web`, `click`, `croniter`, `tzdata`, and `websocket`.

- `2026-05-27` pending commit `fix: continue after capi stream read errors`: Diagnosed the latest live WeCom Agent failure at 21:52:52 as `stream_read_error` / `upstream_error` from the `capi_monthly` streaming backend. `is_capi_runtime_fallback_error()` now classifies `stream_read_error`, `stream read error`, `upstream_error`, and `upstream error` as transient CAPI runtime fallback errors. Agent streaming also treats the same signature as retryable; after the retry budget it switches to the selected fallback backend and replays the same in-memory LLM request instead of surfacing `ERROR`. For this specific stream-read/upstream signature, the Agent emits a user-visible stream notice such as "已自动从 CAPI monthly card (capi_monthly) 切换到 Codex (codex)" and prepends the same notice to the final text for non-stream paths. Validation so far: `.venv\Scripts\python.exe -m pytest tests\test_agent_stream_capi_fallback.py tests\test_codex_backend_router.py -q` passed with 37 tests; touched files passed `py_compile`.

- `2026-05-27` pending commit `fix: harden visual knowledge completion gates`: Finished the production hardening pass for local document visual artifact completion. Group validation now separates hard reasons from threshold reasons so continuation confidence below 0.70, empty `source_pages`, multipage results with fewer than two parts, merged table rows without headers, explicit model `should_index=false`, and failed/low-confidence members without reliable fallback always keep group chunks out of retrieval even when `index_low_confidence=true`. Inferred groups now prefer existing overlapping group IDs, anchor new IDs on the earliest artifact instead of the current window end, drop stale member rows when artifacts move between groups, filter stale memberships at read time, and reject unstable/stale groups before merge. Dirty, skipped, low-confidence, failed, and force group paths clear group/member retrievable flags plus page/group chunks. Page reanalysis context now excludes existing `visual_analysis` chunks, grouping page windows filter in SQL before `limit=2000`, group model merge uses multi-image content when supported and falls back to text when images/backend support are unavailable, tile force/reuse now checks image hash/model/prompt and any failed/low-confidence tile blocks page indexing, group merging waits for prepare completion or lookahead stability, and visual reset clears mappings even when chunks have no spans. Added regressions for all reviewed production risks. Validation before publish: `py -3 -m py_compile agent\knowledge\backend\visual_grouping.py agent\knowledge\backend\visual_analyzer.py agent\knowledge\backend\service.py agent\knowledge\backend\storage.py agent\knowledge\backend\models.py agent\knowledge\backend\visual_extractors.py` passed; `PYTHONPATH=. py -3 -m pytest -q tests\test_knowledge_backend_visual.py` passed with 58 tests; `node --check channel\web\static\js\console.js` passed. Optional full suite: `PYTHONPATH=. py -3 -m pytest -q tests` failed during collection because the global interpreter lacks `web`, `croniter`, and `websocket`; after installing `tzdata` into the ignored `.venv`, `.venv\Scripts\python.exe -m pytest -q tests` reached execution and reported 894 passed, 15 failed in unrelated non-visual areas tied to local AMAP/CAPI configuration, Windows temp-directory cleanup, image-generation timing, Qianfan README docs, and image-recognition/tool-memory tests.

- `2026-05-27` pending commit `fix: harden knowledge repair storage integrity`: Continued the public knowledge text-chunk repair hardening without reverting the existing data-dir/report/WAL/preserved-visual-span fixes. `KnowledgeStorage.save_document(commit=True)` now runs its full document replacement inside a savepoint and rolls back partial chunk/source-span/entity writes on any mid-save failure, while `commit=False` remains owned by the caller transaction for repair apply. Source-span deletion now prunes matching `entities.source_span_ids` and `knowledge_relations.evidence_span_ids` first; relations with all evidence pruned keep the relation and receive `evidence_pruned` metadata. The repair script also prunes references before deleting orphan spans, includes `message` and `error` in stdout JSON, and avoids overwriting same-second report files. Added regressions for save rollback, repair apply entity pruning, relation evidence pruning, CLI error output, and report path collision. Validation: `py -3 -m pytest -q tests/test_knowledge_backend_repair_chunks.py -q` passed with 21 tests; `py -3 -m compileall -q scripts/repair_knowledge_text_chunks.py agent/knowledge/backend/storage.py agent/knowledge/backend/service.py` passed; real dry-run wrote `public_protocol_knowledge/reports/repair-text-chunks-20260527-204315.json` with embedded report_path, `ok=true`, 632 old ordinary chunks, 575 new ordinary chunks, expected noise examples, and no backup. Current mixed worktree `py -3 -m pytest -q tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py -q` failed 2 visual tests (`test_group_continued_table_pages`, `test_tile_resume_reuses_succeeded_tiles`) in the parallel visual changes to `service.py`/visual modules; those files are not part of this repair/storage commit.

- `2026-05-27` pending commit `fix: repair text chunk edge cases`: Strictly preferred `<data_dir>/indexes/kb.sqlite` data-dir inference in `scripts/repair_knowledge_text_chunks.py`, even when project config points `sqlite_path` at the same DB but has a wrong `data_dir`. `KnowledgeStorage.save_document()` now remaps incoming ordinary `source_spans` that conflict with spans still referenced by preserved visual chunks, and rewrites incoming chunks/entities/relations plus relation IDs so preserved `visual_analysis` chunks keep their original spans without causing source-span primary-key collisions. Added repair regressions for the config mismatch and shared-span remap paths. Validation: clean worktree `D:\CowAgent_repair_verify` passed `D:\CowAgent\.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_repair_chunks.py -q` with 17 tests and `D:\CowAgent\.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py -q` with 46 tests; main worktree repair test passed with 17 tests; compileall passed; real dry-run wrote `public_protocol_knowledge/reports/repair-text-chunks-20260527-193948.json` under `public_protocol_knowledge/reports` with report_path embedded, 632 old ordinary chunks, 575 new ordinary chunks, and no backup.

- `2026-05-27` pending commit `fix: stabilize visual group indexing`: Hardened visual completion so grouped page artifacts no longer leave page-scope visual chunks behind, low-confidence/failed groups clear member page chunks, group pages are unioned across windows, and grown terminal groups reset to `pending` before re-merge. Page analysis now preserves `is_partial` and normalized `continuation`, high-res retry uses the high-res long-edge limit, tile analysis can reuse succeeded tiles and carries tile metadata into chunks, and grouping splits repeated captions by contiguous page chains while improving no-caption continuation detection. Also carried forward repair-script data-dir inference and preserved-span remap regressions. Validation: focused backend `py_compile` passed; `py -3 -m pytest tests\test_knowledge_backend_visual.py -q` passed with 38 tests; `py -3 -m pytest tests\test_knowledge_backend_repair_chunks.py -q` passed with 17 tests; `git diff --check` passed.

- `2026-05-27` pending commit `fix: harden knowledge text chunk repair`: Hardened `scripts/repair_knowledge_text_chunks.py` for real legacy public-protocol SQLite runs. The script now infers `data_dir` from `<data_dir>/indexes/kb.sqlite` before falling back to config, writes `report_path` into the report JSON, selects documents through a non-immutable read-only connection before creating apply backups, returns `ok=false` for missing `document_id` or `kb_id` without creating backups, and keeps stdout clean for JSON summaries. `KnowledgeStorage` now supports `immutable_read=False` for WAL-visible dry-runs and protects source spans still referenced by preserved chunks when ordinary chunks are replaced. Visual build now skips real credential checks for injected test analyzers while default `VisualAnalyzer` still enforces backend availability and returns zeroed stats on unavailable-backend errors. Added regressions for report paths, explicit data-dir override, selector misses, WAL reads, preserved shared spans, orphan span cleanup, and visual analyzer injection. Real dry-run report `public_protocol_knowledge/reports/repair-text-chunks-20260527-184032.json` shows UCIe 1.1 ordinary text chunks changing from 632 to 575, total ordinary chars from 858429 to 757699, and 7110 sanitizer-removed lines. Validation: `.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_repair_chunks.py -q` passed with 15 tests; `.venv\Scripts\python.exe -m pytest -q tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py -q` passed with 46 tests; compileall passed for the touched repair/storage/service modules; real dry-run without `--data-dir` wrote its report under `public_protocol_knowledge/reports`; CowAgent was restarted from `.venv` and `/chat` returned HTTP 200.

- `2026-05-27` pending commit `feat: add multipage visual artifact grouping`: Added cross-page visual artifact grouping for local document knowledge. Visual artifacts now keep page-level analysis but can be linked into stable group-level artifacts with members, roles, source pages, continuation confidence, group-level chunks, and low-confidence non-indexing. The grouping detects repeated captions, continued/续表/续图 markers, adjacent table/header continuity, and top/bottom large-figure continuity while skipping TOC/list pages. Group merge supports artifact-level resume, direct group retry, model-based group merge for small groups, text-based merge for larger groups, source-page attribution, and page-level chunk suppression. Dense single-page visuals can retry with high-resolution rendering or tile into overlapped vertical slices with tile results merged before indexing. Web status now reports group/tile/high-res progress, `AGENTS.md` contains concise README update-log rules, and README's update log was consolidated into short dated bullets. Validation: `py -3 -m pytest tests\test_knowledge_backend_visual.py -q` passed with 27 tests; PowerShell-expanded `py -3 -m pytest -q @files` for `tests/test_knowledge_backend*.py` passed with 82 tests; `py -3 -m compileall -q agent\knowledge\backend channel\web` passed; `node --check channel\web\static\js\console.js` passed; README update-log date uniqueness check passed.

- `2026-05-27` pending commit `fix: generalize document visual knowledge`: Generalized the local knowledge backend UI/export wording from protocol-specific to local document knowledge, with default exports under `knowledge/documents/<kb_id>/` and configurable `knowledge_backend.ingest.document_library_category`. Visual build responses now include prepared/scanned page deltas so the Web loop keeps advancing through pages with no candidates; caption detection now preserves multiline captions while rejecting body references such as "Figure X shows..." and dedupes image candidates covered by caption crops. Re-ingesting the same path with a new version removes old-version visual chunks/FTS/source-span mappings and marks old artifacts non-retrievable. Visual backend availability now reflects the resolved current backend, OpenAI-compatible vision timeout is configurable up to 600 seconds, and LLM study prompts are generic technical-document prompts. Validation: `py -3 -m pytest tests\test_knowledge_backend_visual.py tests\test_knowledge_backend_service.py tests\test_knowledge_backend_pdf_sanitizer.py tests\test_knowledge_backend_optional.py tests\test_knowledge_backend_complete.py -q` passed with 65 tests; `node --check channel\web\static\js\console.js` passed; focused `py_compile` passed.

- `2026-05-27` pending commit `fix: version visual knowledge pipeline`: Reviewed and integrated the external visual KB patch. The default local document backend path now uses `public_document_knowledge/indexes/kb.sqlite`, visual artifacts/prepare state/chunk metadata carry `pipeline_version`, stale visual pipeline caches are reset automatically when that version changes, and admin `visual/reset` can clear artifacts, chunks, source spans, and prepare state by document or KB. Caption-led crop windows now expand around caption position to cover large figures/tables, the Web document selector requires explicit choice for multi-document manual visual actions, and `visual_grouping.py` provides bbox area/IoU/coverage helpers. Validation: `py -3 -m pytest -q tests\test_knowledge_backend*.py` equivalent PowerShell-expanded run passed with 67 tests; `py -3 -m compileall -q agent\knowledge\backend channel\web` passed; `node --check channel\web\static\js\console.js` passed.

- `2026-05-27` pending commit `fix: remove redundant row visual build action`: Removed the redundant per-row `补全此文档` button from the Web protocol knowledge document list because the top document selector plus `补全图表知识` / `继续补全图表知识` already performs the same scoped build. Document rows now keep only the per-document `低置信` shortcut and exported-document open icon. Validation: `node --check channel\web\static\js\console.js` passed.

- `2026-05-27` pending commit `fix: make visual prepare incremental`: Changed visual chart completion so `visual/build` no longer synchronously scans and renders an entire PDF. Added `visual_prepare_states`, page-range candidate extraction, lazy per-artifact crop rendering, strict caption matching with TOC/List page skips, prepare progress in API/UI, multi-upload sequential auto-build, available-backend fallback selection, CAPI availability via effective OpenAI config, force-run duplicate-claim protection, visual FTS text enrichment, and `save_document()` preservation of existing `visual_analysis` chunks/source spans/FTS/artifact mappings when ordinary text chunks are refreshed. Validation: `py -3 -m pytest tests\test_knowledge_backend_visual.py tests\test_knowledge_backend_service.py tests\test_knowledge_backend_pdf_sanitizer.py tests\test_knowledge_backend_optional.py tests\test_knowledge_backend_complete.py -q` passed with 58 tests; `node --check channel\web\static\js\console.js` passed; focused `py_compile` passed.

- `2026-05-27` pending commit `fix: sanitize pdf visual text chunks`: Hardened the protocol knowledge backend so new PDF uploads sanitize ordinary text chunks before indexing. The sanitizer uses PyMuPDF block/image/caption regions where available, falls back to conservative line cleanup, preserves captions and natural language, and removes chart-internal split-character labels or signal-label noise from `Source Chunks`. Visual completion now supports document/kb filtering, selectable analysis backends (`current`, `capi`, `capi_monthly`, `codex`) without switching the global chat backend, per-artifact backend/model metadata, `retry_failed` semantics, `has_retryable_failed`, and a `visual/backends` admin API. The Web console now has document and visual-backend selectors, per-document completion/low-confidence actions, progress details, and one export after completion; exported Markdown keeps `visual_analysis` chunks out of `Source Chunks`. Validation: `py -3 -m pytest tests/test_knowledge_backend_pdf_sanitizer.py tests/test_knowledge_backend_visual.py tests/test_knowledge_backend_service.py tests/test_knowledge_backend_optional.py tests/test_knowledge_backend_complete.py -q` passed with 54 tests; focused `py_compile` passed; `node --check channel\web\static\js\console.js` passed.

- `2026-05-27` pending commit `fix: preserve protocol library indexes`: Fixed a Web-visible protocol document library export bug where `export_document_library(document_id=...)` rewrote `knowledge/protocols/index.md` and per-protocol indexes from only the selected document, hiding previously indexed protocols after a single-document upload/export. The exporter now writes the requested document page but rebuilds root and protocol indexes from all indexed documents, preferring already-existing document-library paths for generated analysis/study pages. Refreshed the local `C:\Users\Hi\cow\knowledge\protocols\index.md`, which now lists `amba_axi_v2_0`, `axi4_stream`, and `ucie_1_1`, each with two documents. Validation: `.venv\Scripts\python.exe -m pytest tests\test_knowledge_backend_service.py tests\test_knowledge_backend_complete.py tests\test_knowledge_backend_visual.py -q` passed with 24 tests; `.venv\Scripts\python.exe -m py_compile agent\knowledge\backend\service.py tests\test_knowledge_backend_service.py scripts\export_knowledge_backend_docs.py` passed; `scripts\export_knowledge_backend_docs.py` exported 6 documents and regenerated the local Markdown library.

- `2026-05-27` pending commit `fix: harden memory and backend failure notices`: Diagnosed the live `memory_get(error)` as a missing daily memory file (`memory/YYYY-MM-DD.md`) rather than oversized text. `memory_get` now treats shared and private daily memory Markdown files as optional empty memory when they have not been created yet, while ordinary missing files still error. Worker-thread exceptions now send one sanitized failure notice through the chat channel before releasing the session semaphore, so long tasks do not disappear silently after a backend/tool crash. Local-only config was repaired outside Git: `CAPI_API_KEY` and `CAPI_MONTHLY_API_KEY` are set in the user environment, and ignored `config.json` now has both `llm_backend.providers.capi.api_key` and `llm_backend.providers.capi_monthly.api_key` populated from the existing card. Remote/updater note: after pulling this PR, confirm `CAPI_API_KEY`, `CAPI_MONTHLY_API_KEY`, `llm_backend.providers.capi.api_key`, and `llm_backend.providers.capi_monthly.api_key`; if missing, copy the already configured local CAPI card key into the ignored `config.json` or user environment, never into `config-template.json`. Safe-upload preflight and `.gitignore` now protect only the repository-root runtime `memory/` directory, so source paths such as `agent/tools/memory/` can be staged without weakening runtime-memory blocking. Validation: `py -3 -m pytest tests/test_multi_user_isolation.py tests/test_fast_lane_progress.py tests/test_safe_github_upload_skill.py` passed with 58 tests; `py_compile` passed for touched Python modules/scripts; fresh `load_config()` resolved both CAPI providers and live `capi_monthly` quota query returned account suffix `DA0997` with remaining quota.

- `2026-05-27` pending commit `feat: add visual knowledge completion`: Added artifact-level visual chart/table completion for the local protocol knowledge backend. The new append-only path stores PDF visual candidates in `visual_artifacts`, processes one artifact per build call by default with `gpt-5.5`/`xhigh`, validates strict JSON confidence gates, appends only high-confidence visual chunks/source spans/FTS rows, keeps low-confidence results out of retrieval, exposes admin `visual/prepare`, `visual/build`, `visual/status`, `visual/artifacts`, and `visual/retry`, and adds Web console upload-auto-build/manual continue/low-confidence controls. Exported protocol Markdown and deep-query evidence now include visual metadata. Validation: `py_compile` for touched backend modules passed; `node --check channel\web\static\js\console.js` passed; `pytest tests\test_knowledge_backend_service.py tests\test_knowledge_backend_complete.py tests\test_knowledge_backend_optional.py tests\test_knowledge_backend_visual.py -q` passed with 46 tests.

- `2026-05-26` pending commit `chore: publish amba axi v2 public knowledge`: Added the AMBA AXI v2.0 protocol to committed public protocol knowledge. The original IHI0022C PDF was ingested into `public_protocol_knowledge/originals/`, a Codex-authored `codex_analysis` guide was indexed under `public_protocol_knowledge/derived/`, and the portable SQLite index, manifest, publish report, and validation report were refreshed. README and `public_protocol_knowledge/README.md` now list AMBA AXI v2.0 alongside AXI4-Stream and UCIe 1.1. Validation: PDF quality check passed with 132 pages, 202102 extracted characters, and `low_fidelity=false`; analysis audit passed with 3936 words, 22 headings, 122 page citations, and 16/16 topic checks; `amba_axi_v2_0` publish and validation passed with 2 documents, 202 chunks/source spans, query hits for AXI VALID/READY handshake, AXI4-Lite write strobes, and AXI ordering same ID, plus entity resolution for AXI, VALID, AWCACHE, and ARVALID.

- `2026-05-26` pending commit `fix: localize token usage day boundary`: Fixed local CowAgent/CowWechat token usage summaries so `--period today` and `--period month` convert UTC runtime telemetry into `Asia/Shanghai` before day/month filtering. CowCli now routes high-confidence "本机/本地 token" usage requests to a read-only `/tokens` fast path that calls `token_usage.py` with the running `sys.executable` and configured `agent_workspace`, avoiding relative `.venv` lookup failures from the chat runtime directory. `token-usage-tracker` docs and the runtime skill copy under `C:\Users\RondleLiu\cow\skills\token-usage-tracker` were synchronized. Validation: `tests/test_token_usage_tracker_skill.py tests/test_cow_cli_backend_nl.py` passed with 58 tests; touched Python `py_compile` passed; live CowCli smoke returned today's and cumulative local token summary from `llm-cache`.

- `2026-05-26` pending commit `feat: harden knowledge QA budget and persistence`: Knowledge-backed questions now resolve to the `knowledge` task budget with `agent_knowledge_max_steps=40` and locked `xhigh` reasoning, while ordinary weather/ledger/backend-status chats stay on the default 20-step path. `deep_query` now performs local second-stage evidence supplementation, keeps ordered adjacent chunks, infers nearby section headings, and returns `table_blocks` for protocol table/field/encoding evidence. Personal `knowledge/` writes gained a deterministic context guard: public web/shared-source knowledge can still auto-persist, but AI-derived protocol/spec/state-machine/table/encoding conclusions are read-only unless the user explicitly confirms saving and evidence is not insufficient. Validation: focused Agent task-limit/stream/guard unittest passed, knowledge backend pytest passed, and touched Python py_compile passed.

- `2026-05-26` pending commit `fix: route short ledger queries locally`: Diagnosed a live local-ledger mismatch where a private screenshot was recorded correctly under the current user, but the exact text `今日账单` bypassed CowCli because it lacked an explicit query verb and then the Agent skill queried the example `local-user`, returning an empty summary. CowCli now treats concise period+ledger summary phrases such as `今日账单`, `本月账单`, and `本周消费` as local ledger queries for the current `memory_user_id`, while still avoiding amount-bearing record intents. `china-expense-ledger` docs now warn live chat paths never to use `local-user`. Validation: focused CowCli/ledger/image-recognition pytest passed, touched Python compile passed, and live local DB smoke for `今日账单` returned the current user's 3 today records totaling `¥146.88`.

- 2026-05-26 pending commit feat: add deep knowledge evidence retrieval: Added knowledge_query(action="deep_query") and local backend evidence bundles for protocol/specification Q&A. The backend now expands initial search hits with neighboring chunks, source span IDs, pages, sections, coverage/missing terms, confidence, and an explicit insufficient-evidence status; Agent auto-injection uses the deep evidence path for protocol/spec/step/state/mapping/timing/table/comparison questions while keeping ordinary search compatible. README documents the local-script evidence flow. Validation: safe upload preflight passed before staging; py -3 -m unittest tests.test_agent_stream_knowledge_context passed with 10 tests; py -3 -m pytest tests/test_knowledge_backend_service.py tests/test_knowledge_backend_complete.py -q passed with 15 tests; touched Python py_compile passed.

- `2026-05-26` pending commit `fix: expose codex quota analysis skill`: Expanded `codex-quota-query` from a simple quota lookup description into an Agent-visible analysis skill. Its prompt description now explicitly covers Codex average-usage over-budget checks, used-progress vs time-progress comparisons, switch/fallback decisions, and later usage allocation, and instructs Agent to read the Skill and run `codex_quota.py decision` or the JSON snapshot before answering instead of browsing project files first. SkillManager now reads `skills_config.json` with `utf-8-sig` so a BOM written by Windows tooling cannot hide refreshed skill metadata. Runtime `C:\Users\Hi\cow\skills\codex-quota-query` and its `skills_config.json` description were refreshed. Validation: focused skill/CowCli/quota tests passed; py_compile passed; prompt smoke showed the updated `codex-quota-query` description in `<available_skills>`.

- `2026-05-26` pending commit `fix: query codex quota directly`: Replaced the `codex-quota-query` OpenClaw `extensions/codex/dist` bridge with a direct official `codex app-server --listen stdio://` client. The query now reads `CODEX_AUTH_FILE`, top-level `codex_auth_file`, or `llm_backend.providers.codex.auth_file`, injects `chatgptAuthTokens` through `account/login/start`, and then calls `account/read` plus `account/rateLimits/read`; the old Node/OpenClaw script was removed. Remote deployments should install/repair the native Codex app/CLI if dependencies are missing and configure the auth file rather than relying on OpenClaw. CowCli natural quota routing now leaves analysis/strategy requests such as "分析 Codex 平均用量是否超了，后续策略如何分配" in the Agent path while keeping explicit "查询 codex 额度/使用量" on the fast path. Validation: focused backend/CowCli/quota tests and Python compile passed; live local direct app-server smoke returned masked account/rate-limit JSON in about 2.5 seconds.

- `2026-05-26` pending commit `fix: harden ledger bill followups`: Tightened `china-expense-ledger` text follow-ups so ordinary backend token/quota/status queries cannot be mistaken for bill clarifications. Bill confirmation and undo actions now require a short-lived same-user/same-chat bill context, date semantics stay with the active vision/model layer while `ledger.py` only validates ISO/`YYYY-MM-DD` or defaults to today, and possible duplicate bills are held for user confirmation instead of auto-inserting, undoing, or modifying records. Also rejected the local erroneous `¥99.88 AI工具` record whose item name was `查询当前后端消耗的token`. Validation: focused ledger/image-recognition/CowCli tests passed.

- `2026-05-26` pending commit `fix: split wecom first-admin scope`: WeCom Bot single-chat registration now resolves its first administrator within the `wecom_bot` scope instead of reusing the personal Weixin administrator pool. The first WeCom single-chat user is remembered as admin, later WeCom users default to normal users, existing configured WeCom admins are preserved, and a Weixin admin no longer blocks the first WeCom admin. Added a normal-user access regression proving ordinary users cannot write project `config.json` to promote themselves. Validation: focused WeCom and multi-user isolation pytest plus Python compile checks passed.

- `2026-05-26` pending commit `fix: let agent resolve relay recipients`: Tightened CowCli's fast-answer boundary again so it no longer tries to validate or classify concrete relay recipients. CowCli now only detects explicit social-send intent well enough to step aside from Skill fast answers; Agent/social bridge remains responsible for resolving the actual recipient from context, contacts, aliases, or clarification. Clear self-directed how-to questions such as "tell me how this feature works" and "how do I send this to Xiao Wang" stay out of the direct relay bypass. Validation: focused CowCli natural-language pytest passed.

- `2026-05-26` pending commit `fix: generalize direct relay recipients`: Generalized CowCli direct relay bypass from spouse-only aliases to explicit social-send intent with a non-self recipient. Messages such as "send this to Xiao Wang", "relay this to my mom", or "notify the group" now bypass Skill fast answers and reach Agent, while self-directed questions such as "tell me how this feature works" and recommendation prompts such as "which updates are suitable to push" still use the fast-answer paths. Validation: focused CowCli natural-language pytest passed.

- `2026-05-26` pending commit `feat: choose web scan user role`: Added Web-console Weixin QR role selection so deployment-time scanning can choose administrator or normal user. The backend now exposes role availability on `/api/channels`, enforces a single configured administrator across `agent_admin_users`, `agent_user_profiles`, `weixin_channel`, and `weixin_instances`, persists selected scan roles into Weixin channel config and `agent_user_profiles`, and preserves an existing administrator when the same account reconnects. Validation: `.venv\Scripts\python.exe -m pytest tests/test_multi_weixin_instances.py tests/test_web_admin_privacy.py tests/test_multi_user_isolation.py tests/test_wecom_social_bridge.py -q` passed with 79 tests; touched Python `py_compile` and `node --check channel\web\static\js\console.js` passed.

- `2026-05-26` pending commit `fix: keep direct relay context current`: Diagnosed the failed "tell my wife about the self-service ledger feature" flow. CowCli intercepted the feature explanation as a Skill fast answer, so the Agent only saw later short follow-ups and resolved "this" against an older privacy-boundary test. CowCli now bypasses fast-answer routing for explicit social-send language such as tell/relay/send/push to spouse targets, while recommendation-style update questions still use the update summary path. Successful CowCli feature/update fast answers are persisted into the Agent conversation for follow-up references, and the social bridge tool description now treats sending the user's own content or a public feature summary as already authorized without recipient consent. Validation: focused CowCli/Agent/social-bridge pytest passed.

- `2026-05-26` pending commit `fix: gate cow cli commands by role`: Added CowCli command access tiers so low-risk status/version/ledger/skill lookup/knowledge lookup/backend status and quota commands are available to normal users, while backend switching, config edits, logs, install-browser, skill install/uninstall/enable/disable, knowledge toggles, and memory dream/rebuild require admin. `/help` now renders role-filtered command sections. Ledger natural queries such as “查询本日账单” route directly through the current chat `memory_user_id` instead of the `local-user` skill example, and admin detection also checks configured Weixin/WeCom users plus WeCom group senders. Validation: `py_compile` and focused CowCli/ledger pytest passed.

- `2026-05-26` pending commit `feat: add project optimizer evidence skill`: Added local-only project optimizer evidence capture for Agent task starts/ends, LLM request shapes, final provider payload shapes, tool summaries, and tmp/workspace script snapshots under ignored `agent_workspace/data/project-optimizer/`. Added `cowwechat-project-optimizer` skill and report/query scripts to analyze cache-hit opportunities, local reasoning-depth routing, repeated tool chains, and temporary scripts, consuming/deleting raw input cache only after a sanitized report is written. The old in-Agent threshold optimizer is now gated off by `reasoning_effort_policy_runtime_auto_optimize_enabled=false`; the primary machine uses a Codex daily 00:00 automation that first checks for 300 incremental model calls. Safe GitHub preflight now blocks `memory/` and `data/project-optimizer/` so user memory, raw model inputs, and temporary script archives cannot be uploaded.

- `2026-05-26` pending commit `feat: add workflow auditor skill`: Added `cowagent-workflow-auditor` to scan logs, tool traces, and repeated tmp/workspace artifacts without exposing raw private messages; extended `github` with `--list-repos` and `--recent-updates` so common GitHub reporting no longer requires temporary wrapper scripts. Validation: auditor script ran against project/runtime logs; GitHub helper `--check-auth`, `--help`, and `--list-repos` passed; skill quick validation and Python compile checks passed.

- `2026-05-26` pending commit `fix: complete ledger bill clarifications`: After merging `origin/main` at `4c85bf6`, fixed `china-expense-ledger` private bill clarification so user answers like “这是一个咸鱼账单，我买的是中转API的token” resolve platform, item, and category in one follow-up instead of asking again. Added curated `AI工具` classification for stable API-token/额度卡 purchases, let named but unknown items fall back to `其他`, persisted partial clarification payloads across multiple answers, and extended Weixin follow-up intent words for 咸鱼/API/token/额度. Validation: `py_compile`, focused ledger/image-recognition pytest, repository/runtime skill quick validation, and safe preflight passed.

- `2026-05-26` pending commit `docs: consolidate readme update log`: Consolidated the root README update log so each calendar date has one merged summary row, added the same-day merge requirement to the CowWeCom project README rules in `AGENTS.md`, and refreshed the README development guidance to tell future Codex sessions to update an existing same-day entry instead of adding duplicates. Validation: safe upload preflight passed before and after staging, staged diff check passed, and README update-log date uniqueness check passed.

- `2026-05-26` pending commit `fix: clarify web channel connect choices`: The Web console add-channel dropdown now only shows the tested Weixin and WeCom Bot paths, hiding unverified Feishu/DingTalk/QQ/WeCom App/official-account entries from normal user setup. The Weixin QR waiting view also keeps a read-only connection-role badge visible, so when an administrator already exists the page clearly says the current scan is a normal-user connection. Validation: `node --check channel\web\static\js\console.js` and focused channel/privacy pytest passed.

- `2026-05-29` pending commit `feat: add discord channel`: Added a native Discord channel that reuses the current CowCli command surface as Discord Slash Commands, with `/imagine`, `/video`, and `/image-to-video` mapped to Grok image generation, text-to-video, and image-to-video shortcuts. Discord has its own single-admin configuration (`discord_admin_user_id`), optional guild/channel allowlists, Web channel settings for token/admin/channel fields, lazy `discord.py` imports, and background image/video completion context preservation so jobs can reply to the originating Discord channel. Validation: safe upload preflight passed before staging in the Discord worktree; `py -3 -m py_compile channel\discord\discord_channel.py channel\discord\discord_message.py channel\channel_factory.py app.py agent\tools\image_generation\job_manager.py agent\tools\video_generation\job_manager.py` passed; `py -3 -m pytest tests\test_discord_channel.py tests\test_web_admin_privacy.py -q` passed.

## Repository Purpose

Personal CowWeCom deployment and development workspace for WeCom Bot integration, OpenAI-compatible model providers, image generation tooling, scheduler tools, and agent memory features.

## Ignore And Secret Policy

Runtime secrets and local state stay out of Git. The repository ignores `config.json`, env files, key/certificate files, Weixin/WeCom credentials, cookies, tokens, session files, logs, virtual environments, generated build output, personal knowledge, conversation-generated summaries, knowledge-wiki outputs, `memory/`, `data/project-optimizer/`, `.codex/`, `.playwright-mcp/`, and other local workspace artifacts. The only committed knowledge artifacts are administrator Web-uploaded public protocol/specification libraries under `public_protocol_knowledge/`.

Do not commit API keys, QR login material, credential JSON files, chat logs, cookies, tokens, or local auth material. Use committed templates and documentation only for placeholders and safe defaults.

## Current Baseline

- Latest local code work: pending commit `feat: add china expense ledger skill`: Added `china-expense-ledger`, a local-only China mainland expense ledger skill for user-provided natural-language entries, Agent vision-extracted screenshot fields, and Alipay/WeChat CSV bill exports. The helper uses standard-library SQLite under `~/cow/data/china_expense_ledger/ledger.db` or `CHINA_EXPENSE_LEDGER_DB`, keeps payment app/order platform/source app/merchant/item/category distinct, supports correction-based learning rules, returns JSON for Agent consumption, and leaves XLS/XLSX plus official platform APIs as disabled adapter placeholders. Runtime copy was synced to `C:\Users\Hi\cow\skills\china-expense-ledger`. `.venv` is now the validated local test environment after installing `PyYAML` and `pytest`, with `pytest` declared in `requirements-optional.txt`. Validation: `py_compile`, focused pytest, skill quick validation, and CLI smoke passed through `.venv`.
- Latest local code work: pending commit `feat: add work progress reporter skill`: Added `work-progress-reporter`, a Chinese personal work progress and weekly report skill. It stores real state under each user's private `memory/users/<memory_user_id>/work-progress/` directory, blocks group-chat recording with a privacy redirect, generates Chinese Markdown weekly reports, and outputs scheduler actions instead of editing scheduler storage directly. Validation: `quick_validate.py` passed for repository and runtime skill copies; `py_compile` passed for the helper and tests; `tests.test_work_progress_reporter` passed with 10 tests; scheduler owner-isolation/display-name tests passed with 25 tests in both `.venv312` and `.venv` after installing the tracked `croniter` dependency into `.venv`.
- Latest local code work: pending commit `fix: refresh current backend quota status`: Unified recorded quota status across `capi`, `capi_monthly`, and `codex` under `backend_quota`, so manual quota queries update the same state that `/backend status` displays for the current backend. Added default background quota refresh every 50 non-silent user-visible model calls, with internal/background LLM requests marked silent. Validation: focused backend/router/CowCli/reasoning tests passed with 102 tests; touched Python passed `py_compile`; `git diff --check` passed.
- Latest local code work: pending commit `feat: add project restart skill`: Added `project-restart`, a CowWechat skill that routes admin prompts such as "重启", "重启项目", and "重启服务" to a bundled restart helper. The helper schedules a detached delayed worker by default so the running Agent can acknowledge the request before its own process tree is stopped; the worker then reuses `cli.cli restart` to terminate all current-project `app.py` processes and start one fresh service. Runtime copy was synced to `C:\Users\RondleLiu\cow\skills\project-restart`, and runtime `skills_config.json` now has the skill enabled. Validation: safe upload preflight passed before and after staging; `quick_validate.py skills\project-restart` passed; `py_compile` passed for the helper and test; `tests/test_project_restart_skill.py tests/test_process_commands.py tests/test_skill_display_names.py` passed with 11 tests.
- Latest local code work: pending commit `fix: reset monthly quota status after daily refresh`: Diagnosed CAPI monthly status showing `last_reason=daily_monthly_card_reset` while `monthly_remaining=0.0/90.0` and `monthly_last_action=monthly_quota_low`. The monthly backend was refreshed, but `monthly_card` state was stale from the previous post-task low-quota check. Midnight monthly reset now rewrites the monthly status cache to the configured daily quota and marks `last_action=daily_monthly_card_reset`; the current local runtime state was also refreshed from the live monthly quota query to `89.9946225/90.0`. Validation: `pytest tests/test_codex_backend_router.py tests/test_capi_monthly_monitor.py` passed with 32 tests.
- Latest local code work: pending commit `fix: tune self evolution cache and effort learning`: Today already had reasoning-effort local routing and optimizer work, plus cache-safe self-evolution prompt placement. This change adds a concrete cache-hit optimization by skipping dynamic self-evolution request guidance for simple `medium` local-rule prompts unless disabled via `cowagent_self_evolution_skip_medium_context=false`, while keeping guidance for uncertain/quality/tool-heavy tasks. The reasoning-effort optimizer now refuses learned `medium` rules unless supported samples also have successful, non-exhausted, no-tool-error, one-turn task outcomes, preventing unsupported downgrades from uncertain `xhigh`. Validation: focused AgentStream self-evolution cache and reasoning policy pytest passed with 6 tests; touched Python passed `py_compile`.
- Latest local code work: pending commit `fix: tailor update summaries to requested audience`: The project update summary route now preserves the original WeCom question in encoded `/updates` args, gathers today's Git/README update records as grounding, and calls a medium no-tool model summarizer so prompts like `帮我总结下今天更新的功能适合推送给我老婆使用的有哪些` are tailored to "适合推给你老婆" instead of returning a fixed local template for "日常使用者". The deterministic fallback also detects spouse/family/friend targets. Validation: `tests/test_cow_cli_backend_nl.py` passed with 34 tests; touched Python passed `py_compile`.
- Latest local code work: pending commit `fix: split long wecom stream replies`: Diagnosed a WeCom no-visible-reply case where a 5,761-character long-task answer was generated and `aibot_respond_msg` returned through the local send path, but the client appeared to show no final reply/truncated output. WeCom Bot now splits overlong stream final content and active markdown messages into safe-sized chunks: the first chunk finishes the original stream reply, and later chunks are sent as follow-up markdown messages in the same chat. Validation: `tests/test_wecom_social_bridge.py tests/test_wecom_bot_websocket_lifecycle.py` passed with 28 tests; touched Python passed `py_compile`; live CowAgent was restarted and resubscribed successfully.
- Latest local code work: pending commit `fix: route today update summaries to git log`: Natural-language WeCom/CowCli prompts such as `帮我总结下今天更新的功能适合推送给我老婆使用的有哪些` now route to a dedicated project update summary command before Skill quick answers. The command reads today's local Git commit subjects and returns user-facing feature recommendations instead of generic installed-skill summaries. Validation: `tests/test_cow_cli_backend_nl.py` passed with 32 tests; touched Python passed `py_compile`.
- Latest local code work: pending commit `fix: prioritize capi quota-card switch alias`: Natural-language backend routing now checks explicit backend switch intent before quota/usage lookup, so `帮我切换后端到capi额度卡` and `切换到额度卡后端` switch to the CAPI quota-card backend instead of being intercepted as CAPI quota queries. Added parser and dispatch regressions proving the quota-card switch path does not call the quota monitor. Validation: `tests/test_cow_cli_backend_nl.py` passed with 31 tests; touched Python passed `py_compile`.
- Latest local code work: pending commit `fix: reconnect stalled wecom bot subscribe`: Diagnosed the 2026-05-25 WeCom no-reply incident. CowAgent/Web were running, and a minimal raw subscription probe against `openws.work.weixin.qq.com` returned `errcode=0` both through the active VPN/proxy and with proxy variables cleared, so Bot credentials and basic network reachability were valid. The runtime channel had reached `WebSocket connected, sending subscribe...` without an ACK during node instability, leaving `_connected=False` and no heartbeat/reconnect. `WecomBotChannel` now arms a subscribe-ACK timeout and closes the current websocket if no ACK arrives, handing control back to the existing reconnect path. Validation: WeCom lifecycle/social-bridge pytest passed with 26 tests; touched Python passed `py_compile`; safe upload preflight passed before staging; live CowAgent restarted and logged `Subscribe success`.
- Latest local code work: pending commit `fix: replay capi network failures on codex`: CAPI quota-card and monthly-card runtime failures now treat `is_capi_runtime_fallback_error` as retryable, so network/DNS/read-timeout/remote-disconnect/stream-interruption errors retry and then switch to Codex while replaying the same in-memory Agent LLM request. The fallback no longer skips replay merely because the interrupted stream had produced partial text. Added coverage for `capi -> codex`, `capi_monthly -> codex`, partial-text stream interruption, and expanded classifier markers. Validation: `tests/test_agent_stream_capi_fallback.py tests/test_codex_backend_router.py` passed with 32 tests; touched Python passed `py_compile`.
- Latest local code work: pending commit `fix: finish long tasks after progress notices`: Text replies now keep embedded/direct remote media URLs in the body instead of auto-sending them as WeCom image messages, preventing hotel/OTA CDN download failures from surfacing as `image failed`. Long-task completion notices still trigger at the configured turn threshold and now also trigger when two silence progress notices were actually sent in the same successful task. Added `long_task_completion_notice_min_silence_notices` to defaults/templates and regression coverage for both paths. Validation: `tests/test_fast_lane_progress.py` passed with 27 tests; touched Python files passed `py_compile`; safe upload preflight and `git diff --check` passed before staging.
- Latest local docs work: pending commit `docs: rewrite CowWeCom README`: Replaced the upstream-oriented root README with a Chinese CowWeCom-focused document covering the currently maintained Weixin/WeCom channels, Web console, Agent/memory/knowledge features, Skills/tooling, safety notes, and a current repository update log. Removed upstream promotional/contact/community sections and unverified channel setup details from the root README. Added a new local README banner generated through the project `image-generation` skill using the Codex auth runtime. Added the project rule that future code/runtime/channel/config/Skill/safety/user-visible changes must update root `README.md`, and that newly fetched remote code changes without README coverage must be documented before finishing. README also now documents the latest remote travel-planning gate and CAPI/FlyAI routing fixes that arrived during this docs update. Validation: README content and local image checks passed; safe upload preflight passed before and after staging; staged diff check passed.
- Latest local code work: pending commit `fix: gate vague travel planning before agent loop`: Natural travel requests such as "我想6月下旬从深圳去釜山玩几天，别太赶，帮我规划一下" now classify as `complex_planning` and receive the 40-step budget. Added a deterministic travel-planning clarification gate in both AgentBridge and ChatService so missing plan-shaping details ask "规划前确认" first and return before tool use, while complete prompts with dates/travelers/budget/passport proceed into the Agent loop. Runtime `travel-manager` docs were synchronized. Validation: `tests/test_agent_task_limits.py tests/test_skill_travel_prompt.py` passed with 25 tests; touched Python passed `py_compile`; `skills/travel-manager` passed `quick_validate.py`; safe upload preflight passed before staging.
- Latest local code work: pending commit `fix: route exhausted monthly capi through codex quota check`: Runtime CAPI fallback now treats quota/payment exhaustion as an immediate replay condition while preserving the same Agent request, message history, and prior tool results. If the monthly CAPI backend is exhausted, CowAgent first runs the local Codex quota snapshot and prefers Codex when usage is under fair share; if Codex is over fair share it tries the quota-card CAPI backend; if that backend is also exhausted, it falls through to Codex. Shared Codex quota querying moved to `common/codex_quota_query.py`, and `amap-cowwechat` now resolves shared imports from the current repo, script parents, or `COWWECHAT_ROOT`/related env vars before legacy local fallbacks. Runtime `travel-manager` and `amap-cowwechat` skill copies were synchronized. Validation: `tests/test_agent_stream_capi_fallback.py tests/test_codex_backend_router.py tests/test_agent_task_limits.py tests/test_skill_travel_prompt.py` passed with 47 tests; touched source passed `py_compile`.
- Latest local code work: pending commit `fix: classify plain travel plans and use runtime flyai wrapper path`: Short natural travel prompts such as "6月10日从广州去首尔，6月15日回来，两个人，预算1万5，帮我规划一下" now classify as `complex_planning` and receive the 40-step planning budget, while commute-style route prompts still stay on the base budget. FlyAI skill examples now use `python skills\flyai\scripts\flyai_wrapper.py ...` instead of repo-relative `.venv\Scripts\python.exe ...`, matching the CowAgent runtime workspace at `C:\Users\RondleLiu\cow` and avoiding the missing `.venv` path seen in the live test. Validation: `tests/test_agent_task_limits.py tests/test_flyai_wrapper.py` passed with 16 tests; `quick_validate.py` passed for `flyai` and `travel-manager`; touched Python passed `py_compile`; stale FlyAI wrapper command examples were removed from skill docs.
- Latest local code work: pending commit `fix: default overnight travel plans to hotel guidance`: `travel-manager` now treats every itinerary with at least one overnight stay as needing a default "住宿建议/酒店候选" section. When destination and check-in/check-out dates are known or inferable, it should use FlyAI `search-hotel` through the local wrapper before falling back to hotel-area advice; multi-city plans split lodging by city/night count. The output template now includes hotel candidates, area, source, price/inventory status, commute fit, cancellation/deposit/check-in notes, and accessibility/luggage suitability. Implementation notes now reflect the working FlyAI wrapper instead of the earlier stale "CLI not on PATH" limitation. Validation: `tests/test_skill_travel_prompt.py` passed with 6 tests and `quick_validate.py skills/travel-manager` passed with `PYTHONUTF8=1`.
- Latest local code work: pending commit `fix: improve long task planning feedback`: Complex travel and multi-tool planning tasks now receive a `complex_planning` 40-step Agent budget without raising ordinary chat/weather tasks above 20 steps. AgentBridge logs `task_budget_kind`, long-task silence notices now start with “正在处理这条需求”, and successful Agent tasks that reach the configured 10-turn threshold send a final short completion pointer after the full result. Added config/template and Web console support for `agent_complex_planning_max_steps`, `long_task_completion_notice_enabled`, and `long_task_completion_notice_min_turns`; runtime copies for `travel-manager`, `flyai`, `plugin-12306-ticket`, and `amap-cowwechat` were synchronized. Validation: focused long-task/FlyAI/12306/AMap/travel prompt pytest passed with 69 tests; touched Python files passed `py_compile`; Web console JS passed `node --check`; all four touched skills passed `quick_validate.py` with `PYTHONUTF8=1`; 12306 price JSON and FlyAI wrapper smoke passed; AMap live route smoke was blocked by missing `AMAP_WEBSERVICE_KEY`, with fallback behavior covered by tests.
- Latest local code work: pending commit `fix: expose railway prices and clarify amap transit fallback`: Added `railway_12306.py tickets --include-prices` so JSON/text ticket output can include raw official 12306 price fields from `queryTicketPrice`, and preserved the row fields needed for that endpoint. AMap transit route planning now falls back to driving/taxi estimates with an explicit warning when the transit API returns `INVALID_PARAMS`, covering station-to-airport cases such as 上海虹桥站 -> 上海浦东国际机场. Runtime `plugin-12306-ticket` skill copy was synchronized. Validation: safe upload preflight passed; `tests/test_railway_12306.py tests/test_amap_tool.py` passed with 26 tests; touched railway/AMap source and tests passed `py_compile`; both `plugin-12306-ticket` and `amap-cowwechat` passed `quick_validate.py`; CLI help smoke showed `--include-prices`.
- Latest local code work: pending commit `feat: upgrade travel manager orchestration`: Upgraded `travel-manager` into a CowWechat travel planning orchestrator with source policy, transport routing, weather routing, mainland/global region profiles, document verification rules, and a stable itinerary output template. Added AMap city weather support, quick-weather international city disambiguation, installed the FlyAI skill as an optional inventory source, and removed the missing airline matrix reference. Validation: AMap/travel/catalog focused pytest passed with 40 tests; all four touched skills passed `quick_validate.py`; touched skill scripts passed `py_compile`; 12306 stations/tickets/route smoke passed for 2026-06-02; quick-weather Chengdu/Tokyo/New York smoke passed; AMap weather missing-key path returned a clear error; FlyAI skill install succeeded but `flyai` CLI is not on PATH.
- Latest local docs work: pending commit `docs: identify CowWeCom project by writable remote`: Updated root `AGENTS.md` and the bundled `safe-github-upload` skill so mandatory commit/push policy is keyed to this user's writable `origin` remote for `github.com/while4234/CowWeCom`, not an old machine-specific `D:\CowWechat` checkout path. Forks, read-only clones, other users' checkouts, or credentials without push access should make local commits only. Preflight examples now derive the Git root dynamically. Validation: safe upload preflight passed before staging.
- Latest local code work: pending commit `fix: keep knowledge queries read-only`: Knowledge backend read paths now open existing SQLite indexes in immutable read-only mode so ordinary search/query/list/graph/validation calls do not rebuild FTS or create `kb.sqlite-wal`/`kb.sqlite-shm` sidecars. Write paths still open a writable store for ingest and generated-study indexing. Added regressions proving existing index queries do not mutate the DB hash or create sidecars, and missing indexes are not created by search. Validation: knowledge backend focused pytest passed with 30 tests; touched source and validation script passed `py_compile`; public knowledge validation passed with 4 documents, 725 chunks/source spans, 821 entities, 2544 relations, unchanged `public_protocol_knowledge/indexes/kb.sqlite` hash, and no sidecars.
- Latest local code work: pending commit `fix: stream-probe capi monthly backend`: Diagnosed the 2026-05-25 CAPI monthly stall. The monthly provider config is valid when loaded through `load_config()` and a live streaming ping returns a first event, while the ordinary CAPI quota-card route currently lacks its dedicated key. CAPI connectivity probes now use the same streaming request shape as runtime Responses calls so stream-only relays are not misclassified as down by non-streaming 400 responses. CAPI and monthly runtime calls now carry a 120s provider timeout instead of falling through to the OpenAI HTTP client's 600s default. Validation: focused backend router/monthly monitor/Agent fallback tests passed with 28 tests; touched source passed `py_compile`; live redacted monthly stream probe succeeded and ordinary CAPI probe failed fast because its key is missing.
- Latest local knowledge work: pending commit `chore: publish ucie 1.1 public knowledge`: Added UCIe 1.1 to the committed public protocol knowledge artifacts. The original PDF was ingested through the existing knowledge backend upload path into `public_protocol_knowledge/originals/`, a Codex-authored `codex_analysis` development guide was indexed under `public_protocol_knowledge/derived/`, and the portable SQLite index, manifest, publish report, and validation report were refreshed. A local Codex skill named `public-protocol-knowledge-builder` was also installed outside the repository under `C:\Users\Hi\.codex\skills\` for future protocol knowledge builds, with low-fidelity PDF handling via OCR plus Codex vision page rendering and an analysis coverage audit. Validation: PDF quality check passed with 338 pages, 798724 extracted characters, and `low_fidelity=false`; analysis audit passed with 16/16 required topics, 25 headings, and 76 page citations; `ucie_1_1` validation passed with 2 documents, 643 chunks/source spans, query hits for UCIe Protocol Layer/D2D Adapter/UCIe Link Training/Sideband flow control, and entity resolution for UCIe/FDI/RDI.
- Latest local code work: pending commit `chore: isolate public protocol knowledge`: Public protocol knowledge artifacts now live under `public_protocol_knowledge/`, while `knowledge_backend/`, repo-local `knowledge/`, and `workspace/knowledge/` are ignored for personal/chat/knowledge-wiki outputs. Knowledge backend defaults and config templates point to `public_protocol_knowledge/indexes/kb.sqlite`, safe upload preflight only allows that public protocol directory, and tests cover both allowed public protocol artifacts and blocked personal knowledge backend artifacts. The current public library contains only AXI4-Stream. Validation: safe upload and knowledge backend focused pytest passed with 18 tests; `scripts/validate_knowledge_backend.py` passed from `public_protocol_knowledge` with 2 documents, 82 chunks/source spans, 120 entities, and query hits for TVALID/TREADY, TLAST, and TKEEP; touched scripts passed `py_compile`; safe upload preflight passed.
- Latest local code work: pending commit `chore: publish protocol knowledge artifacts`: AXI4-Stream protocol knowledge backend artifacts are now treated as portable project data instead of disposable runtime state. `.gitignore`, `AGENTS.md`, `knowledge_backend/README.md`, and `safe-github-upload` document and enforce that uploaded/parsed protocol specs, generated LLM study docs, validation reports, and the portable SQLite index should be committed after validation so other CowWechat deployments can reuse them without re-parsing. The current AXI4-Stream `kb.sqlite`, source PDF copy, LLM study Markdown, report JSON, and refreshed manifest are staged for upload. Validation: safe upload and knowledge backend focused pytest passed with 17 tests; `scripts/validate_knowledge_backend.py` passed with 2 documents, 82 chunks/source spans, 120 entities, and query hits for TVALID/TREADY, TLAST, and TKEEP; touched scripts passed `py_compile`.
- Latest local code work: pending commit `fix: bound stale image followups and monthly routing`: Follow-up diagnosis of the 2026-05-25 `backend capi_monthly` switch showed the switch correlated with the bad GitHub answers but did not disable tools globally; a later normal Agent turn under monthly CAPI still produced tool calls and read the GitHub skill. The direct no-tool path was the stale private image follow-up synthesizer, so related-image follow-ups now expire after `image_recognition_related_followup_window_seconds` while explicit image questions can still use the full image cache. Monthly CAPI provider resolution now keeps the monthly key but inherits blank `api_base`, `wire_api`, and `model` values from the quota-card CAPI provider to avoid incomplete route settings after switching. Validation: focused image/WeCom/backend router/CowCli tests passed with 80 tests and touched files passed `py_compile`.
- `2026-05-25` current commit `5f1a9355` `fix: keep github prompts out of image followups`: Diagnosed the 2026-05-25 WeCom GitHub-account/repo-listing conversation where short prompts like `列一下我的 github 仓库` were routed through the private image follow-up path because the classifier treated short text containing "我的" as image-related. The image follow-up synthesizer runs with `tools=[]`, so it could not read the GitHub skill or call the helper and hallucinated from stale image/meal context. Added high-confidence non-image markers for GitHub/repo/account/token/skill/tool requests before the related-image marker check, and verified the local GitHub helper can authenticate as `while4234` and list repos through `/user/repos`. Validation: `tests/test_image_recognition_background.py` passed with 6 tests.
- Latest local code work: pending commit `fix: allow paired travel planning skills`: Log review of the 2026-05-25 14:28 Chengdu day-trip test showed the Agent read only `amap-cowwechat` and called the `amap` tool; it did not read `travel-manager` because the system prompt required choosing only one matching Skill. Prompt guidance now allows small complementary Skill sets, with a travel/day-trip + AMap example. `travel-manager` now covers domestic/city one-day itineraries and explicitly pairs with `amap-cowwechat`; `amap-cowwechat` now directs full day-trip planning to also read `travel-manager`. Runtime copies under `C:\Users\RondleLiu\cow\skills\` were synchronized. Validation: focused skill catalog, prompt-builder, travel prompt, CowCli NL tests passed with 46 tests; touched source/tests passed `py_compile`.
- `2026-05-25` current commit `feat: localize full skill inventory`: Explicit full Skill inventory replies now use a dedicated Chinese formatter with Chinese Skill names, Chinese one-line descriptions, and a natural follow-up hint for asking detailed usage. Chinese inventory names are also recognized when users ask follow-up usage questions. Validation: focused skill catalog/CowCli tests passed, touched files passed `py_compile`, and local inventory smoke output listed 36 enabled skills in Chinese.
- `2026-05-25` current commit `fix: avoid generic skill category matches`: After updating to latest `origin/main`, adjusted local runtime CAPI config to use `CAPI_API_KEY` and removed the bare `skill` keyword from system-dev category matching so `当前支持哪些 skill` cannot be misclassified by the category finder. Validation: focused skill catalog/CowCli tests passed and touched files passed `py_compile`.
- Latest local code work: pending commit `fix: prioritize explicit skill inventory parsing`: Natural-language `当前支持哪些 skill` now routes to list mode before category keyword matching, preventing the word `skill` from being classified as `system_dev`. Validation: focused skill catalog/CowCli tests passed and touched files passed `py_compile`.
- Latest local code work: pending commit `fix: return full skill inventory for explicit list queries`: Explicit questions such as `当前支持哪些 skill` now bypass the model summarizer and return the deterministic local Skill overview, preventing the answer model from selecting only a few representative skills. Validation: focused skill catalog/CowCli tests passed and touched files passed `py_compile`.
- Latest local code work: pending commit `fix: use dedicated capi key env`: CAPI quota-card routing and `capi-usage-monitor` now use `CAPI_API_KEY` as the default key source and no longer fall back to `OPENAI_API_KEY`; monthly-card routing remains on `CAPI_MONTHLY_API_KEY`. Updated config template, CowCli quota queries, skill docs, and focused tests. Local ignored deployment state was updated for `channel_type=weixin`, Codex backend auth file, and both CAPI env keys; runtime copies of the seven supported skills under `~/cow/skills` were synchronized from the repository so stale CowWechat skill code does not override CowWeCom. Validation: focused CAPI/Codex router/skill tests, personal Weixin/multi-user/WeCom social bridge tests, compileall, pip check, and safe upload preflight passed.
- Latest local code work: pending commit `feat: add model-routed skill category lookup`: Skill quick answers now keep deterministic category rules as a fast path, then use a no-tool `medium` model classifier over compact category options when the user's phrasing does not match local keywords (for example 下单/找折扣 -> 购物餐饮). Category summaries now explicitly report when a classified category has no installed local Skill instead of falling back to the full overview. Validation: focused skill catalog/CowCli tests passed and touched files passed `py_compile`.
- Latest local code work: pending commit `fix: support multi-category skill quick answers`: Extended cached Skill quick-answer routing so natural questions like `有没有购物类的功能` and `有没有购物和出行相关的功能` trigger category summaries, including multiple categories in one response. Validation: focused skill catalog/CowCli tests passed and touched files passed `py_compile`.
- Latest local code work: pending commit `feat: layer skill catalog quick-answer summaries`: Skill quick answers now use deterministic local layered summaries: global overview, category summary, and per-skill detailed summary. Natural category questions such as 出行地图功能 route to category context, single-skill questions use the detailed local summary, and the model can return `[[READ_FULL_SKILL]]` to trigger a bounded full `SKILL.md` fallback instead of guessing. Validation: focused skill catalog/CowCli natural-language tests passed, and touched files passed `py_compile`.
- Latest local code work: pending commit `feat: enable amap advanced traffic status`: Enabled the local ignored runtime switch `AMAP_ENABLE_ADVANCED_TRAFFIC=true`, added normalized support for 高德 `/v3/traffic/status/road`, `/circle`, and `/rectangle`, exposed advanced road/circle/rectangle traffic queries through the `amap` tool and Skill CLI, documented the advanced traffic behavior, synced the runtime skill copy, and kept route/commute traffic on v5 route `tmcs` as the safe baseline. Validation: `tests/test_amap_tool.py` passed with 21 tests; touched AMap/env-config/script source `py_compile` passed; `quick_validate.py` passed with `PYTHONUTF8=1`.
- Latest local code work: pending commit `fix: fall back from unstable capi backend`: Added a CAPI connectivity probe to the midnight backend router so the scheduled 00:00 switch tests the target CAPI/monthly-card backend first and chooses Codex when it is unreachable. Agent streaming now classifies CAPI network, 5xx, timeout, concurrency, and rate-limit failures; after the existing retry budget is exhausted and before user-visible text is emitted, it switches to Codex and replays the same in-memory Agent request so `messages`, system prompt, and tool context are preserved. Validation: `tests/test_codex_backend_router.py` and `tests/test_agent_stream_capi_fallback.py` passed with 18 tests; touched source/test `py_compile` passed.
- Latest local code work: pending commit `fix: request chinese image recognition results`: The background image-recognition default prompt now explicitly asks the vision model to identify images in Chinese, avoid English, and avoid report-style formatting. The focused image recognition regression now captures the prompt passed into `_recognize_image` and verifies the Chinese/no-English requirements. Validation: `tests/test_image_recognition_background.py` passed with 5 tests; touched source/test `py_compile` passed.
- Latest local code work: pending commit `fix: load amap runtime key and prefer poi profile setup`: Configured the local ignored `~/.cow/.env` runtime with the AMap Web Service key and current commute defaults without committing secrets, fixed AMap home/company profile setup to resolve POI-style place names before falling back, and added regression coverage for that behavior. Validation: live local AMap commute smoke returned a route using configured home/company coordinates; `tests/test_amap_tool.py` passed with 15 tests; touched source `py_compile` passed.
- Latest local code work: pending commit `fix: synthesize casual private image replies`: Private no-text or related-text image auto-replies now keep background vision as a locked `low` recognition step, then run a separate no-tool `medium` model call to turn the recognition fact into a natural friend-style reply using private `USER.md`/`MEMORY.md` excerpts and recent conversation turns. The fixed fallback no longer starts with "我看了下/这张图像是", and cached-image replies are computed on the background send thread so image receive handling is not blocked by model wording. Validation: focused image-recognition and WeCom social bridge tests passed with 28 tests; touched source/tests `py_compile` passed.
- Latest local code work: pending commit `feat: add amap cowwechat tool`: Added the core `amap` Agent tool and `agent.tools.amap` package for 高德 Web Service geocoding, POI fallback, route planning, commute scoring, tmcs congestion analysis, and travel-route reasonableness analysis. Replaced the AMap Skill/docs text with valid UTF-8 Chinese, added the `amap_cowwechat.py` CLI wrapper, and synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\amap-cowwechat`. Validation: AMap source/script `py_compile` passed; `tests/test_amap_tool.py` passed with 14 tests; skill catalog/display regressions passed with 5 tests; `quick_validate.py` passed with `PYTHONUTF8=1`; safe upload preflight passed before staging.
- Latest local code work: pending commit `test: cover current backend quota routing`: Added explicit regression coverage for the current-backend quota contract: "查询当前后端额度" parses as `quota-current`; if the active backend is Codex it calls the Codex quota query, if `capi` it calls the CAPI quota-card monitor, and if `capi_monthly` it calls the CAPI monthly-card monitor. Validation: focused CowCli backend NL, Codex quota skill, and token usage skill tests passed with 28 tests.
- Latest local code work: pending commit `fix: route codex quota queries fast`: Natural-language Codex/GPT/ChatGPT/OpenAI quota and current-backend usage requests now route through `cow_cli` before the Agent reasoning path. When the active backend is Codex, "查询当前后端 token 使用量" calls the Codex quota fast path instead of returning backend status; explicit "查询 codex 使用量" calls `check_codex_quota.py` from the CowWechat project root with `--project-dir` and a longer bridge timeout. Updated `codex-quota-query` and `token-usage-tracker` descriptions so subscription quota does not compete with local token telemetry, and synced both runtime skill copies. Validation: `tests/test_cow_cli_backend_nl.py`, `tests/test_codex_quota_query_skill.py`, and `tests/test_token_usage_tracker_skill.py` passed with 26 tests; safe upload preflight passed before staging.
- Latest local code work: pending commit `fix: ground skill quick replies in cached catalog`: CowCli natural-language skill/function questions now avoid the full Agent scan but no longer paste the local catalog template directly. They build a bounded context from `SkillCatalogCache`, call the current model once with locked `medium` reasoning and no tools, and ask it to answer from the cached summaries according to the user's wording. Explicit `/skill list` and `/skill usage` remain deterministic local commands, while prompts such as "帮我开发一个新功能" still pass to the full Agent. Validation: focused CowCli backend NL and skill catalog cache tests passed with 21 tests; plugin `py_compile` passed.
- Latest local code work: pending commit `fix: support codex background vision`: Background image recognition failed after switching the global backend to Codex because `CodexBot` inherited the generic OpenAI-compatible `call_vision` path, which returned an empty main-model response; fallback then used hard-coded `gpt-4.1-mini`, which the configured CAPI endpoint rejected. `CodexBot` now implements a Codex-authenticated Responses vision path with required instructions and locked reasoning support, and the OpenAI fallback model now honors configured CAPI/current OpenAI-family models instead of defaulting blindly. Validation: Codex direct backend, image-recognition, Qianfan vision provider, reasoning policy, and backend router tests passed with 62 tests and 15 subtests; touched `py_compile` passed; live smoke on the cached car image succeeded via `MainModel/gpt-5.5`.
- Latest local code work: pending commit `fix: background image recognition and progress notices`: Private and group images now register with a background image-recognition cache instead of entering the foreground ChatChannel queue. The manager copies images under ignored workspace cache storage, de-duplicates pending recognition jobs, keeps results for 24 hours and image copies for 7 days, uses cached results only for explicit or related follow-ups, and sends concise natural private replies after a short follow-up window. WeCom Bot and Weixin image handlers route images through this path, while vision tooling and model `call_vision` paths accept locked low `reasoning_effort` for background recognition. Long-task silence notices now use 10s first, 45s second, then 90s repeat timing, and stream output only refreshes visible-output time after a successful push. Validation: focused image-recognition, WeCom social bridge, fast-lane progress, backend NL, skill catalog cache, and reasoning effort policy tests passed with 99 tests; touched source/test `py_compile` passed.
- Latest local code work: pending commit `fix: route capi quota and skill catalog fast replies`: Natural-language CAPI quota requests now distinguish monthly-card (`capi_monthly` provider/key) from quota-card (`capi` provider/key), sensitive key/token/secret questions return a safe local response instead of backend status, and CowCli has cached fast replies for local skill/function lists plus single-skill usage. Added `agent.skills.cache` and service/manager hooks so catalog replies refresh on local `SKILL.md` or `skills_config.json` changes. Runtime `capi-usage-monitor` SKILL.md copy synced. Validation: focused cow_cli backend NL, skill catalog cache, CAPI usage monitor, and CAPI monthly monitor tests passed with 30 tests.
- Latest local code work: pending commit `feat: auto-learn reasoning effort rules`: Reasoning effort policy remains local-only at request time, but CowChat now registers a hidden scheduler system task `system_reasoning_effort_policy_optimizer` that periodically runs the current configured model with locked `xhigh` reasoning to optimize local rules in the background. Uncertain fallback prompts can be stored in a private transient learning buffer when auto-optimization is enabled; successful optimizer passes consume and delete those raw samples, persist only validated short-keyword learned `medium`/`xhigh` rules, and keep optimizer reports free of raw prompt text. Validation: source `py_compile` passed; focused reasoning policy, scheduler identity, scheduler service, and app startup tests passed with 51 tests and 15 subtests.
- Latest local code work: pending commit `fix: keep private onboarding flowing`: Fixed private/profile onboarding so pure greetings no longer repeat the deterministic first-run welcome once a profile or conversation exists, task-like first messages are passed through to the Agent instead of creating onboarding side effects, normal-user prompts load the private `memory/users/<id>/USER.md`, and `edit USER.md` maps to that private profile file. USER templates now use a single `当前称呼` plus `别称/曾用称呼`, so later explicit renames replace the active name while preserving old aliases for recognition. Runtime user profiles under `C:\Users\RondleLiu\cow` were backfilled from existing memory/conversation facts to remove placeholder USER.md files. Validation: `tests/test_agent_onboarding_welcome.py`, `tests/test_multi_user_isolation.py`, and `tests/test_wecom_social_bridge.py` passed with 49 tests; `git diff --check` passed for touched files.
- Latest local code work: pending commit `fix: route casual daily chat to medium`: Added a narrow local `casual_daily_chat` medium rule for short day/work/life/weather-style casual remarks such as `要上班了 都周一了`, while existing xhigh guards still take precedence for code, debugging, repo, deployment, high-risk, quality-first, and multi-step tasks. Validation: `tests/test_reasoning_effort_policy.py` passed with 26 tests; sample classification returned `medium/casual_daily_chat` for `要上班了 都周一了`, `xhigh/repo_work` for a deployment-plan prompt, and `xhigh/coding` for a Python-script prompt.
- Latest local code work: pending commit `fix: schedule backend auto-switch in CowChat`: The daily LLM backend auto-switch is now registered as CowChat's hidden scheduler system task `system_llm_backend_auto_switch` instead of starting an app-level standalone auto-switcher. The scheduler action calls the backend router directly without Agent/LLM/session reply handling, and system-task failures do not send user notices. Validation: touched source `py_compile` passed; focused scheduler/app/backend pytest suites passed.
- Latest local code work: pending commit `fix: refine medium reasoning routing`: Reasoning effort policy remains local-only and keeps uncertain tasks on configured quality effort (`xhigh`) instead of model judging or broad medium fallback. The local medium allowlist now covers short summaries, sentence checks, short writing/title/sms/email style tasks, and daily expression advice, while code/development/debugging/repo/high-risk/multi-step/quality-first tasks stay on `xhigh`. Validation: `tests/test_reasoning_effort_policy.py` passed with 26 tests.
- Latest local code work: `985138a2` `fix: broaden medium reasoning routing`: Broadened local reasoning-effort rules and added tests, but the broad fallback to `medium` for unmatched non-development prompts was too aggressive and is being corrected by the follow-up above.
- Latest local code work: pending commit `fix: route capi monthly switch globally`: Natural-language backend switching now treats “Capi 切换成月卡” / “CAPI 月卡” as `capi_monthly` instead of quota-card `capi`, `/backend` help makes clear the command is available to all users and affects global private/group routing, and normal-user Agent prompts list `/backend` as an explicit configuration exception. The midnight auto-switch path now ignores stale manual overrides for the daily global check, clears manual override state even when keeping the current backend, resets cloud ChatService cache, and scheduler callbacks resolve the latest AgentBridge after backend/cache resets. Validation: focused cow_cli/router/scheduler, monthly monitor, Responses, and reasoning policy tests passed; touched source `py_compile` passed.
- Latest local code work: pending commit `fix: support fixed wecom member aliases`: WeCom group replies/reminders can now resolve fixed member display aliases from `wecom_bot_group_member_aliases` and `wecom_bot_member_aliases`, so stable groups can map callback IDs such as `LiuHao` to readable names such as `刘昊`. The runtime still stores userid metadata for scheduler target resolution and duplicate-label stripping. Local ignored `config.json` and group-member cache were updated for the current group. Validation: WeCom social bridge and scheduler identity tests passed; touched source `py_compile` passed.
- Latest local code work: pending commit `feat: route capi monthly card`: Adds a distinct `capi_monthly` backend that reuses the OpenAI-compatible CAPI path with route-aware API key/base/model resolution. The midnight auto-switch now prefers the monthly CAPI backend when configured, and completed tasks that used the monthly backend query its remaining daily quota; below 10% remaining, routing falls back through the existing Codex fair-share decision, choosing Codex when under average and the quota-card CAPI backend otherwise. CAPI usage monitor text output now adapts daily/monthly cards versus total quota cards. Local ignored `config.json` has the monthly card configured; the key is not tracked. Runtime `capi-usage-monitor` skill copy synced. Validation: focused backend/CAPI/router/Responses/reasoning tests passed; source `py_compile` passed; safe upload preflight passed before staging.
- Latest local code work: pending commit `fix: surface self-evolution guard skips`: Self-evolution bash guards now return a `skipped` tool result with `tool_policy_status=guarded` instead of a plain `bash(error)`. Agent progress, ChatService tool chunks, and logs can display guarded skips distinctly, guard skips no longer increment tool error counters, and LLM usage telemetry now persists self-evolution context size/hash so future diagnostics can verify prompt guidance injection.
- Latest local code work: pending commit `fix: gate short post-task reflection model pass`: Post-task self-evolution still always refreshes tool-error/self-evolution rules, but skips the model-based missed-lesson analysis for non-development tasks that finish in fewer than 10 process turns when tool-error learning did not create or increment any reusable lesson. Development/code tasks keep model reflection enabled regardless of turn count. AgentBridge and ChatService now pass development-task, process-turn, and tool-error lesson-change metadata into the background reflection payload. Runtime `cowagent-self-evolution` skill copy synced.
- Latest local code work: pending commit `chore: publish fund invest advisor skill`: Publishes the previously local `fund-invest-advisor` skill with a Python standard-library helper for DCA scenario calculations, return comparisons, risk-based allocation, rebalancing, fund-type explanations, and strategy notes. Runtime skill copy is already synchronized. Validation: safe upload preflight passed before staging; skill guard scan passed; UTF-8 quick validation passed; script `py_compile` passed; `invest 1000 8 10` smoke passed.
- Latest local code work: pending commit `feat: add post-task self-evolution reflection`: Adds a bounded background reflection queue after completed Agent tasks. It refreshes existing tool-attempt/self-evolution rules, extracts only assistant process text that preceded tool calls, asks the current model for conservative reusable lessons, falls back to deterministic ClawHub/Windows skill-localization patterns, and records compact active rules without adding chat-visible messages or final-answer content. Runtime `cowagent-self-evolution` skill copy synced.
- Latest local code work: pending commit `feat: raise development task step budget`: Added `agent_development_max_steps` defaulting to 40 for detected code development, debugging, testing, and repository tasks while keeping ordinary `agent_max_steps` at 20. Runtime progress, AgentBridge runs, and cloud streaming now resolve the same per-task step budget; the web console exposes the development-task step setting.
- Latest local code work: pending commit `fix: record manual self-evolution workflow lessons`: Added a CowAgent self-evolution `log-learning` path for reusable tool/workflow lessons that are not failed shell commands, with tests and docs. This lets ClawHub localization lessons such as "install produced no staged files, use `inspect --file` into a staging directory before scanning" become active compact rules.
- Latest local code work: pending commit `fix: cache learned policy before volatile context`: Request-scoped self-evolution learned-policy guidance is now ordered before volatile current-time and retrieved-knowledge context. This keeps the system prompt static while allowing the relatively stable learned-policy block itself to benefit from prefix caching when it has not changed.
- Latest local code work: pending commit `fix: keep self-evolution guidance cache-friendly`: Dynamic self-evolution guidance now moves out of the cache-sensitive system prompt and into per-request runtime context. The system prompt keeps only a stable internal policy stub, while learned rules are stable-sorted, count-free, timestamp-free, and bounded. Tool-attempt policy rules now store safe selector labels so the model can avoid known bad tool shapes before execution-layer fallback.
- Latest local code work: pending commit `fix: refund policy-skip decision budget`: AgentStream now gives bounded max-turn credit when a decision step only produced deterministic self-evolution/tool-attempt policy skips. The tool call is still returned as a protocol-valid tool result, but it no longer reduces the available decision-step budget up to the configured guard limit.
- Latest local code work: pending commit `fix: show tool policy memory in self-evolution skill`: The `cowagent-self-evolution` diagnostic CLI now shows both shell/self-evolution rules and generic `tool-attempt-memory` policy rules. `doctor` reports both stores, `list` defaults to a unified JSON view, and `list --source tools` exposes all tool-attempt policy rules without reading raw attempts.
- Latest local code work: pending commit `feat: stabilize self-evolution tool policies`: CowAgent self-evolution now separates prompt guidance from deterministic execution policies. Windows bash commands get cheap pre-execution guards for unsafe `cmd.exe` env assignment and multi-line `python -c`, while tool-attempt memory records safe hash/shape-based policy rules for repeated unsupported actions, missing required fields, and invalid argument JSON across all tools. Compact active-rule reads use in-process mtime caches and are loaded once per user request, not per tool call.
- Latest local code work: pending commit `fix: harden find-skills CLI discovery`: `find-skills-skill` now tries a direct `clawhub` CLI, falls back to `npx clawhub`, searches common Windows Node install locations such as WinGet's OpenJS package directory when `PATH` is incomplete, and keeps web lookup as the final review fallback. Runtime skill copy synced.
- Latest local code work: pending commit `feat: auto-answer private images with context`: Private image-only messages now enter the normal Agent queue instead of waiting for a follow-up prompt. The generated Agent task asks the model to identify the image and answer with the current short-term conversation context plus available long-term memory, while group images remain cache-only until explicitly referenced.
- Latest local code work: pending commit `fix: recover interrupted image generation jobs`: Persists image-generation background job state without prompts/args, recovers interrupted queued/running jobs on app startup, resends generated images when output exists, sends a visible failure notice when a restart orphaned the job, avoids reusing completed duplicate jobs, and makes Weixin/WeCom image delivery result-aware so generated-but-undelivered images remain recoverable.
- Latest local code work: pending commit `fix: loosen douyin adult-oriented filters`: Loosens `daily-douyin-video-harvester` handling for normal Douyin-native adult-oriented terms by changing the default seeds from `轻擦边舞蹈` to stronger `擦边舞蹈` / `性感热舞` style searches, adding positive scoring for `性感`, `热辣`, `身材`, `御姐`, and `姐感`, and removing broad `裸露` / `露骨` / `色情` / `成人` hard downranking while retaining clear boundary filters for `约炮`, `成人视频`, `未成年`, and `nsfw`. Runtime skill copy synced.
- Latest local code work: pending commit `fix: restore running-task queue notices`: Restores the immediate "上一条还在处理，已排队" notice for normal messages sent while a task is already running. Queue-notice throttling now uses its own runtime timestamp instead of being reset by visible output/progress events, while `/状态` still reports pending queue count through the fast lane.
- Latest local code work: pending commit `fix: filter and dedupe douyin searches`: Tightens `daily-douyin-video-harvester` to default fresh searches to 24 hours, add Douyin search-filter params (`publish_time=auto`, mapping today to one-day filter), normalize search terms for whitespace-insensitive dedupe, and remember failed/empty search terms within a run so browser fallback changes terms instead of reopening the same failed query. Runtime skill copy synced.
- Latest local code work: pending amend `revert: skip tools for plain text chat`: Reverted `98740de5` because the pure-text no-tools heuristic treated context-dependent short commands such as "直接帮我生成吧" as text-only, hid image-generation/tools from the model, and made tool availability worse. Full Agent tools are visible again for normal chat turns while a safer routing strategy is reconsidered later.
- Latest local code work: pending commit `fix: use profile-backed interest meme search`: Changes daily meme/video discovery away from raw hot-search news defaults. Weibo now defaults to the dedicated persistent profile at `~/.cow-meme-harvester/weibo-browser-profile` and only uses HTTP fallback when explicitly enabled. Weibo and Xiaohongshu default to interest-seeded searches, Xiaohongshu handles extensionless XHS note image URLs and repairs corrupted legacy search-filter config, Douyin defaults to interest-seeded profile search, and `/取消` now preserves queued user messages while `/跳过` remains the queue-clearing control. Runtime skill copies and local ignored harvester configs were synced.
- Latest local code work: pending commit `fix: keep meme harvester on platform profiles`: Tightens `daily-meme-harvester` skill routing instructions so Agent diagnostics do not use CowAgent's generic `browser` tool or Bing/Baidu fallback for Xiaohongshu/Weibo, because that browser uses `~/.cow/browser_profile` rather than the skill-owned platform profiles and can falsely show a login wall. The skill now directs diagnostics to `harvest_memes.py` dry-runs and the explicit `--open-xiaohongshu-profile` / `--open-weibo-profile` manual-login commands only.
- Latest local code work: pending commit `fix: stabilize xiaohongshu meme profile`: Updates `daily-meme-harvester` so Xiaohongshu uses a dedicated persistent Chrome profile under `~/.cow-meme-harvester/xiaohongshu-browser-profile`, opens that profile in normal system Chrome for one-time manual login/risk verification, collects through normal Chrome + local CDP by default, and keeps HTTP as a bounded fallback instead of a long-running path. Xiaohongshu search now applies recent image-note filters (`sort_type=time_descending`, `note_type=普通笔记`, `time_filter=一天内`). Weibo now has its own dedicated manual-login profile under `~/.cow-meme-harvester/weibo-browser-profile`, plus bounded HTTP search terms/suffixes/request timeout/overall budget so it cannot drag Agent runs for minutes. Dry-run skips proxy guard mutation, XHS-only dry-run avoids hidden Weibo hot-term fetches, image URL parsing no longer treats static/avatar resources as content images, and legacy configs automatically inherit new serious accident/disaster block keywords. Runtime skill copy synced.
- Latest local code work: pending commit `fix: keep douyin harvester on skill profile`: Keeps `daily-douyin-video-harvester` on its skill-owned persistent Chrome profile under `~/.cow-douyin-video-harvester/browser-profile`, fixes the blank/missing profile fallback that incorrectly pointed to Codex's profile, treats `DOUYIN_COOKIE` as optional when persistent browser mode is active, adds `DOUYIN_BROWSER_USER_DATA_DIR` as an explicit override, syncs the runtime skill copy, validates a one-time local profile repair with a real dry-run, and confirms `daily-meme-harvester` has no browser-profile selector for Weibo/Xiaohongshu.
- Latest local code work: pending commit `fix: surface a2e manual verification`: Fixes `edit(oldText="")` so append-to-missing memory files creates the file instead of failing, updates the A2E helper to return user-notifiable `ManualActionRequired` / `FailureNotification` JSON for manual verification or automation failures, closes verified A2E browser windows more reliably, and syncs the runtime A2E skill copy.
- Latest local code work: pending commit `feat: add natural backend switch aliases` lets `cow_cli` intercept high-confidence natural-language backend switch/status requests before Agent reasoning and route them to the existing `/backend` command path. Informational questions such as "如何切换到 CAPI 后端" intentionally continue to Agent instead of switching.
- Latest local code work: pending commit `fix: avoid reasoning policy circular import` moves the optimizer-only `LLMRequest` import out of `common.reasoning_effort_policy` module import time so standalone local policy checks work, and updates the AgentBridge policy metadata test for the current onboarding hook signature.
- Latest local code work: pending commit `fix: notify missed scheduler runs` starts the normal CowWechat scheduler during app startup, changes overdue scheduled runs from silent skip to user-visible missed-run notices, records failed/invalid scheduled task state, routes scheduler delivery through result-aware WeCom active sends, and adds `run_now` / `skip_pending` scheduler actions so users can choose whether to补跑 or skip a missed task.
- Latest local code work: pending commit `feat: isolate wecom group memory` makes WeCom Bot private chats use per-user Agent identity while group chats use `wecom_bot:group:<chatid>` for shared group conversation and memory. Group messages now carry sender metadata into the Agent prompt, missing group `chatid` messages are dropped before context/memory creation, and each group member gets an independent first-touch welcome state under the group memory directory.
- Latest local code work: pending commit `feat: enrich reasoning effort policy telemetry` enables the local reasoning-effort policy for all users by default, propagates group/private scope into policy routing, separates group/private decision logs while keeping optimizer analysis unified, records optimizer attempts with model/effort/status/failure reason, and appends sanitized task outcome records with final turn count, max-turn exhaustion, and tool-attempt counters.
- Latest local code work: pending commit `fix: make reasoning effort policy local only` removes runtime model-decision routing and the benchmark script, keeps admin-only task-start local reasoning effort rules, defaults uncertain tasks to `xhigh`, and limits future optimizer recommendations to local rule changes.
- Previous local code work: `52398f0` `fix: adapt codex reasoning benchmark` switched the runtime backend state to Codex for testing, fixed locked empty effort handling, and omitted Codex-unsupported `max_output_tokens` and `prompt_cache_retention`.
- Previous local code work: `39d94f2` `feat: add reasoning effort policy routing` added admin-only task-start reasoning effort routing, sanitized decision audit logs, and a background recommendation-only optimizer; latest work supersedes the earlier model-decision path with local-only routing.
- Previous local code work: pending commit `fix: harden admin privacy boundaries` scopes Web memory/dream APIs to the single configured admin profile, scopes Web scheduler output to the admin's own tasks, replaces arbitrary `/api/file?path=` reads with issued tokens plus narrow safe fallback roots, and adds a normal-user bash backstop that restores security policy files if a command tampers with them.
- Previous local code work: pending commit `fix: schedule daily deep dream at midnight` moves automatic Deep Dream out of lazy Agent initialization into an app-level daily scheduler, runs at `00:00` against the previous day's daily memory, and catches up yesterday on restart using persisted per-scope state.
- Previous local code work: `3624f48` `feat: add codex llm backend router` adds a unified `capi`/`codex` LLM backend router, a CodexBot backend using the existing Codex login, daily quota-based auto-switch state, `/backend` commands, web config status, and a `codex-quota-query` skill synced to the runtime skill directory.
- Latest Codex backend work: CodexBot reuses local Codex auth JSON without printing or copying tokens, streams ChatGPT Codex SSE responses, sends Agent tool definitions to Codex through the Responses tool-call adapter, records prompt-cache usage under the `codex` wire label, and defaults Codex reasoning effort to `xhigh`.
- Latest code baseline commit: current commit `feat: optimize cache tool attempts`.
- Latest validation: `tests.test_fast_lane_progress` passed with 14 tests; source `py_compile` passed for touched bridge/channel/runtime files; safe GitHub upload preflight passed before staging.
- Previous local code work: safe GitHub upload skill for WeChat-driven code publishing.
- Latest committed skill sync: `631f159` `feat: sync local cow skills`
- Latest merged code work: `852e909` `feat: add wechat quick progress lane`; `c309e61` `fix: make weixin onboarding greeting deterministic`
- GitHub upload target: new private repository `while4234/CowWeCom` once created.
- Remote layout: `origin` points to `https://github.com/while4234/CowWeCom.git` only; old `while4234/CowWechat` and `upstream` are no longer configured as remotes.
- Working tree: local ignored runtime files such as `config.json`, logs, and personal `knowledge_backend/` data remain on disk but are not tracked/staged.
- Validation: skill display-name regression tests passed; skill manager/test py_compile passed; `node --check channel\web\static\js\console.js` passed; runtime skill config now returns `企业微信 CLI` and `哔哩哔哩`; safe upload preflight passed before staging. Broader prior validation still includes social bridge natural-tone and private knowledge-boundary tests, related Weixin/multi-user/progress regressions, skill script py_compile checks, and web console `node --check`.

## Change Log

- `2026-05-29` pending commit `fix: route grok media generation deterministically`: Replaced broad image-create prefix matching with shared explicit intent routing so legacy `看` / `找` prefixes no longer turn quoted image questions into `IMAGE_CREATE`; updated Weixin, WeCom Bot, Feishu, QQ, and the generic ChatChannel path. Admin/whitelist actors with a personal Grok backend now route explicit natural-language image/video generation directly before Agent, while status/failure questions are kept as text and `image_generation_task` / `grok_video_generation_task` refuse those diagnostic prompts. Added shared send-side image normalization and wired Weixin, WeCom Bot, and WeChatCom App image sends through max width/height plus per-channel byte caps before upload; removed the WeCom Bot hard-coded `/tmp` compression path from the active send flow. Fixed image job final-state persistence so terminal in-memory status is not visible before the state file is written. Config defaults now expose explicit image prefixes and image send limits. Validation: `py_compile` passed for touched routing/channel/tool files; focused pytest passed with `166 passed` for image intent, Grok image/video routing, background jobs, Grok image skill, config regressions, backend routing, and CowCli backend NL; Grok video focused tests passed with `26 passed`.
- `2026-05-28` pending commit `fix: add python 3.13 pydub audioop dependency`: Installed `audioop-lts` in the active `.venv` after `pydub` failed to import on Python 3.13 because stdlib `audioop` is no longer present and `pyaudioop` has no available distribution in this environment. Added the conditional optional dependency to `requirements-optional.txt`, pinned optional `browser-use` to the last checked 0.1 baseline so optional installs do not pull `aiohttp>=3.13` against the main service's `aiohttp<3.10` pin, and constrained `chardet` below 6 to avoid Requests compatibility warnings. Updated README and recorded reusable dependency lessons. Validation: `python -m pip check` passed; runtime dependency import smoke passed; `py_compile` for startup/dependency files passed; CowAgent restarted as PID `26940`; post-restart log scan found no new `pydub` import failure, tracebacks, or errors.
- `2026-05-28` pending commit `fix: repair stale visual group cleanup`: `cleanup_stale_visual_artifact_group_members()` now records affected groups before deleting legacy stale membership rows, repairs each group from current joined members, resets polluted `source_pages`/retrievable state, and clears stale group/page visual chunks. `_mark_visual_group_member_change()` now ignores stale member rows when recomputing pages while preserving the newly changed page during normal grouping. Added four visual regressions for cleanup repair, member-change source pages, grouper cleanup recovery, and table-level old chunk deletion across `chunks`, `chunks_fts`, `source_spans`, and `visual_artifact_chunks`. Validation: `python -m py_compile agent/knowledge/backend/visual_grouping.py agent/knowledge/backend/visual_analyzer.py agent/knowledge/backend/service.py agent/knowledge/backend/storage.py agent/knowledge/backend/models.py agent/knowledge/backend/visual_extractors.py` passed; `PYTHONPATH=. pytest -q tests/test_knowledge_backend_visual.py` passed with 72 passed and 3 skipped; `node --check channel/web/static/js/console.js` passed; safe upload preflight passed before staging.
- `2026-05-26` pending commit `feat: auto-ledger private bill screenshots`: Extends `china-expense-ledger` so private-chat bill-like screenshots are auto-recorded when clear, ask only missing fields when uncertain, learn per-user screenshot UI/item/merchant rules after confirmation, support undoing the latest auto-recorded bill, and keep per-user day/week/month summary caches for fast today/week/month/last-month answers. Group chats remain non-auto-ledger and menu/price-list images are filtered out. Validation pending.
- `2026-05-24` pending commit `fix: route capi monthly switch globally`: Fixed natural-language backend target detection so monthly-card phrases map to `capi_monthly`, updated `/backend` user-facing help and normal-user prompt guidance to expose backend switching to ordinary users, and made midnight backend auto-switch a global daily reset that ignores stale manual overrides and refreshes cloud/scheduler bridge references. Validation: `pytest tests/test_cow_cli_backend_nl.py tests/test_codex_backend_router.py tests/test_scheduler_execution_identity.py` passed with 33 tests; `pytest tests/test_capi_monthly_monitor.py tests/test_openai_responses_api.py tests/test_reasoning_effort_policy.py` passed with 27 tests; touched source `py_compile` passed.
- `2026-05-24` pending commit `fix: smooth long task progress`: Added `long_task_initial_notice_seconds` and a one-time early progress notice that fires only when a long task has produced no user-visible output, using generic status text rather than invented query steps. The existing silence notice wording is more natural and still reports task, elapsed time, phase, turn/tool state, and `/状态`/`/取消` controls. WeCom group messages now carry `chatname` into group metadata when available, label the opaque callback `chatid` as `群会话ID`, and keep `_visible_task_summary` separate so progress notices do not surface injected group metadata. Validation: `pytest tests/test_fast_lane_progress.py tests/test_wecom_social_bridge.py` passed; touched source `py_compile` passed.
- `2026-05-24` pending commit `feat: route capi monthly card`: Added `capi_monthly` as a third backend id, route-aware CAPI provider resolution, midnight monthly-card preference, and a post-task monthly quota monitor that falls back to Codex only when the existing fair-share decision says Codex is under average; otherwise it switches to the original CAPI quota card. `capi_usage.py --format text` now presents daily/monthly cards differently from total quota cards, including daily reset and expiry. Queried the monthly card once and confirmed daily quota `90`, today remaining `90`, and expiry `2026-07-08T10:41:06+08:00`; no key was committed. Validation: `pytest tests/test_codex_backend_router.py tests/test_capi_usage_monitor_skill.py tests/test_capi_monthly_monitor.py tests/test_cow_cli_backend_nl.py tests/test_openai_responses_api.py tests/test_reasoning_effort_policy.py` passed with 52 tests; source `py_compile` passed; runtime `capi-usage-monitor` skill copy synced.
- `2026-05-24` pending commit `fix: surface self-evolution guard skips`: Changed Windows self-evolution bash policy blocks from ordinary error results to explicit skipped/guarded results, propagated policy status through AgentStream events, ChatService tool chunks, and progress snapshots, and excluded guarded skips from tool error counters while preserving policy-skip budget refunds. Strengthened the request-scoped execution policy wording and added self-evolution context telemetry fields to `llm_cache_usage.jsonl`. Validation: source `py_compile` passed; `tests/test_bash_tool.py`, `tests/test_agent_stream_tool_attempt_memory.py`, `tests/test_fast_lane_progress.py`, and `tests/test_llm_usage_tracker.py` passed with 40 tests.
- `2026-05-24` pending commit `fix: gate short post-task reflection model pass`: Added post-task reflection metadata and gating so short non-code tasks skip only the model-based missed-lesson pass when no reusable tool-error lesson changed, while tool-error refresh and deterministic process inference still run. Added snapshots for self-evolution/tool-attempt lesson counts so existing-rule increments also keep model analysis enabled. Updated `cowagent-self-evolution` docs and synced the runtime skill copy. Validation: source `py_compile` passed; `tests/test_cowagent_self_evolution.py` passed with 21 tests; `tests/test_agent_task_limits.py` and `tests/test_agent_stream_tool_attempt_memory.py` passed with 15 tests.
- `2026-05-24` pending commit `chore: publish fund invest advisor skill`: Added `skills/fund-invest-advisor` to Git, including local calculation script, skill instructions, tips, and install/validation notes. Updated `skills/README.md` so the skill appears in the included local skills table. Validation: safe upload preflight passed before staging; `skill-guard` passed; `$env:PYTHONUTF8='1'; quick_validate.py skills\fund-invest-advisor` passed; script `py_compile` passed; `fund_advisor.py invest 1000 8 10` smoke passed. Runtime copy at `C:\Users\RondleLiu\cow\skills\fund-invest-advisor` matched the repo copy before staging.
- `2026-05-24` pending commit `feat: add post-task self-evolution reflection`: Added background post-task self-evolution mining for assistant process text. `AgentBridge` captures assistant text from `message_end` events with tool calls, normal chat defers scheduling until after `runtime.finish_task()`, direct/scheduler paths schedule immediately in a daemon queue, and `ChatService` also schedules after cloud-style runs. `common.self_evolution` now writes redacted `post_task_reflections.jsonl`, supports model-based and deterministic process-lesson extraction, refreshes tool-attempt rules first, and exposes CLI `reflect-task` for diagnostics/backfill. Validation: `py_compile` passed for touched files; `tests.test_cowagent_self_evolution`, `tests.test_agent_stream_tool_attempt_memory`, `tests.test_fast_lane_progress`, and `tests.test_agent_task_limits` passed; CLI `reflect-task` smoke passed; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: cache learned policy before volatile context`: Reordered request context injection so stable self-evolution guidance appears before current-time and retrieved-knowledge context. Added regression coverage that the learned policy block precedes volatile runtime context, preserving system-prompt cache stability while improving cacheability of unchanged learned guidance.
- `2026-05-24` pending commit `fix: keep self-evolution guidance cache-friendly`: Changed dynamic learned self-evolution guidance from system-prompt injection to request-scoped runtime context so rule churn does not invalidate the large system prompt cache prefix. Kept a stable system prompt stub, added prompt-cache metadata for self-evolution context size/hash, sorted guidance deterministically, and added safe tool policy labels such as `action=teleport` for proactive model steering before execution-layer interception.
- `2026-05-24` pending commit `fix: refund policy-skip decision budget`: Added bounded decision-step budget credit for turns where every tool result is a deterministic policy skip, including persisted tool-attempt skips and bash self-evolution guards. This preserves protocol correctness while preventing learned guardrails from prematurely exhausting `max_turns`. Added AgentStream regression coverage with `max_turns=1`.
- `2026-05-24` pending commit `fix: show tool policy memory in self-evolution skill`: Added a public `common.tool_attempt_memory.list_active_rules()` diagnostic API and updated `skills/cowagent-self-evolution/scripts/self_evolution.py` so `doctor` and `list` include both self-evolution shell rules and generic tool-attempt policy rules. Added `list --source self|tools` and workspace-root diagnostics, updated skill docs, and added regression coverage for the CLI and diagnostic API.
- `2026-05-24` pending commit `feat: stabilize self-evolution tool policies`: Added deterministic Windows bash policy guards, specific reusable classifiers for cmd env quoting, fragile `python -c`, and npm `.cmd` shim subprocess failures, plus generic tool-attempt policy-shape rules for repeated unsupported actions, missing required fields, and invalid JSON/schema failures. Active rule reads are cached by file mtime so requests reuse compact snapshots without scanning full logs or doing per-tool file reads.
- `2026-05-24` pending commit `fix: harden find-skills CLI discovery`: Made `skills/find-skills-skill/scripts/find_skills.py` robust when Codex or PowerShell starts without Node's install directory in `PATH`. The helper now discovers `clawhub.cmd` / `npx.cmd` from `PATH` or common Windows Node locations, prefers direct `clawhub search`, falls back to `npx clawhub search`, reports clearer no-CLI wording, and documents the behavior in the skill docs. Added focused tests and synced `C:\Users\RondleLiu\cow\skills\find-skills-skill`. Validation: skill quick validation, script/test `py_compile`, focused pytest, normal ClawHub JSON smoke, runtime-copy smoke, and empty-`PATH` smoke passed.
- `2026-05-24` pending commit `feat: auto-answer private images with context`: Added private image auto-answer routing through the existing ChatChannel session queue. WeCom, Weixin, QQ, Feishu, and DingTalk now enqueue private image-only messages as `IMAGE` contexts; ChatChannel turns them into an Agent text task with the image reference and a prompt to combine image understanding with short-term conversation context and available long-term memory. Group images still cache for an explicit follow-up. Added WeCom regressions for private auto-queueing and group cache-only behavior. Validation: `pytest tests/test_wecom_social_bridge.py tests/test_fast_lane_progress.py tests/test_multi_weixin_instances.py -q` passed with 57 tests; `git diff --check` passed with CRLF warnings only.
- `2026-05-24` pending commit `fix: recover interrupted image generation jobs`: Fixed silent image-generation stalls when CowAgent restarts while a background job is queued/running. Jobs now write a safe `job_state.json`, startup recovery sends either the generated image or a visible restart-interrupted failure, completed same-args jobs no longer suppress fresh requests, and Weixin/WeCom sends return delivery status so generated-but-undelivered images are retried on recovery. Validation: source/test `py_compile` passed; `pytest tests/test_image_generation_background_jobs.py tests/test_app_scheduler_startup.py tests/test_wecom_social_bridge.py tests/test_multi_weixin_instances.py -q` passed with 59 tests.
- `2026-05-24` pending commit `fix: loosen douyin adult-oriented filters`: Updated `daily-douyin-video-harvester` to stop over-filtering normal Douyin-native adult-oriented wording. Default fallback searches now include `擦边舞蹈` and `性感热舞`; scoring treats `性感`, `热辣`, `身材`, `御姐`, and `姐感` as positive signals; and broad hard downranking for `裸露`, `露骨`, `色情`, and `成人` was removed. Clear boundary filters for `约炮`, `成人视频`, `未成年`, and `nsfw` remain. Updated docs/tests and synced the runtime copy under `C:\Users\RondleLiu\cow\skills\daily-douyin-video-harvester`. Validation: source/test `py_compile` passed; `tests/test_daily_douyin_video_harvester.py` passed with 37 tests; real `--dry-run --json --max-total 1 --max-candidates 5 --since-hours 24` returned 1 fresh candidate matching the looser adult-oriented search direction.
- `2026-05-24` pending commit `fix: restore running-task queue notices`: Split queue-notice throttling from visible-output/progress tracking in `SessionRuntime`, so recent Agent output or `/状态` checks no longer suppress the first queued-task reminder for a new normal message. Added a regression test that starts a running task, records recent visible output, queues another normal message, and confirms the user still receives the "已排队" notice while `/状态` reports the pending queue count. Validation: source/test `py_compile` passed; `tests/test_fast_lane_progress.py` passed with 17 tests.
- `2026-05-24` pending commit `fix: filter and dedupe douyin searches`: Updated `daily-douyin-video-harvester` search behavior so default and legacy-default configs use a 24-hour freshness window, Douyin search URLs include platform freshness filters (`publish_time=1` for today/24h, broader filters for diagnostic windows), and search-term dedupe ignores whitespace differences. HTTP searches that fail or return no usable candidates are remembered for the current run, so the browser fallback no longer retries the same normalized term and instead moves on to remaining interest seeds. Updated skill docs/tests and synced the runtime copy under `C:\Users\RondleLiu\cow\skills\daily-douyin-video-harvester`. Validation: source/test `py_compile` passed; `tests/test_daily_douyin_video_harvester.py` passed with 35 tests; real `--dry-run --json --max-total 1 --max-candidates 5 --since-hours 24` returned 1 candidate and confirmed the one-day filter in collected Douyin search requests; safe upload preflight passed before staging.
- `2026-05-24` pending amend `revert: skip tools for plain text chat`: Reverted the pure-text no-tools path added in `98740de5`. The heuristic was too broad for short follow-up commands and removed image-generation/tool schemas from requests that depended on prior context, causing the bot to claim no image tool was available. The rollback deletes `tests/test_agent_plain_text_mode.py`, removes the AgentBridge plain-text classifier, restores normal auto knowledge retrieval, and removes `agent_plain_text_no_tools_enabled` from the template. Validation pending after amend.
- `2026-05-24` pending commit `fix: use profile-backed interest meme search`: Fixed FastLane `/取消` so it cancels only the running task and keeps pending session messages for later dequeue; `/跳过` still clears the queue. Updated `daily-meme-harvester` so Weibo defaults to persistent-profile browser collection, disables silent HTTP fallback by default, filters parseable Weibo candidates older than `since_hours`, and uses interest seeds instead of raw hot-search news. Updated Xiaohongshu defaults to interest seeds, repaired legacy `????` search-filter config at runtime, increased browser query breadth, and accepts modern extensionless `xhscdn.com` note image URLs. Updated `daily-douyin-video-harvester` defaults to interest-seeded profile search with platform-safe light-suggestive/funny seeds and explicit-content downranking. Synced runtime skill copies and refreshed ignored local harvester configs. Validation: source `py_compile` passed; focused pytest passed with 85 tests; real dry-runs returned Weibo and Xiaohongshu candidates through their dedicated profiles; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: keep meme harvester on platform profiles`: Added a prominent Agent routing guard to `daily-meme-harvester` so Xiaohongshu/Weibo diagnostics do not use CowAgent's generic browser profile or public search-engine fallback after thin dry-run results. The guard explains that `~/.cow/browser_profile` is separate from the skill-owned `~/.cow-meme-harvester/*-browser-profile` directories and that generic-browser login walls do not prove the dedicated profile lost login state.
- `2026-05-24` pending commit `fix: stabilize xiaohongshu meme profile`: Changed `daily-meme-harvester` Xiaohongshu collection from unbounded HTTP-first behavior to a dedicated persistent browser profile at `~/.cow-meme-harvester/xiaohongshu-browser-profile`, opened by normal system Chrome for manual login/risk verification and collected through short-lived local CDP by default. Added bounded HTTP fallback limits, dry-run proxy-guard suppression, XHS-only dry-run fallback keywords without hidden Weibo hot-search fetches, legacy config migration for old 12-query/20-second XHS defaults, profile override via `XHS_BROWSER_USER_DATA_DIR`, risk-verification text detection, stricter image URL parsing so static/avatar resources are not treated as content images, and default XHS search filters for recent image notes (`sort_type=time_descending`, `note_type=普通笔记`, `time_filter=一天内`). Added a dedicated Weibo manual-login profile at `~/.cow-meme-harvester/weibo-browser-profile` with `WEIBO_BROWSER_USER_DATA_DIR` and `--open-weibo-profile`, bounded Weibo HTTP search by search terms, suffixes, request timeout, and total time budget, and made legacy configs inherit the expanded serious accident/disaster block keywords so tragedy-news images are not selected as daily memes. Updated docs/tests and synced runtime copy under `C:\Users\RondleLiu\cow\skills\daily-meme-harvester`. Validation: source/test `py_compile` passed; `tests/test_daily_meme_harvester.py` passed with 34 tests; safe GitHub upload preflight passed before staging. Opened the dedicated Xiaohongshu and Weibo profiles once in system Chrome for user login/verification.
- `2026-05-24` pending commit `fix: keep douyin harvester on skill profile`: Updated `daily-douyin-video-harvester` so its default and blank-profile fallback both use the skill-owned persistent Chrome profile under `~/.cow-douyin-video-harvester/browser-profile`, while `DOUYIN_BROWSER_USER_DATA_DIR` can explicitly override the profile when needed. Persistent-browser mode now treats `DOUYIN_COOKIE` as optional instead of warning when the profile is expected to carry session state. Updated skill docs/tests and synced the runtime copy under `C:\Users\RondleLiu\cow\skills\daily-douyin-video-harvester`. Per user instruction, the temporary Codex-profile copy was used only as a one-time local repair and was not added to skill logic or Git. Also checked `daily-meme-harvester`: Weibo/Xiaohongshu use only `WEIBO_COOKIE` / `XHS_COOKIE` from the environment and do not select a browser profile; repo/runtime copies match. Validation: source/test `py_compile` passed; `tests/test_daily_douyin_video_harvester.py` passed with 30 tests; real `--dry-run --json --max-total 3` used the repaired skill profile, discovered 15 meme-worthy hot-board terms, selected 0 videos under the default freshness/quality gate, and did not hit a login wall or cookie warning.
- `2026-05-24` pending commit `fix: surface a2e manual verification`: Fixed the Agent `edit` tool append mode so `oldText=""` creates missing memory files and marks memory dirty instead of returning `File not found`, which prevented daily memory appends from becoming a final-turn tool error. Updated `a2e-daily-checkin` so the helper falls back to A2E tokens when the Chrome LevelDB record lacks the account email, returns `ManualActionRequired.NeedsNotification` when a clicked claim cannot be API-verified or needs human verification, returns `FailureNotification.NeedsNotification` for pre-browser failures, leaves manual-verification pages open, and sweeps verified A2E Chrome windows closed after successful claims. Synced the runtime A2E skill copy under `C:\Users\RondleLiu\cow\skills\a2e-daily-checkin`. Validation: PowerShell parser check passed; source/test `py_compile` passed; `tests/test_knowledge_guard.py` and `tests/test_a2e_daily_checkin_skill.py` passed with 11 tests.
- `2026-05-24` pending commit `feat: add natural backend switch aliases`: Added a precision-first `cow_cli` natural-language backend parser so phrases like "帮我切换到 CAPI 后端" and "现在后端状态是什么" execute through the existing `/backend` path before Agent reasoning, while informational/negative phrases keep flowing to Agent. Added parser, dispatch, and event-interception tests. Validation: `tests.test_cow_cli_backend_nl` passed with 9 tests; `tests.test_codex_backend_router` passed with 7 tests; touched source `py_compile` passed; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: avoid reasoning policy circular import`: During CAPI effort-routing validation, a direct `common.reasoning_effort_policy` import exposed a circular import through `agent.protocol`. Moved the optimizer-only `LLMRequest` import into `run_policy_optimizer_once` and updated the AgentBridge metadata test stub for the current onboarding hook signature. Validation: source `py_compile` passed; `tests.test_reasoning_effort_policy` and `tests.test_llm_usage_tracker` passed with 25 tests. Additional CAPI smoke: local routing produced `medium` for simple IM and `xhigh` for coding on `gpt-5.5`; Responses payloads carried `reasoning.effort=medium/xhigh`; real CAPI calls to `gpt-5.5` accepted both `medium` and `xhigh` and returned `OK`.
- `2026-05-24` pending commit `fix: make cli restart exit cleanly`: Reworked `cli/commands/process.py` so plain `cow restart` / `.venv\Scripts\python.exe -m cli.cli restart` stops and starts CowAgent then exits instead of tailing logs forever. Interactive log following moved to `--logs`, Windows termination now uses `taskkill /T`, stop checks that processes actually exit before removing `.cow.pid`, stale PID recovery scans exact current-project `app.py` processes, and a lightweight process lock prevents overlapping start/stop/restart commands. Validation: process source `py_compile` passed; `tests/test_process_commands.py` and scheduler startup/regression suites passed with 27 focused tests; real restart smoke passed twice and `/chat` returned HTTP 200.
- `2026-05-24` current commit `fix: reload clash verge runtime rules`: Updated `scripts/clash_verge_rule_guard.py` so one normal run patches both the active subscription profile and Clash Verge's generated runtime `clash-verge.yaml`, normalizes required rule indentation to the existing `rules:` list style, and calls the Mihomo/Clash external-controller reload API after runtime config changes. Added `--runtime-profile`, `--controller`, `--no-runtime-apply`, `--no-reload`, and `--reload-always`. Validation: script/test `py_compile` passed; `tests.test_clash_verge_rule_guard` passed with 12 tests; real guard run fixed generated runtime config and `core reloaded`; real browser checks loaded Douyin (`https://www.douyin.com/jingxuan`) and Xiaohongshu (`https://www.xiaohongshu.com/explore`).
- `2026-05-24` pending commit `fix: notify missed scheduler runs`: Starts the normal CowWechat scheduler from `app.run()` after channels start instead of waiting for the first Agent message, so daily jobs survive process restarts. Scheduler runs that are overdue beyond the grace window now record `last_missed_run_at`, notify the original task receiver, and leave the user to decide whether to `run_now` or `skip_pending`; invalid schedules are disabled with a notice instead of failing silently. Execution callbacks now return success/failure, WeCom scheduler delivery uses result-aware proactive send, failed runs send a failure notice, and scheduler tools gained owner-scoped `run_now` / `skip_pending` actions. Validation: source `py_compile` passed; `tests/test_app_scheduler_startup.py`, `tests/test_scheduler_service.py`, `tests/test_scheduler_execution_identity.py`, `tests/test_scheduler_duplicates.py`, `tests/test_web_admin_privacy.py`, and `tests/test_wecom_social_bridge.py` passed with 38 focused tests.
- `2026-05-24` pending commit `feat: isolate wecom group memory`: Updated WeCom Bot identity derivation so private chats keep `wecom_bot:<user_id>` while group chats share `wecom_bot:group:<chatid>` for `conversation_id` and group-level `memory_user_id`. Added group sender metadata and prompt prefixing so group memory can track who spoke, dropped group callbacks without `chatid`, and added per-group-member onboarding files under `memory/users/<group_memory_user_id>/members/<sender_slug>/USER.md` with a lightweight welcome that asks only how to address the member. Validation: source `py_compile` passed; `tests/test_wecom_social_bridge.py`, `tests/test_agent_onboarding_welcome.py`, and `tests/test_multi_user_isolation.py` passed with 36 tests; safe upload preflight passed before staging.
- `2026-05-24` pending commit `feat: enrich reasoning effort policy telemetry`: Enabled reasoning-effort policy defaults for all users instead of admin-only, propagated `isgroup` into Agent model metadata, split sanitized policy audit logs into private and group JSONL files, and kept optimizer reads unified across private/group/legacy logs. Added decision features useful for future local-rule optimization, optimizer attempt JSONL records with model/effort/success/failure reason, task outcome records keyed by `task_id` with final turn count, max-turn exhaustion, status, final response hash/size, and tool-attempt counters, plus usage metadata for `reasoning_effort_chat_scope`. Validation: source `py_compile` passed; `tests.test_reasoning_effort_policy` passed with 17 tests; `tests.test_llm_usage_tracker` passed with 8 tests; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: make reasoning effort policy local only`: Removed runtime model-decision routing, model benchmark tooling, and classifier config keys from the reasoning effort policy. The policy now uses local rules only: obvious simple IM/short rewrite/translation/explanation gets `medium`, obvious coding/debugging/repo/tool/high-risk/quality-first work gets `xhigh`, and uncertain tasks conservatively default to `xhigh`. Audit and usage telemetry now record only local rule metadata, and the recommendation-only optimizer prompt focuses on adding or tightening local rules rather than model selection. Runtime ignored `config.json` was also cleaned of obsolete classifier keys.
- `2026-05-24` pending commit `fix: adapt codex reasoning benchmark`: Switched ignored runtime backend state to Codex for benchmark testing and fixed Codex classifier-call compatibility. `AgentLLMModel` now forwards the `reasoning_effort_locked` marker, OpenAI-compatible and Codex backends honor locked empty effort without falling back to configured `xhigh`, CodexBot no longer sends unsupported `max_output_tokens` or `prompt_cache_retention`, and Codex mini ranking now prefers numeric recency so `gpt-5.4-mini` is tested before `gpt-5.1-codex-mini`. Validation: `tests.test_codex_direct_backend` and `tests.test_reasoning_effort_policy` passed with 22 tests; Codex benchmark with 6 balanced samples and 700ms threshold tested `gpt-5.4-mini`, `gpt-5.5/none`, and `gpt-5.5/low`. All classified correctly with no false-medium errors, but all exceeded the 700ms threshold, so no classifier config was selected. CowAgent restarted on Codex backend and `/chat` returned HTTP 200.
- `2026-05-24` pending commit `feat: add reasoning effort policy routing`: Added a disabled-by-default, admin-only reasoning effort policy that decides once per Agent task, keeps the selected effort sticky across later LLM calls, supports high-confidence local `medium`/`xhigh` rules, calls one configured same-backend classifier for uncertain tasks, and falls back to `xhigh` on classifier absence, backend mismatch, timeout, invalid output, or errors. Added sanitized `reasoning_effort_policy_decisions.jsonl` audit logging, safe usage metadata fields, a recommendation-only background optimizer triggered after a configurable number of decisions, and `scripts/benchmark_reasoning_effort_policy.py` for choosing exactly one classifier. Validation: focused py_compile passed; `tests.test_reasoning_effort_policy`, `tests.test_llm_usage_tracker`, `tests.test_openai_responses_api`, and `tests.test_codex_backend_router` passed. Benchmark smoke on current CAPI backend with 6 balanced samples and 700ms threshold tested `gpt-5.4-mini`, `gpt-5.5/none`, and `gpt-5.5/low`; all exceeded the threshold and returned no selected classifier recommendation.
- `2026-05-24` pending commit `fix: harden admin privacy boundaries`: Added a single-admin profile resolver for non-chat admin surfaces, made Web memory/dream APIs read only `memory/users/<admin_memory_user_id>/...`, made Web scheduler output use owner-scoped `TaskStore.list_tasks_for_owner`, and narrowed `/api/file` so new file-send events use expiring server-issued tokens while legacy path fallback only serves upload/tmp/download/attachment/browser-download roots or `workspace/users/*/files`. Kept normal-user bash enabled for skill/helper UX, but added a post-execution integrity backstop that restores core security policy files and fails the tool result if normal-user bash tampers with them. Validation: source `py_compile` passed; `tests.test_web_admin_privacy`, `tests.test_multi_user_isolation`, `tests.test_scheduler_duplicates`, `tests.test_memory_deep_dream_scheduler`, and `tests.test_scheduler_execution_identity` passed.
- `2026-05-24` pending commit `fix: schedule daily deep dream at midnight`: Added an app-level `daily_dream_scheduler` started from `app.run()`, with startup catch-up for yesterday, idempotent per-scope state under the agent workspace memory directory, active-agent daily flush before dreaming, shared/user memory scope discovery, and date-anchored `MemoryFlushManager.deep_dream(...)` so midnight runs process the previous day rather than today's partial file. Updated config defaults/templates for `memory_deep_dream` and added focused scheduler/date-anchor/app-startup tests. Validation: source `py_compile` passed; `tests.test_memory_deep_dream_scheduler` passed with 6 tests; memory/multi-user/tool-attempt regressions passed with 26 tests; Codex backend regressions passed with 15 tests.
- `2026-05-24` pending commit `feat: add codex llm backend router`: Added a persistent `llm_backend` router state under ignored `data/llm-backend-router/`, CodexBot as a first-class non-image LLM backend using existing Codex auth, Agent/Web/Bridge/cow_cli routing through the effective backend, daily 00:00 quota evaluation with fair-share/remaining/manual-override/latch policy, `/backend` and quota aliases, sanitized web backend status, and the `codex-quota-query` skill copied to `C:\Users\RondleLiu\cow\skills\codex-quota-query`. Codex tool calls stay on Codex, not CAPI, and Codex prompt-cache telemetry uses `wire_api=codex` with default reasoning effort `xhigh`. Validation: source `py_compile` passed for touched router/backend/web/skill files; `tests.test_codex_backend_router`, `tests.test_codex_quota_query_skill`, and `tests.test_codex_direct_backend` passed with 18 tests; image-generation Codex-auth and OpenAI Responses regressions passed with 17 tests; skill quick validation passed for repo and runtime copies; `node --check` passed for web console JS and the quota Node script.
- `2026-05-23` pending commit `feat: add social hot meme harvesters`: Updated `daily-meme-harvester` for hot-topic-driven Weibo + Xiaohongshu image harvesting, per-provider TOP3 selection, cross-provider topic dedupe without backfill, same-post image dedupe, XHS proxy-rule guard retry, and optional WeCom image sending. Added `daily-douyin-video-harvester` as a separate Douyin TOP3 hot-video skill with persistent-browser hot-board discovery, 48-hour freshness filtering, one-video-per-hot-term diversity, meme-worthiness scoring, sharp commentary, atomic video/cover downloads, WeCom sending, dedupe state, and 24-hour cleanup scheduling. Validation: meme and Douyin scripts passed `py_compile`; `tests.test_daily_meme_harvester` and `tests.test_daily_douyin_video_harvester` passed; real Weibo/Xiaohongshu image-send smoke downloaded and sent 3 Weibo images to the admin private chat, while Xiaohongshu produced no downloadable candidates in that run; Douyin persistent-browser dry-run discovered current hot-board terms and intentionally returned 0 candidates under the 48-hour freshness/quality gate when only stale/generic candidates were available, while a 7-day diagnostic window found an older `峰哥` candidate that the default gate correctly skipped.
- `2026-05-23` current commit `feat: add clash verge rule guard`: Added `scripts/clash_verge_rule_guard.py`, a no-dependency helper that resolves the active Clash Verge Rev profile, inserts Xiaohongshu/Douyin `DIRECT` rules at the top of `rules:`, supports `--watch` polling after subscription file changes, and avoids printing proxy node contents. Added focused unit coverage for insertion order, duplicate prevention, dry-run behavior, current profile resolution, JSON output shape, and preserving normal UTF-8 files without adding a BOM. Validation: script/test `py_compile` passed; `tests.test_clash_verge_rule_guard` passed with 6 tests; real active Clash profile dry-run reported all required rules already present.
- `2026-05-23` pending commit `fix: deploy weibo-only meme harvester`: Removed X/Twitter from the deployed default path for `daily-meme-harvester`, set the default and persisted local config to Weibo-only TOP3 with a larger candidate pool for dedupe resilience, kept output path configuration, and synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\daily-meme-harvester`. Validation: CowAgent restart and `/chat` healthcheck passed; skill quick validation passed; script `py_compile` passed; `pytest tests/test_daily_meme_harvester.py` passed with 10 tests; fresh real Weibo TOP3 smoke downloaded 3 images and generated `manifest.jsonl`, `index.md`, and state files.
- `2026-05-23` `e936f6c` `fix: default shopping comparison links`: Changed `shopping-lite-compare` so manual product links and copy commands are returned by default via the local `taobao`/`maishou` detail path while still forbidding automatic opens, clicks, carts, orders, sensitive form filling, or automatic rebate-link use. Added explicit Pinduoduo platform handling and kept `--no-links` for cases where the user explicitly asks not to include links. Synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\shopping-lite-compare`. Validation: skill quick validation passed; helper `py_compile` passed; default-link Taobao smoke, `--no-links` Taobao smoke, and Pinduoduo link smoke passed without opening a browser.
- `2026-05-23` pending commit `feat: add daily meme harvester skill`: Added `daily-meme-harvester` with `harvest_memes.py`, configurable output path priority, Weibo hot-search image collection, official X API collection, conservative Xiaohongshu public-page/user-cookie collection, optional Reddit support, sensitive keyword/provider filtering, URL and SHA-256 dedupe state, atomic image writes, `manifest.jsonl`, `index.md`, and `state_delta.json`. Synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\daily-meme-harvester`. Validation: skill quick validation passed; `py_compile` passed; `pytest tests/test_daily_meme_harvester.py` passed with 9 tests; `--help` passed; dry-run JSON passed. Real TOP3 smoke for `weibo,xiaohongshu,x` created output/state files but downloaded 0 images because Weibo search returned access-limited/invalid public responses, Xiaohongshu public pages failed SSL access without `XHS_COOKIE`, and `X_BEARER_TOKEN` was not set.
- `2026-05-23` `6f0143b` `fix: constrain lite commerce skills`: Updated `takeout-lite-recommender` to allow displaying `meituan-coupons` returned `jump_url` links for manual user tapping while forbidding automatic opens/clicks, and hardened `shopping-lite-compare` so it must use the local `taobao`/`maishou` read-only search helper rather than browser/Baidu/public-web fallback. Added `shopping_compare_helper.py`, which locates the installed OpenClaw `taobao` script and calls its `search` path only. Validation: skill quick validation passed; py_compile passed for takeout and shopping helper scripts; helper returned a read-only Apple 20W charger comparison without opening a browser; safe upload preflight and staged diff checks passed. Restarted CowAgent after push; service running as PID `39676`.
- `2026-05-23` pending commit `fix: repair skill display names`: Added `display-name` frontmatter to `wecom-cli` and `bilibili-all-in-one`, taught `SkillManager` to replace persisted `????` placeholder display names from frontmatter while preserving valid manual labels, kept skill card titles after enable/disable toggles, synced the two runtime `SKILL.md` files, and refreshed `C:\Users\RondleLiu\cow\skills\skills_config.json` so the web console shows `企业微信 CLI` and `哔哩哔哩`. Validation: `tests.test_skill_display_names` passed; `py_compile` passed for `agent\skills\manager.py` and the new test; `node --check channel\web\static\js\console.js` passed; safe upload preflight passed before staging.
- `2026-05-23` pending commit `feat: add long task expectation notices`: Added per-session visible-output tracking, silence-based long-task notices, `/状态` visibility resets, friendly Agent error fallback text, max-step pre-summary notices via existing `turn_end` events, config defaults for long-task expectation thresholds, and focused fast-lane progress tests. Validation: `tests.test_fast_lane_progress` passed with 14 tests; touched source files passed `py_compile`; safe upload preflight passed before staging.
- `2026-05-23` current commit `feat: optimize cache tool attempts`: Added safe tool-attempt memory for repeated non-retryable failures, request-local read-only tool result reuse, request-copy compaction for older oversized tool results, and cache telemetry/report counters for tool attempts, skips, duplicate successes, memory rule hits, and compacted results. Validation: focused tool-attempt, prompt-cache, Responses API, knowledge-context, scheduler duplicate, and CowAgent self-evolution pytest suites passed; safe upload preflight passed before staging.
- `2026-05-23` current commit `feat: add community skill discovery guards`: Added localized `find-skills-skill`, `skill-guard`, `quick-weather`, and `fast-market-price` CowWechat skills; synced the community search workflow from ClawHub's popular `find-skills-skill`, added a local pre-install static security gate based on `skill-guard`, and extracted qq_openclaw-style Open-Meteo weather plus Coinbase/Yahoo quick quote scripts. Validation: quick validation passed for all four skills; scripts passed `py_compile`; `find_skills.py` returned known ClawHub leads plus live `npx clawhub search`; `quick_weather.py "明天成都天气"` returned an Open-Meteo forecast; `fast_price.py "比特币"` returned Coinbase BTC/USD; `fast_price.py "黄金"` returned Yahoo Finance `GC=F`; `skill-guard` passed on clean skills and blocked a malicious temp skill with prompt injection and token upload text.
- `2026-05-23` current commit `feat: add takeout and shopping lite skills`: Added local `takeout-lite-recommender` and `shopping-lite-compare` skills, synced their runtime copies, installed the requested OpenClaw ClawHub skills (`eat-what-today-skill`, `meituan-coupons`, `taobao`), and documented that the installed `taobao` package is a `maishou` skill with `maishou88.com` requests, a default invite code, hardcoded openid, possible commission fields, and detail/share purchase-link generation. Validation: both skill quick validations passed with `PYTHONUTF8=1`; `takeout_lite.py` py_compile passed; takeout sample output included 3 recommendations, reasons, budget ranges, Meituan search keywords, coupon reminder, and compliance notice; routing/disclosure smoke passed. Per user instruction, this local integration is not pushed to `origin`.
- `2026-05-23` current commit `docs: document wecom bot auth source`: Added `wecom_bot_auth_source` to defaults and safe templates, and updated WeCom Bot setup docs/quickstart to explain QR creation source fallback to manual Bot ID/Secret configuration. Validation: `py_compile config.py` passed; `node --check channel\web\static\js\console.js` passed; safe upload preflight passed.
- `2026-05-23` current commit `chore: add requested cow skills`: Installed `bdpan-storage`, `bilibili-all-in-one`, `frontend-design`, and `wecom-cli` from the requested sources, synced runtime copies into `skills/`, normalized upstream frontmatter where needed for CowWechat validation/loading, and intentionally skipped `plugin-amap` because its package hard-requires LinkAI plugin execution through `LINKAI_API_KEY`. Validation: quick validation passed for runtime and repo copies; SkillManager loaded all four installed skills; Bilibili import smoke passed; safe upload preflight passed.
- `2026-05-23` current commit `feat: add cache shape telemetry`: Added safe request-shape hashes/counts to LLM usage telemetry, grouped cache reports by `request_kind`, surfaced long-input zero-cache metrics, stabilized retrieved knowledge snippets as compact JSON lines, and lightly extended the cache dashboard recent table. Validation: focused prompt-cache/Responses/knowledge-context pytest passed with 19 tests; `git diff --check` passed with CRLF warnings only; safe upload preflight passed before staging.
- `2026-05-23` `b43fd74` `fix: make github skill use local credentials`: Updated the GitHub skill so it no longer requires `jq` or a shell-exported `GITHUB_TOKEN`; added a helper that resolves `GITHUB_TOKEN`, `GH_TOKEN`, or Git Credential Manager credentials and calls the GitHub REST API through `curl.exe`; removed the unsafe token-in-remote guidance; synced the runtime skill copy. Validation: skill quick validation passed; helper resolved a local token and authenticated `/user` as `while4234`; safe upload preflight passed.
- `2026-05-23` current commit `feat: add playwright browser control skill`: Added the local persistent Playwright browser-control skill to the project skills directory, included its agent metadata and setup notes, documented it in the skills index, and synced the deployed workspace copy. Validation: skill quick validation and safe upload preflight passed.
- `2026-05-23` current commit `fix: prefer manual wecom bot setup`: Changed the WeCom bot console flow to default to manual Bot ID/Secret entry and replaced QR auth creation with a warning that it grants creator data permissions, causing non-creators to see "仅限创建者本人可使用". Validation: `node --check channel\web\static\js\console.js` passed; Web restart smoke returned 200; safe upload preflight passed.
- `2026-05-23` current commit `fix: isolate per-user onboarding`: Changed onboarding detection to fall back to per-user `USER.md` for resolved chat profiles, auto-create a safe placeholder user file for new actors, and keep initialized users from seeing the first-run welcome repeatedly. Validation: agent onboarding, WeCom social bridge, and multi-user isolation unittest suites passed; py_compile passed for touched files; safe upload preflight passed.
- `2026-05-23` `a5c4062` `chore: prepare cowwecom github baseline`: Changed safe templates and Docker compose defaults to `wecom_bot`, added `COWWECOM_QUICKSTART.md`, removed generated knowledge sqlite/PDF/derived/report artifacts from Git tracking while keeping local files ignored, reset the knowledge manifest to an empty safe baseline, and updated safe upload preflight/tests to allow staged deletion of protected runtime files. Validation: staged preflight OK; focused pytest passed with 82 total tests; py_compile passed for touched skill scripts.
- `2026-05-23` current commit `feat: add a2e daily checkin skill`: Added project-local A2E check-in skill, bundled the existing PowerShell browser/profile helper, added a safe example state while ignoring the real local state file, synced the deployed workspace copy, added focused SkillManager discovery tests, and added hard project rules requiring local Git for all projects and automatic GitHub push for this CowWechat repository. Validation: A2E skill pytest passed; skill quick validation passed; PowerShell parser check passed; safe upload preflight passed.
- `2026-05-22` `feat: add safe github upload skill`: Added `safe-github-upload` with a preflight guard for staged protected files and staged secret-like content, documented origin-only upload workflow, updated `skill-creator` so CowWechat skill development must sync both `$HOME\cow\skills` and `skills/`, deployed the updated skills to the workspace copy, and added focused discovery/preflight tests. Validation: safe upload and skill-creator quick validation passed; preflight script py_compile passed; related pytest passed with 8 tests.
- `2026-05-22` `fix: harden social bridge relay delivery`: Made bridge target resolution fall back to the current LLM when deterministic IDs/names/relationship aliases do not match; added spouse aliases; fixed user display fallback for `鐜夌帀 / Rikoo032700`; stopped empty-context Weixin public ID inference; made Weixin text sends check API `ret` and treat send timeouts as failures rather than delivered messages; added local CowAgent skill/self-evolution files. Validation: focused social bridge/multi-Weixin/tool tests passed with 48 tests, related py_compile passed, service restart smoke returned `/chat` 200, and a direct non-send probe resolved `鑰佸﹩`/`閰嶅伓` to the intended bridge target.
- `2026-05-22` `0450e24` `feat: capture browser downloads and knowledge exports`: Captured Playwright downloads from browser clicks into the active tool workspace, returned send-compatible file metadata, updated knowledge backend study/export service and web console support, and added related generated study artifacts/scripts. Validation: `.venv\Scripts\python.exe -m py_compile agent\tools\browser\browser_service.py agent\tools\browser\browser_tool.py` OK; local selector and ref PDF download smokes OK; `.venv\Scripts\python.exe -m pytest tests\test_knowledge_backend_service.py tests\test_knowledge_backend_complete.py` OK (12 tests); `node --check channel\web\static\js\console.js` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: widen trusted normal-user permissions`: Added code-default normal-user read roots for workspace temp/download/attachment directories, system temp, `~/Downloads`, and `~/Desktop`; allowed shared `knowledge/` writes by default; added config extension keys; and kept file deletion plus CowAgent project modification blocked. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_knowledge_guard tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (69 tests); `.venv312\Scripts\python.exe -m unittest tests.test_multi_user_isolation` OK (16 tests); `.venv312\Scripts\python.exe -m compileall agent/access_control.py agent/user_profiles.py bridge/agent_initializer.py config.py tests/test_multi_user_isolation.py` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: soften bridge tone and protect private knowledge`: Made social bridge generated messages sound more like a warm friend relaying a message, removed stiff public wording such as `鎺堟潈鎴戣浆杩癭, added a non-apology bridge-message test, added prompt/skill/tool guardrails that keep private Weixin users, Bridge IDs, social-bridge reachability, and cross-user relay metadata out of `knowledge/`, and cleaned the local workspace knowledge page that had captured 灏忔爛/Rondle bridge details. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_knowledge_guard tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (65 tests); `.venv312\Scripts\python.exe -m compileall agent/prompt/builder.py agent/prompt/workspace.py agent/social_bridge/service.py agent/tools/knowledge_guard.py agent/tools/write/write.py agent/tools/edit/edit.py tests/test_knowledge_guard.py tests/test_social_bridge_service.py` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: route bridge active sends through running app`: Fixed social bridge proactive sends when CowAgent is launched as `python app.py` by keeping a canonical `app` module reference and falling back to the `__main__` ChannelManager. Pending bridge messages are now visible/retryable by both sender and target, with a sender/status index. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (59 tests); `.venv312\Scripts\python.exe -m compileall app.py agent/social_bridge/service.py agent/social_bridge/store.py tests/test_social_bridge_service.py tests/test_social_bridge_store.py` OK; `git diff --check` OK with CRLF warnings only. Note: one unrelated image-generation concurrency timing test exceeded its local 0.65s threshold during a broad run, while the affected manager lookup test passed separately.
- `2026-05-22` `fix: show declared names in bridge directory`: Made the social bridge directory derive public display names from both `USER.md` and `MEMORY.md`, including Agent-written formats such as `鐢ㄦ埛甯屾湜琚О涓恒€屽皬鏍€銆峘 and `鐢ㄦ埛绉板懠锛氬皬鏍€`, while ignoring blank-template placeholders. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (57 tests); `.venv312\Scripts\python.exe -m compileall agent/social_bridge/service.py tests/test_social_bridge_service.py` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `feat: add social bridge messaging`: Added controlled cross-Weixin user directory, relationship memory, authorized bridge messaging, pending/retry handling, Weixin proactive text routing, social bridge tool registration, config defaults, and focused privacy/active-send tests. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_social_bridge_service tests.test_multi_weixin_instances tests.test_multi_user_isolation` OK (47 tests); `.venv312\Scripts\python.exe -m compileall agent\social_bridge agent\tools\social_bridge channel\weixin\weixin_channel.py config.py` OK; ToolManager bridge-tool loading smoke OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: make token usage skill read CowAgent runtime logs`: Updated `token-usage-tracker` to fall back to `data/llm_cache_usage.jsonl`, surface cache/reasoning metrics, and fixed the Bash tool so skill subprocesses receive only string env vars and resolve the active Python interpreter.
- `2026-05-22` `fix: persist real Weixin id mappings`: Stopped the channels API from exposing iLink `@im.wechat` raw IDs as display WeChat IDs, added save support for real WeChat IDs on Weixin channel cards, and covered the mapping path with focused tests.
- `2026-05-22` `d4976d2` `fix: show multi-weixin users and real ids`: Added dynamic `weixin_*` channel cards, repeated QR login for new WeChat users, admin/member role display, startup credential backfill, and real WeChat ID mapping for prompt-cache/user labels.
- `2026-05-22` `631f159` `feat: sync local cow skills`: Synced the seven locally installed CowWechat skills into `skills/`, added per-skill `INSTALL.md` files, documented required user-provided API keys, omitted pycache/local state, and sanitized local-only paths.
- `2026-05-22` `852e909` `feat: add wechat quick progress lane`: Added `/q` progress and quick-reply fast lanes, `/鐘舵€乣/`/鍙栨秷`/`/璺宠繃` local controls, per-session progress snapshots, cooperative Agent cancellation, and fast-lane unit tests.
- `2026-05-22` `c309e61` `fix: make weixin onboarding greeting deterministic`: Added a narrow pre-model onboarding greeting path so a clean workspace with `BOOTSTRAP.md` returns the full first-run welcome for pure greeting messages.
- `2026-05-22` `3fc82be` `docs: update latency telemetry notes`: Updated local handoff and Git notes after latency telemetry validation.
- `2026-05-22` `3d39398` `feat: add latency telemetry for weixin replies`: Added hashed request latency logs for session queue wait, channel handling, AgentBridge execution, and each LLM stream turn so slow Weixin replies can be attributed to queueing, model latency, cache misses, tools, persistence, or send time.
- `2026-05-22` `7598c2d` `feat: improve prompt cache telemetry`: Added prompt cache usage visibility and persisted cache hit metrics for the current deployment.
- `2026-05-22` `4ed1edf` `feat: add safe github code update skill`: Added natural-language `code-update` skill plus guarded `git_code_update` tool for fast-forward-only updates that refuse dirty worktrees and protected config/secret paths.
- `2026-05-22` `51e861b` `docs: document github remote layout`: Documented that Codex/UI pushes should use the user's `origin` remote while the original project is kept as fetch-only `upstream`.
- `2026-05-22` `cd29392` `fix: preserve private memory isolation in shared scope`: Prevented shared memory access from exposing other users' private memory entries and added isolation tests.
- `2026-05-22` `40ee608` `chore: include latest web console state`: Captured the final current web console cache-view state before GitHub upload.
- `2026-05-22` `af5f161` `chore: snapshot current CowWechat workspace`: Captured the current local workspace, including image generation tooling, access control/user isolation helpers, scheduler tests, and GitHub-upload ignore hardening.
- `2026-05-21` `3d47ee5` `docs: note browser dependency installation`: Documented browser dependency installation.
- `2026-05-21` `c135c23` `chore: document browser and vision optional deps`: Documented Playwright/browser-use/agentmesh and image/QR/HTML helper dependencies in optional requirements after installing them locally.
- `2026-05-21` `f44c612` `feat: cover OpenAI multimodal capability config`: Added OpenAI multimodal capability configuration and focused tests.
- `2026-05-21` `b6d43bb` `feat: add OpenAI responses wire mode`: Added Responses API wire-mode support across OpenAI-compatible chat, streaming, tools, vision, voice gateway handling, config, and focused tests.
- `2026-05-21` `ac69a90` `deploy: document windows weixin deepseek setup`: Initial local Windows Weixin deployment documentation and safe ignore policy.

## Rollback Notes

- To roll back the GitHub upload snapshot, inspect `git show af5f161` and `git show 40ee608`, then revert in reverse order if needed.
- To roll back the CowWechat skill sync, revert `631f159`; local installed skills under `~/cow/skills` are not changed by reverting the repository.
- To roll back latency telemetry only, revert `3d39398` after confirming no runtime-only config files are staged.
- To roll back the `/q` fast-lane behavior only, revert `852e909` after confirming no runtime-only config files are staged.
- Local runtime config is ignored; rolling back code does not change `config.json`, `.env*`, Weixin credentials, or local virtual environments.
- The active service can be checked with `PYTHONUTF8=1 .venv\Scripts\python.exe -m cli.cli status`.
- `2026-05-28` pending commit `fix: hot switch voice and preserve bill amounts`: Analyzed the latest local logs for the reported voice-mode delay, 15:20-ish bill amount mismatch, memory-search miss, and bill clarification false fast-path. Findings: the image recognition/cache path had the exact 99.88 amount available while the structured ledger payload carried rounded 10000 cents; memory search missed because the long-term memory SQLite index had no chunks/files indexed after embedding sync failures; voice switching was slow because config.json edits do not change the in-process `conf()` until reload/restart and already-started voice turns keep their decision. Code changes: CowCli now exposes `/voice`, natural `开启/关闭语音模式`, and hot-updates both runtime config and config.json without restart after this deployment is loaded; `VoiceReplyStreamer` re-checks runtime voice switches before/during segment TTS and stops further voice sends while allowing final text fallback; image ledger payloads prefer exact screenshot raw-text amounts over rounded model `amount_cents` unless a follow-up answer explicitly states an amount; CowCli backend/ledger natural parsers now let bill clarification phrases like `这个账单是中转api额度卡` pass through instead of treating them as CAPI quota or local ledger queries. Runtime `china-expense-ledger` skill copy synced to `C:\Users\RondleLiu\cow\skills\china-expense-ledger`. Validation so far: `.venv\Scripts\python.exe -m py_compile plugins\cow_cli\cow_cli.py plugins\cow_cli\backend_nl.py skills\china-expense-ledger\scripts\ledger.py channel\voice_streamer.py` passed; `.venv\Scripts\python.exe -m pytest tests/test_china_expense_ledger.py tests/test_cow_cli_backend_nl.py tests/test_voice_reply_streamer.py -q` -> 98 passed, 1 pydub/audioop deprecation warning.
