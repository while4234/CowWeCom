# Git Notes

## Repository Purpose

Personal CowWeCom deployment and development workspace for WeCom Bot integration, OpenAI-compatible model providers, image generation tooling, scheduler tools, and agent memory features.

## Ignore And Secret Policy

Runtime secrets and local state stay out of Git. The repository ignores `config.json`, env files, key/certificate files, Weixin/WeCom credentials, cookies, tokens, session files, logs, virtual environments, generated build output, generated knowledge indexes/originals/reports, `.codex/`, `.playwright-mcp/`, and other local workspace artifacts.

Do not commit API keys, QR login material, credential JSON files, chat logs, cookies, tokens, or local auth material. Use committed templates and documentation only for placeholders and safe defaults.

## Current Baseline

- Latest local code work: pending commit `fix: recover interrupted image generation jobs`: Persists image-generation background job state without prompts/args, recovers interrupted queued/running jobs on app startup, resends generated images when output exists, sends a visible failure notice when a restart orphaned the job, avoids reusing completed duplicate jobs, and makes Weixin/WeCom image delivery result-aware so generated-but-undelivered images remain recoverable.
- Latest local code work: pending commit `fix: loosen douyin adult-oriented filters`: Loosens `daily-douyin-video-harvester` handling for normal Douyin-native adult-oriented terms by changing the default seeds from `轻擦边舞蹈` to stronger `擦边舞蹈` / `性感热舞` style searches, adding positive scoring for `性感`, `热辣`, `身材`, `御姐`, and `姐感`, and removing broad `裸露` / `露骨` / `色情` / `成人` hard downranking while retaining clear boundary filters for `约炮`, `成人视频`, `未成年`, and `nsfw`. Runtime skill copy synced.
- Latest local code work: pending commit `fix: restore running-task queue notices`: Restores the immediate "上一条还在处理，已排队" notice for normal messages sent while a task is already running. Queue-notice throttling now uses its own runtime timestamp instead of being reset by visible output/progress events, while `/状态` still reports pending queue count through the fast lane.
- Latest local code work: pending commit `fix: filter and dedupe douyin searches`: Tightens `daily-douyin-video-harvester` to default fresh searches to 24 hours, add Douyin search-filter params (`publish_time=auto`, mapping today to one-day filter), normalize search terms for whitespace-insensitive dedupe, and remember failed/empty search terms within a run so browser fallback changes terms instead of reopening the same failed query. Runtime skill copy synced.
- Latest local code work: pending amend `revert: skip tools for plain text chat`: Reverted `98740de5` because the pure-text no-tools heuristic treated context-dependent short commands such as "直接帮我生成吧" as text-only, hid image-generation/tools from the model, and made tool availability worse. Full Agent tools are visible again for normal chat turns while a safer routing strategy is reconsidered later.
- Latest local code work: pending commit `fix: use profile-backed interest meme search`: Changes daily meme/video discovery away from raw hot-search news defaults. Weibo now defaults to the dedicated persistent profile at `~/.cow-meme-harvester/weibo-browser-profile` and only uses HTTP fallback when explicitly enabled. Weibo and Xiaohongshu default to interest-seeded searches, Xiaohongshu handles extensionless XHS note image URLs and repairs corrupted legacy search-filter config, Douyin defaults to interest-seeded profile search, and `/取消` now preserves queued user messages while `/跳过` remains the queue-clearing control. Runtime skill copies and local ignored harvester configs were synced.
- Latest local code work: pending commit `fix: keep meme harvester on platform profiles`: Tightens `daily-meme-harvester` skill routing instructions so Agent diagnostics do not use CowAgent's generic `browser` tool or Bing/Baidu fallback for Xiaohongshu/Weibo, because that browser uses `~/.cow/browser_profile` rather than the skill-owned platform profiles and can falsely show a login wall. The skill now directs diagnostics to `harvest_memes.py` dry-runs and the explicit `--open-xiaohongshu-profile` / `--open-weibo-profile` manual-login commands only.
- Latest local code work: pending commit `fix: stabilize xiaohongshu meme profile`: Updates `daily-meme-harvester` so Xiaohongshu uses a dedicated persistent Chrome profile under `~/.cow-meme-harvester/xiaohongshu-browser-profile`, opens that profile in normal system Chrome for one-time manual login/risk verification, collects through normal Chrome + local CDP by default, and keeps HTTP as a bounded fallback instead of a long-running path. Xiaohongshu search now applies recent image-note filters (`sort_type=time_descending`, `note_type=普通笔记`, `time_filter=一天内`). Weibo now has its own dedicated manual-login profile under `~/.cow-meme-harvester/weibo-browser-profile`, plus bounded HTTP search terms/suffixes/request timeout/overall budget so it cannot drag Agent runs for minutes. Dry-run skips proxy guard mutation, XHS-only dry-run avoids hidden Weibo hot-term fetches, image URL parsing no longer treats static/avatar resources as content images, and legacy configs automatically inherit new serious accident/disaster block keywords. Runtime skill copy synced.
- Latest local code work: pending commit `fix: keep douyin harvester on skill profile`: Keeps `daily-douyin-video-harvester` on its skill-owned persistent Chrome profile under `~/.cow-douyin-video-harvester/browser-profile`, fixes the blank/missing profile fallback that incorrectly pointed to Codex's profile, treats `DOUYIN_COOKIE` as optional when persistent browser mode is active, adds `DOUYIN_BROWSER_USER_DATA_DIR` as an explicit override, syncs the runtime skill copy, validates a one-time local profile repair with a real dry-run, and confirms `daily-meme-harvester` has no browser-profile selector for Weibo/Xiaohongshu.
- Latest local code work: pending commit `fix: surface a2e manual verification`: Fixes `edit(oldText="")` so append-to-missing memory files creates the file instead of failing, updates the A2E helper to return user-notifiable `ManualActionRequired` / `FailureNotification` JSON for manual verification or automation failures, closes verified A2E browser windows more reliably, and syncs the runtime A2E skill copy.
- Latest local code work: pending commit `feat: add natural backend switch aliases` lets `cow_cli` intercept high-confidence natural-language backend switch/status requests before Agent reasoning and route them to the existing `/backend` command path. Informational questions such as "如何切换到 CAPI 后端" intentionally continue to Agent instead of switching.
- Latest local code work: pending commit `fix: avoid reasoning policy circular import` moves the optimizer-only `LLMRequest` import out of `common.reasoning_effort_policy` module import time so standalone local policy checks work, and updates the AgentBridge policy metadata test for the current onboarding hook signature.
- Latest local code work: pending commit `fix: notify missed scheduler runs` starts the normal CowWechat scheduler during app startup, changes overdue scheduled runs from silent skip to user-visible missed-run notices, records failed/invalid scheduled task state, routes scheduler delivery through result-aware WeCom active sends, and adds `run_now` / `skip_pending` scheduler actions so users can choose whether to补跑 or skip a missed task.
- Latest local code work: pending commit `feat: isolate wecom group memory` makes WeCom Bot private chats use per-user Agent identity while group chats use `wecom_bot:group:<chatid>` for shared group conversation and memory. Group messages now carry sender metadata into the Agent prompt, missing group `chatid` messages are dropped before context/memory creation, and each group member gets an independent first-touch welcome state under the group memory directory.
- Latest local code work: pending commit `feat: enrich reasoning effort policy telemetry` enables the local reasoning-effort policy for all users by default, propagates group/private scope into policy routing, separates group/private decision logs while keeping optimizer analysis unified, records optimizer attempts with model/effort/status/failure reason, and appends sanitized task outcome records with final turn count, max-turn exhaustion, and tool-attempt counters.
- Latest local code work: pending commit `fix: make reasoning effort policy local only` removes runtime model-decision routing and the benchmark script, keeps admin-only task-start local reasoning effort rules, defaults uncertain tasks to `xhigh`, and limits future optimizer recommendations to local rule changes.
- Previous local code work: `52398f0` `fix: adapt codex reasoning benchmark` switched the runtime backend state to Codex for testing, fixed locked empty effort handling, and omitted Codex-unsupported `max_output_tokens` and `prompt_cache_retention`.
- Previous local code work: `39d94f2` `feat: add reasoning effort policy routing` added admin-only task-start reasoning effort routing, sanitized decision audit logs, and a background recommendation-only optimizer; latest work supersedes the earlier model-decision path with local-only routing.
- Previous local code work: pending commit `fix: harden admin privacy boundaries` scopes Web memory/dream APIs to the single configured admin profile, scopes Web scheduler output to the admin's own tasks, replaces arbitrary `/api/file?path=` reads with issued tokens plus narrow safe fallback roots, and adds a normal-user bash backstop that restores security policy files if a command tampers with them.
- Previous local code work: pending commit `fix: schedule daily deep dream at midnight` moves automatic Deep Dream out of lazy Agent initialization into an app-level daily scheduler, runs at `00:00` against the previous day's daily memory, and catches up yesterday on restart using persisted per-scope state.
- Previous local code work: `3624f48` `feat: add codex llm backend router` adds a unified `capi`/`codex` LLM backend router, a CodexBot backend using the existing Codex login, daily quota-based auto-switch state, `/backend` commands, web config status, and a `codex-quota-query` skill synced to the runtime skill directory.
- Latest Codex backend work: CodexBot reuses local Codex auth JSON without printing or copying tokens, streams ChatGPT Codex SSE responses, sends Agent tool definitions to Codex through the Responses tool-call adapter, records prompt-cache usage under the `codex` wire label, and defaults Codex reasoning effort to `xhigh`.
- Latest code baseline commit: current commit `feat: optimize cache tool attempts`.
- Latest validation: `tests.test_fast_lane_progress` passed with 14 tests; source `py_compile` passed for touched bridge/channel/runtime files; safe GitHub upload preflight passed before staging.
- Previous local code work: safe GitHub upload skill for WeChat-driven code publishing.
- Latest committed skill sync: `631f159` `feat: sync local cow skills`
- Latest merged code work: `852e909` `feat: add wechat quick progress lane`; `c309e61` `fix: make weixin onboarding greeting deterministic`
- GitHub upload target: new private repository `while4234/CowWeCom` once created.
- Remote layout: `origin` points to `https://github.com/while4234/CowWeCom.git` only; old `while4234/CowWechat` and `upstream` are no longer configured as remotes.
- Working tree: local ignored runtime files such as `config.json`, logs, and `knowledge_backend/indexes/kb.sqlite` remain on disk but are not tracked/staged.
- Validation: skill display-name regression tests passed; skill manager/test py_compile passed; `node --check channel\web\static\js\console.js` passed; runtime skill config now returns `企业微信 CLI` and `哔哩哔哩`; safe upload preflight passed before staging. Broader prior validation still includes social bridge natural-tone and private knowledge-boundary tests, related Weixin/multi-user/progress regressions, skill script py_compile checks, and web console `node --check`.

## Change Log

- `2026-05-24` pending commit `fix: recover interrupted image generation jobs`: Fixed silent image-generation stalls when CowAgent restarts while a background job is queued/running. Jobs now write a safe `job_state.json`, startup recovery sends either the generated image or a visible restart-interrupted failure, completed same-args jobs no longer suppress fresh requests, and Weixin/WeCom sends return delivery status so generated-but-undelivered images are retried on recovery. Validation: source/test `py_compile` passed; `pytest tests/test_image_generation_background_jobs.py tests/test_app_scheduler_startup.py tests/test_wecom_social_bridge.py tests/test_multi_weixin_instances.py -q` passed with 59 tests.
- `2026-05-24` pending commit `fix: loosen douyin adult-oriented filters`: Updated `daily-douyin-video-harvester` to stop over-filtering normal Douyin-native adult-oriented wording. Default fallback searches now include `擦边舞蹈` and `性感热舞`; scoring treats `性感`, `热辣`, `身材`, `御姐`, and `姐感` as positive signals; and broad hard downranking for `裸露`, `露骨`, `色情`, and `成人` was removed. Clear boundary filters for `约炮`, `成人视频`, `未成年`, and `nsfw` remain. Updated docs/tests and synced the runtime copy under `C:\Users\RondleLiu\cow\skills\daily-douyin-video-harvester`. Validation: source/test `py_compile` passed; `tests/test_daily_douyin_video_harvester.py` passed with 37 tests; real `--dry-run --json --max-total 1 --max-candidates 5 --since-hours 24` returned 1 fresh candidate matching the looser adult-oriented search direction.
- `2026-05-24` pending commit `fix: restore running-task queue notices`: Split queue-notice throttling from visible-output/progress tracking in `SessionRuntime`, so recent Agent output or `/状态` checks no longer suppress the first queued-task reminder for a new normal message. Added a regression test that starts a running task, records recent visible output, queues another normal message, and confirms the user still receives the "已排队" notice while `/状态` reports the pending queue count. Validation: source/test `py_compile` passed; `tests/test_fast_lane_progress.py` passed with 17 tests.
- `2026-05-24` pending commit `fix: filter and dedupe douyin searches`: Updated `daily-douyin-video-harvester` search behavior so default and legacy-default configs use a 24-hour freshness window, Douyin search URLs include platform freshness filters (`publish_time=1` for today/24h, broader filters for diagnostic windows), and search-term dedupe ignores whitespace differences. HTTP searches that fail or return no usable candidates are remembered for the current run, so the browser fallback no longer retries the same normalized term and instead moves on to remaining interest seeds. Updated skill docs/tests and synced the runtime copy under `C:\Users\RondleLiu\cow\skills\daily-douyin-video-harvester`. Validation: source/test `py_compile` passed; `tests/test_daily_douyin_video_harvester.py` passed with 35 tests; real `--dry-run --json --max-total 1 --max-candidates 5 --since-hours 24` returned 1 candidate and confirmed the one-day filter in collected Douyin search requests; safe upload preflight passed before staging.
- `2026-05-24` pending amend `revert: skip tools for plain text chat`: Reverted the pure-text no-tools path added in `98740de5`. The heuristic was too broad for short follow-up commands and removed image-generation/tool schemas from requests that depended on prior context, causing the bot to claim no image tool was available. The rollback deletes `tests/test_agent_plain_text_mode.py`, removes the AgentBridge plain-text classifier, restores normal auto knowledge retrieval, and removes `agent_plain_text_no_tools_enabled` from the template. Validation pending after amend.
- `2026-05-24` pending commit `fix: use profile-backed interest meme search`: Fixed FastLane `/取消` so it cancels only the running task and keeps pending session messages for later dequeue; `/跳过` still clears the queue. Updated `daily-meme-harvester` so Weibo defaults to persistent-profile browser collection, disables silent HTTP fallback by default, filters parseable Weibo candidates older than `since_hours`, and uses interest seeds instead of raw hot-search news. Updated Xiaohongshu defaults to interest seeds, repaired legacy `????` search-filter config at runtime, increased browser query breadth, and accepts modern extensionless `xhscdn.com` note image URLs. Updated `daily-douyin-video-harvester` defaults to interest-seeded profile search with platform-safe light-suggestive/funny seeds and explicit-content downranking. Synced runtime skill copies and refreshed ignored local harvester configs. Validation: source `py_compile` passed; focused pytest passed with 85 tests; real dry-runs returned Weibo and Xiaohongshu candidates through their dedicated profiles; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: keep meme harvester on platform profiles`: Added a prominent Agent routing guard to `daily-meme-harvester` so Xiaohongshu/Weibo diagnostics do not use CowAgent's generic browser profile or public search-engine fallback after thin dry-run results. The guard explains that `~/.cow/browser_profile` is separate from the skill-owned `~/.cow-meme-harvester/*-browser-profile` directories and that generic-browser login walls do not prove the dedicated profile lost login state.
- `2026-05-24` pending commit `fix: stabilize xiaohongshu meme profile`: Changed `daily-meme-harvester` Xiaohongshu collection from unbounded HTTP-first behavior to a dedicated persistent browser profile at `~/.cow-meme-harvester/xiaohongshu-browser-profile`, opened by normal system Chrome for manual login/risk verification and collected through short-lived local CDP by default. Added bounded HTTP fallback limits, dry-run proxy-guard suppression, XHS-only dry-run fallback keywords without hidden Weibo hot-search fetches, legacy config migration for old 12-query/20-second XHS defaults, profile override via `XHS_BROWSER_USER_DATA_DIR`, risk-verification text detection, stricter image URL parsing so static/avatar resources are not treated as content images, and default XHS search filters for recent image notes (`sort_type=time_descending`, `note_type=普通笔记`, `time_filter=一天内`). Added a dedicated Weibo manual-login profile at `~/.cow-meme-harvester/weibo-browser-profile` with `WEIBO_BROWSER_USER_DATA_DIR` and `--open-weibo-profile`, bounded Weibo HTTP search by search terms, suffixes, request timeout, and total time budget, and made legacy configs inherit the expanded serious accident/disaster block keywords so tragedy-news images are not selected as daily memes. Updated docs/tests and synced runtime copy under `C:\Users\RondleLiu\cow\skills\daily-meme-harvester`. Validation: source/test `py_compile` passed; `tests/test_daily_meme_harvester.py` passed with 34 tests; safe GitHub upload preflight passed before staging. Opened the dedicated Xiaohongshu and Weibo profiles once in system Chrome for user login/verification.
- `2026-05-24` pending commit `fix: keep douyin harvester on skill profile`: Updated `daily-douyin-video-harvester` so its default and blank-profile fallback both use the skill-owned persistent Chrome profile under `~/.cow-douyin-video-harvester/browser-profile`, while `DOUYIN_BROWSER_USER_DATA_DIR` can explicitly override the profile when needed. Persistent-browser mode now treats `DOUYIN_COOKIE` as optional instead of warning when the profile is expected to carry session state. Updated skill docs/tests and synced the runtime copy under `C:\Users\RondleLiu\cow\skills\daily-douyin-video-harvester`. Per user instruction, the temporary Codex-profile copy was used only as a one-time local repair and was not added to skill logic or Git. Also checked `daily-meme-harvester`: Weibo/Xiaohongshu use only `WEIBO_COOKIE` / `XHS_COOKIE` from the environment and do not select a browser profile; repo/runtime copies match. Validation: source/test `py_compile` passed; `tests/test_daily_douyin_video_harvester.py` passed with 30 tests; real `--dry-run --json --max-total 3` used the repaired skill profile, discovered 15 meme-worthy hot-board terms, selected 0 videos under the default freshness/quality gate, and did not hit a login wall or cookie warning.
- `2026-05-24` pending commit `fix: surface a2e manual verification`: Fixed the Agent `edit` tool append mode so `oldText=""` creates missing memory files and marks memory dirty instead of returning `File not found`, which prevented daily memory appends from becoming a final-turn tool error. Updated `a2e-daily-checkin` so the helper falls back to A2E tokens when the Chrome LevelDB record lacks the account email, returns `ManualActionRequired.NeedsNotification` when a clicked claim cannot be API-verified or needs human verification, returns `FailureNotification.NeedsNotification` for pre-browser failures, leaves manual-verification pages open, and sweeps verified A2E Chrome windows closed after successful claims. Synced the runtime A2E skill copy under `C:\Users\RondleLiu\cow\skills\a2e-daily-checkin`. Validation: PowerShell parser check passed; source/test `py_compile` passed; `tests/test_knowledge_guard.py` and `tests/test_a2e_daily_checkin_skill.py` passed with 11 tests.
- `2026-05-24` pending commit `feat: add natural backend switch aliases`: Added a precision-first `cow_cli` natural-language backend parser so phrases like "帮我切换到 CAPI 后端" and "现在后端状态是什么" execute through the existing `/backend` path before Agent reasoning, while informational/negative phrases keep flowing to Agent. Added parser, dispatch, and event-interception tests. Validation: `tests.test_cow_cli_backend_nl` passed with 9 tests; `tests.test_codex_backend_router` passed with 7 tests; touched source `py_compile` passed; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: avoid reasoning policy circular import`: During CAPI effort-routing validation, a direct `common.reasoning_effort_policy` import exposed a circular import through `agent.protocol`. Moved the optimizer-only `LLMRequest` import into `run_policy_optimizer_once` and updated the AgentBridge metadata test stub for the current onboarding hook signature. Validation: source `py_compile` passed; `tests.test_reasoning_effort_policy` and `tests.test_llm_usage_tracker` passed with 25 tests. Additional CAPI smoke: local routing produced `medium` for simple IM and `xhigh` for coding on `gpt-5.5`; Responses payloads carried `reasoning.effort=medium/xhigh`; real CAPI calls to `gpt-5.5` accepted both `medium` and `xhigh` and returned `OK`.
- `2026-05-24` pending commit `fix: make cli restart exit cleanly`: Reworked `cli/commands/process.py` so plain `cow restart` / `.venv\Scripts\python.exe -m cli.cli restart` stops and starts CowAgent then exits instead of tailing logs forever. Interactive log following moved to `--logs`, Windows termination now uses `taskkill /T`, stop checks that processes actually exit before removing `.cow.pid`, stale PID recovery scans exact current-project `app.py` processes, and a lightweight process lock prevents overlapping start/stop/restart commands. Validation: process source `py_compile` passed; `tests/test_process_commands.py` and scheduler startup/regression suites passed with 27 focused tests; real restart smoke passed twice and `/chat` returned HTTP 200.
- `2026-05-24` current commit `fix: reload clash verge runtime rules`: Updated `scripts/clash_verge_rule_guard.py` so one normal run patches both the active subscription profile and Clash Verge's generated runtime `clash-verge.yaml`, normalizes required rule indentation to the existing `rules:` list style, and calls the Mihomo/Clash external-controller reload API after runtime config changes. Added `--runtime-profile`, `--controller`, `--no-runtime-apply`, `--no-reload`, and `--reload-always`. Validation: script/test `py_compile` passed; `tests.test_clash_verge_rule_guard` passed with 12 tests; real guard run fixed generated runtime config and `core reloaded`; real browser checks loaded Douyin (`https://www.douyin.com/jingxuan`) and Xiaohongshu (`https://www.xiaohongshu.com/explore`).
- `2026-05-24` pending commit `fix: notify missed scheduler runs`: Starts the normal CowWechat scheduler from `app.run()` after channels start instead of waiting for the first Agent message, so daily jobs survive process restarts. Scheduler runs that are overdue beyond the grace window now record `last_missed_run_at`, notify the original task receiver, and leave the user to decide whether to `run_now` or `skip_pending`; invalid schedules are disabled with a notice instead of failing silently. Execution callbacks now return success/failure, WeCom scheduler delivery uses result-aware proactive send, failed runs send a failure notice, and scheduler tools gained owner-scoped `run_now` / `skip_pending` actions. Validation: source `py_compile` passed; `tests/test_app_scheduler_startup.py`, `tests/test_scheduler_service.py`, `tests/test_scheduler_execution_identity.py`, `tests/test_scheduler_duplicates.py`, `tests/test_web_admin_privacy.py`, and `tests/test_wecom_social_bridge.py` passed with 38 focused tests.
- `2026-05-24` pending commit `feat: isolate wecom group memory`: Updated WeCom Bot identity derivation so private chats keep `wecom_bot:<user_id>` while group chats share `wecom_bot:group:<chatid>` for `conversation_id` and group-level `memory_user_id`. Added group sender metadata and prompt prefixing so group memory can track who spoke, dropped group callbacks without `chatid`, and added per-group-member onboarding files under `memory/users/<group_memory_user_id>/members/<sender_slug>/USER.md` with a lightweight welcome that asks only how to address the member. Validation: source `py_compile` passed; `tests/test_wecom_social_bridge.py`, `tests/test_agent_onboarding_welcome.py`, and `tests/test_multi_user_isolation.py` passed with 36 tests; safe upload preflight passed before staging.
- `2026-05-24` pending commit `feat: enrich reasoning effort policy telemetry`: Enabled reasoning-effort policy defaults for all users instead of admin-only, propagated `isgroup` into Agent model metadata, split sanitized policy audit logs into private and group JSONL files, and kept optimizer reads unified across private/group/legacy logs. Added decision features useful for future local-rule optimization, optimizer attempt JSONL records with model/effort/success/failure reason, task outcome records keyed by `task_id` with final turn count, max-turn exhaustion, status, final response hash/size, and tool-attempt counters, plus usage metadata for `reasoning_effort_chat_scope`. Validation: source `py_compile` passed; `tests.test_reasoning_effort_policy` passed with 17 tests; `tests.test_llm_usage_tracker` passed with 8 tests; safe upload preflight passed before staging.
- `2026-05-24` pending commit `fix: make reasoning effort policy local only`: Removed runtime model-decision routing, model benchmark tooling, and classifier config keys from the reasoning effort policy. The policy now uses local rules only: obvious simple IM/short rewrite/translation/explanation gets `medium`, obvious coding/debugging/repo/tool/high-risk/quality-first work gets `xhigh`, and uncertain tasks conservatively default to `xhigh`. Audit and usage telemetry now record only local rule metadata, and the recommendation-only optimizer prompt focuses on adding or tightening local rules rather than model selection. Runtime ignored `config.json` was also cleaned of obsolete classifier keys.
- `2026-05-24` pending commit `fix: adapt codex reasoning benchmark`: Switched ignored runtime backend state to Codex for benchmark testing and fixed Codex classifier-call compatibility. `AgentLLMModel` now forwards the `reasoning_effort_locked` marker, OpenAI-compatible and Codex backends honor locked empty effort without falling back to configured `xhigh`, CodexBot no longer sends unsupported `max_output_tokens` or `prompt_cache_retention`, and Codex mini ranking now prefers numeric recency so `gpt-5.4-mini` is tested before `gpt-5.1-codex-mini`. Validation: `tests.test_codex_direct_backend` and `tests.test_reasoning_effort_policy` passed with 22 tests; Codex benchmark with 6 balanced samples and 700ms threshold tested `gpt-5.4-mini`, `gpt-5.5/none`, and `gpt-5.5/low`. All classified correctly with no false-medium errors, but all exceeded the 700ms threshold, so no classifier config was selected. CowAgent restarted on Codex backend and `/chat` returned HTTP 200.
- `2026-05-24` pending commit `feat: add reasoning effort policy routing`: Added a disabled-by-default, admin-only reasoning effort policy that decides once per Agent task, keeps the selected effort sticky across later LLM calls, supports high-confidence local `medium`/`xhigh` rules, calls one configured same-backend classifier for uncertain tasks, and falls back to `xhigh` on classifier absence, backend mismatch, timeout, invalid output, or errors. Added sanitized `reasoning_effort_policy_decisions.jsonl` audit logging, safe usage metadata fields, a recommendation-only background optimizer triggered after a configurable number of decisions, and `scripts/benchmark_reasoning_effort_policy.py` for choosing exactly one classifier. Validation: focused py_compile passed; `tests.test_reasoning_effort_policy`, `tests.test_llm_usage_tracker`, `tests.test_openai_responses_api`, and `tests.test_codex_backend_router` passed. Benchmark smoke on current CAPI backend with 6 balanced samples and 700ms threshold tested `gpt-5.4-mini`, `gpt-5.5/none`, and `gpt-5.5/low`; all exceeded the threshold and returned no selected classifier recommendation.
- `2026-05-24` pending commit `fix: harden admin privacy boundaries`: Added a single-admin profile resolver for non-chat admin surfaces, made Web memory/dream APIs read only `memory/users/<admin_memory_user_id>/...`, made Web scheduler output use owner-scoped `TaskStore.list_tasks_for_owner`, and narrowed `/api/file` so new file-send events use expiring server-issued tokens while legacy path fallback only serves upload/tmp/download/attachment/browser-download roots or `workspace/users/*/files`. Kept normal-user bash enabled for skill/helper UX, but added a post-execution integrity backstop that restores core security policy files and fails the tool result if normal-user bash tampers with them. Validation: source `py_compile` passed; `tests.test_web_admin_privacy`, `tests.test_multi_user_isolation`, `tests.test_scheduler_duplicates`, `tests.test_memory_deep_dream_scheduler`, and `tests.test_scheduler_execution_identity` passed.
- `2026-05-24` pending commit `fix: schedule daily deep dream at midnight`: Added an app-level `daily_dream_scheduler` started from `app.run()`, with startup catch-up for yesterday, idempotent per-scope state under the agent workspace memory directory, active-agent daily flush before dreaming, shared/user memory scope discovery, and date-anchored `MemoryFlushManager.deep_dream(...)` so midnight runs process the previous day rather than today's partial file. Updated config defaults/templates for `memory_deep_dream` and added focused scheduler/date-anchor/app-startup tests. Validation: source `py_compile` passed; `tests.test_memory_deep_dream_scheduler` passed with 6 tests; memory/multi-user/tool-attempt regressions passed with 26 tests; Codex backend regressions passed with 15 tests.
- `2026-05-24` pending commit `feat: add codex llm backend router`: Added a persistent `llm_backend` router state under ignored `data/llm-backend-router/`, CodexBot as a first-class non-image LLM backend using existing Codex auth, Agent/Web/Bridge/cow_cli routing through the effective backend, daily 00:00 quota evaluation with fair-share/remaining/manual-override/latch policy, `/backend` and quota aliases, sanitized web backend status, and the `codex-quota-query` skill copied to `C:\Users\RondleLiu\cow\skills\codex-quota-query`. Codex tool calls stay on Codex, not CAPI, and Codex prompt-cache telemetry uses `wire_api=codex` with default reasoning effort `xhigh`. Validation: source `py_compile` passed for touched router/backend/web/skill files; `tests.test_codex_backend_router`, `tests.test_codex_quota_query_skill`, and `tests.test_codex_direct_backend` passed with 18 tests; image-generation Codex-auth and OpenAI Responses regressions passed with 17 tests; skill quick validation passed for repo and runtime copies; `node --check` passed for web console JS and the quota Node script.
- `2026-05-23` pending commit `feat: add social hot meme harvesters`: Updated `daily-meme-harvester` for hot-topic-driven Weibo + Xiaohongshu image harvesting, per-provider TOP3 selection, cross-provider topic dedupe without backfill, same-post image dedupe, XHS proxy-rule guard retry, and optional WeCom image sending. Added `daily-douyin-video-harvester` as a separate Douyin TOP3 hot-video skill with persistent-browser hot-board discovery, 48-hour freshness filtering, one-video-per-hot-term diversity, meme-worthiness scoring, sharp commentary, atomic video/cover downloads, WeCom sending, dedupe state, and 24-hour cleanup scheduling. Validation: meme and Douyin scripts passed `py_compile`; `tests.test_daily_meme_harvester` and `tests.test_daily_douyin_video_harvester` passed; real Weibo/Xiaohongshu image-send smoke downloaded and sent 3 Weibo images to the admin private chat, while Xiaohongshu produced no downloadable candidates in that run; Douyin persistent-browser dry-run discovered current hot-board terms and intentionally returned 0 candidates under the 48-hour freshness/quality gate when only stale/generic candidates were available, while a 7-day diagnostic window found an older `峰哥` candidate that the default gate correctly skipped.
- `2026-05-23` current commit `feat: add clash verge rule guard`: Added `scripts/clash_verge_rule_guard.py`, a no-dependency helper that resolves the active Clash Verge Rev profile, inserts Xiaohongshu/Douyin `DIRECT` rules at the top of `rules:`, supports `--watch` polling after subscription file changes, and avoids printing proxy node contents. Added focused unit coverage for insertion order, duplicate prevention, dry-run behavior, current profile resolution, JSON output shape, and preserving normal UTF-8 files without adding a BOM. Validation: script/test `py_compile` passed; `tests.test_clash_verge_rule_guard` passed with 6 tests; real active Clash profile dry-run reported all required rules already present.
- `2026-05-23` pending commit `fix: deploy weibo-only meme harvester`: Removed X/Twitter from the deployed default path for `daily-meme-harvester`, set the default and persisted local config to Weibo-only TOP3 with a larger candidate pool for dedupe resilience, kept output path configuration, and synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\daily-meme-harvester`. Validation: CowAgent restart and `/chat` healthcheck passed; skill quick validation passed; script `py_compile` passed; `pytest tests/test_daily_meme_harvester.py` passed with 10 tests; fresh real Weibo TOP3 smoke downloaded 3 images and generated `manifest.jsonl`, `index.md`, and state files.
- `2026-05-23` `e936f6c` `fix: default shopping comparison links`: Changed `shopping-lite-compare` so manual product links and copy commands are returned by default via the local `taobao`/`maishou` detail path while still forbidding automatic opens, clicks, carts, orders, sensitive form filling, or automatic rebate-link use. Added explicit Pinduoduo platform handling and kept `--no-links` for cases where the user explicitly asks not to include links. Synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\shopping-lite-compare`. Validation: skill quick validation passed; helper `py_compile` passed; default-link Taobao smoke, `--no-links` Taobao smoke, and Pinduoduo link smoke passed without opening a browser.
- `2026-05-23` pending commit `feat: add daily meme harvester skill`: Added `daily-meme-harvester` with `harvest_memes.py`, configurable output path priority, Weibo hot-search image collection, official X API collection, conservative Xiaohongshu public-page/user-cookie collection, optional Reddit support, sensitive keyword/provider filtering, URL and SHA-256 dedupe state, atomic image writes, `manifest.jsonl`, `index.md`, and `state_delta.json`. Synced the runtime skill copy to `C:\Users\RondleLiu\cow\skills\daily-meme-harvester`. Validation: skill quick validation passed; `py_compile` passed; `pytest tests/test_daily_meme_harvester.py` passed with 9 tests; `--help` passed; dry-run JSON passed. Real TOP3 smoke for `weibo,xiaohongshu,x` created output/state files but downloaded 0 images because Weibo search returned access-limited/invalid public responses, Xiaohongshu public pages failed SSL access without `XHS_COOKIE`, and `X_BEARER_TOKEN` was not set.
- `2026-05-23` `6f0143b` `fix: constrain lite commerce skills`: Updated `takeout-lite-recommender` to allow displaying `meituan-coupons` returned `jump_url` links for manual user tapping while forbidding automatic opens/clicks, and hardened `shopping-lite-compare` so it must use the local `taobao`/`maishou` read-only search helper rather than browser/Baidu/public-web fallback. Added `shopping_compare_helper.py`, which locates the installed OpenClaw `taobao` script and calls its `search` path only. Validation: skill quick validation passed; py_compile passed for takeout and shopping helper scripts; helper returned a read-only Apple 20W charger comparison without opening a browser; safe upload preflight and staged diff checks passed. Restarted CowAgent after push; service running as PID `39676`.
- `2026-05-23` pending commit `fix: repair skill display names`: Added `display-name` frontmatter to `wecom-cli` and `bilibili-all-in-one`, taught `SkillManager` to replace persisted `????` placeholder display names from frontmatter while preserving valid manual labels, kept skill card titles after enable/disable toggles, synced the two runtime `SKILL.md` files, and refreshed `C:\Users\RondleLiu\cow\skills\skills_config.json` so the web console shows `企业微信 CLI` and `哔哩哔哩`. Validation: `tests.test_skill_display_names` passed; `py_compile` passed for `agent\skills\manager.py` and the new test; `node --check channel\web\static\js\console.js` passed; safe upload preflight passed before staging.
- `2026-05-23` pending commit `feat: add long task expectation notices`: Added per-session visible-output tracking, silence-based long-task notices, `/状态` visibility resets, friendly Agent error fallback text, max-step pre-summary notices via existing `turn_end` events, config defaults for long-task expectation thresholds, and focused fast-lane progress tests. Validation: `tests.test_fast_lane_progress` passed with 14 tests; touched source files passed `py_compile`; safe upload preflight passed before staging.
- `2026-05-23` current commit `feat: optimize cache tool attempts`: Added safe tool-attempt memory for repeated non-retryable failures, request-local read-only tool result reuse, request-copy compaction for older oversized tool results, and cache telemetry/report counters for tool attempts, skips, duplicate successes, memory rule hits, and compacted results. Validation: focused tool-attempt, prompt-cache, Responses API, knowledge-context, scheduler duplicate, and CowAgent self-evolution pytest suites passed; safe upload preflight passed before staging.
- `2026-05-23` current commit `feat: add community skill discovery guards`: Added localized `find-skills-skill`, `skill-guard`, `quick-weather`, and `fast-market-price` CowWechat skills; synced the community search workflow from ClawHub's popular `find-skills-skill`, added a local pre-install static security gate based on `skill-guard`, and extracted qq_openclaw-style Open-Meteo weather plus Coinbase/Yahoo quick quote scripts. Validation: quick validation passed for all four skills; scripts passed `py_compile`; `find_skills.py` returned known ClawHub leads plus live `npx clawhub search`; `quick_weather.py "明天成都天气"` returned an Open-Meteo forecast; `fast_price.py "比特币"` returned Coinbase BTC/USD; `fast_price.py "黄金"` returned Yahoo Finance `GC=F`; `skill-guard` passed on clean skills and blocked a malicious temp skill with prompt injection and token upload text.
- `2026-05-23` current commit `feat: add takeout and shopping lite skills`: Added local `takeout-lite-recommender` and `shopping-lite-compare` skills, synced their runtime copies, installed the requested OpenClaw ClawHub skills (`eat-what-today-skill`, `meituan-coupons`, `taobao`), and documented that the installed `taobao` package is a `maishou` skill with `maishou88.com` requests, a default invite code, hardcoded openid, possible commission fields, and detail/share purchase-link generation. Validation: both skill quick validations passed with `PYTHONUTF8=1`; `takeout_lite.py` py_compile passed; takeout sample output included 3 recommendations, reasons, budget ranges, Meituan search keywords, coupon reminder, and compliance notice; routing/disclosure smoke passed. Per user instruction, this local integration is not pushed to `origin`.
- `2026-05-23` current commit `docs: document wecom bot auth source`: Added `wecom_bot_auth_source` to defaults and safe templates, and updated WeCom Bot setup docs/quickstart to explain QR creation source fallback to manual Bot ID/Secret configuration. Validation: `py_compile config.py` passed; `node --check channel\web\static\js\console.js` passed; safe upload preflight passed.
- `2026-05-23` current commit `chore: add requested cow skills`: Installed `bdpan-storage`, `bilibili-all-in-one`, `frontend-design`, and `wecom-cli` from the requested sources, synced runtime copies into `skills/`, normalized upstream frontmatter where needed for CowWechat validation/loading, and intentionally skipped `plugin-amap` because its package hard-requires LinkAI plugin execution through `LINKAI_API_KEY`. Validation: quick validation passed for runtime and repo copies; SkillManager loaded all four installed skills; Bilibili import smoke passed; safe upload preflight passed.
- `2026-05-23` current commit `feat: add cache shape telemetry`: Added safe request-shape hashes/counts to LLM usage telemetry, grouped cache reports by `request_kind`, surfaced long-input zero-cache metrics, stabilized retrieved knowledge snippets as compact JSON lines, and lightly extended the cache dashboard recent table. Validation: focused prompt-cache/Responses/knowledge-context pytest passed with 19 tests; `git diff --check` passed with CRLF warnings only; safe upload preflight passed before staging.
- `2026-05-23` `b43fd74` `fix: make github skill use local credentials`: Updated the GitHub skill so it no longer requires `jq` or a shell-exported `GITHUB_TOKEN`; added a helper that resolves `GITHUB_TOKEN`, `GH_TOKEN`, or Git Credential Manager credentials and calls the GitHub REST API through `curl.exe`; removed the unsafe token-in-remote guidance; synced the runtime skill copy. Validation: skill quick validation passed; helper resolved a local token and authenticated `/user` as `while4234`; safe upload preflight passed.
- `2026-05-23` current commit `feat: add playwright browser control skill`: Added the local persistent Playwright browser-control skill to the project skills directory, included its agent metadata and setup notes, documented it in the skills index, and synced the deployed workspace copy. Validation: skill quick validation and safe upload preflight passed.
- `2026-05-23` current commit `fix: prefer manual wecom bot setup`: Changed the WeCom bot console flow to default to manual Bot ID/Secret entry and replaced QR auth creation with a warning that it grants creator data permissions, causing non-creators to see "仅限创建者本人可使用". Validation: `node --check channel\web\static\js\console.js` passed; Web restart smoke returned 200; safe upload preflight passed.
- `2026-05-23` current commit `fix: isolate per-user onboarding`: Changed onboarding detection to fall back to per-user `USER.md` for resolved chat profiles, auto-create a safe placeholder user file for new actors, and keep initialized users from seeing the first-run welcome repeatedly. Validation: agent onboarding, WeCom social bridge, and multi-user isolation unittest suites passed; py_compile passed for touched files; safe upload preflight passed.
- `2026-05-23` `a5c4062` `chore: prepare cowwecom github baseline`: Changed safe templates and Docker compose defaults to `wecom_bot`, added `COWWECOM_QUICKSTART.md`, removed generated knowledge sqlite/PDF/derived/report artifacts from Git tracking while keeping local files ignored, reset the knowledge manifest to an empty safe baseline, and updated safe upload preflight/tests to allow staged deletion of protected runtime files. Validation: staged preflight OK; focused pytest passed with 82 total tests; py_compile passed for touched skill scripts.
- `2026-05-23` current commit `feat: add a2e daily checkin skill`: Added project-local A2E check-in skill, bundled the existing PowerShell browser/profile helper, added a safe example state while ignoring the real local state file, synced the deployed workspace copy, added focused SkillManager discovery tests, and added hard project rules requiring local Git for all projects and automatic GitHub push for this CowWechat repository. Validation: A2E skill pytest passed; skill quick validation passed; PowerShell parser check passed; safe upload preflight passed.
- `2026-05-22` `feat: add safe github upload skill`: Added `safe-github-upload` with a preflight guard for staged protected files and staged secret-like content, documented origin-only upload workflow, updated `skill-creator` so CowWechat skill development must sync both `$HOME\cow\skills` and `skills/`, deployed the updated skills to the workspace copy, and added focused discovery/preflight tests. Validation: safe upload and skill-creator quick validation passed; preflight script py_compile passed; related pytest passed with 8 tests.
- `2026-05-22` `fix: harden social bridge relay delivery`: Made bridge target resolution fall back to the current LLM when deterministic IDs/names/relationship aliases do not match; added spouse aliases; fixed user display fallback for `鐜夌帀 / Rikoo032700`; stopped empty-context Weixin public ID inference; made Weixin text sends check API `ret` and treat send timeouts as failures rather than delivered messages; added local CowAgent skill/self-evolution files. Validation: focused social bridge/multi-Weixin/tool tests passed with 48 tests, related py_compile passed, service restart smoke returned `/chat` 200, and a direct non-send probe resolved `鑰佸﹩`/`閰嶅伓` to the intended bridge target.
- `2026-05-22` `0450e24` `feat: capture browser downloads and knowledge exports`: Captured Playwright downloads from browser clicks into the active tool workspace, returned send-compatible file metadata, updated knowledge backend study/export service and web console support, and added related generated study artifacts/scripts. Validation: `.venv\Scripts\python.exe -m py_compile agent\tools\browser\browser_service.py agent\tools\browser\browser_tool.py` OK; local selector and ref PDF download smokes OK; `.venv\Scripts\python.exe -m pytest tests\test_knowledge_backend_service.py tests\test_knowledge_backend_complete.py` OK (12 tests); `node --check channel\web\static\js\console.js` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: widen trusted normal-user permissions`: Added code-default normal-user read roots for workspace temp/download/attachment directories, system temp, `~/Downloads`, and `~/Desktop`; allowed shared `knowledge/` writes by default; added config extension keys; and kept file deletion plus CowAgent project modification blocked. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_knowledge_guard tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (69 tests); `.venv312\Scripts\python.exe -m unittest tests.test_multi_user_isolation` OK (16 tests); `.venv312\Scripts\python.exe -m compileall agent/access_control.py agent/user_profiles.py bridge/agent_initializer.py config.py tests/test_multi_user_isolation.py` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: soften bridge tone and protect private knowledge`: Made social bridge generated messages sound more like a warm friend relaying a message, removed stiff public wording such as `鎺堟潈鎴戣浆杩癭, added a non-apology bridge-message test, added prompt/skill/tool guardrails that keep private Weixin users, Bridge IDs, social-bridge reachability, and cross-user relay metadata out of `knowledge/`, and cleaned the local workspace knowledge page that had captured 灏忔爛/Rondle bridge details. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_knowledge_guard tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (65 tests); `.venv312\Scripts\python.exe -m compileall agent/prompt/builder.py agent/prompt/workspace.py agent/social_bridge/service.py agent/tools/knowledge_guard.py agent/tools/write/write.py agent/tools/edit/edit.py tests/test_knowledge_guard.py tests/test_social_bridge_service.py` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: route bridge active sends through running app`: Fixed social bridge proactive sends when CowAgent is launched as `python app.py` by keeping a canonical `app` module reference and falling back to the `__main__` ChannelManager. Pending bridge messages are now visible/retryable by both sender and target, with a sender/status index. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (59 tests); `.venv312\Scripts\python.exe -m compileall app.py agent/social_bridge/service.py agent/social_bridge/store.py tests/test_social_bridge_service.py tests/test_social_bridge_store.py` OK; `git diff --check` OK with CRLF warnings only. Note: one unrelated image-generation concurrency timing test exceeded its local 0.65s threshold during a broad run, while the affected manager lookup test passed separately.
- `2026-05-22` `fix: show declared names in bridge directory`: Made the social bridge directory derive public display names from both `USER.md` and `MEMORY.md`, including Agent-written formats such as `鐢ㄦ埛甯屾湜琚О涓恒€屽皬鏍€銆峘 and `鐢ㄦ埛绉板懠锛氬皬鏍€`, while ignoring blank-template placeholders. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_social_bridge_service tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_multi_weixin_instances tests.test_multi_user_isolation tests.test_fast_lane_progress` OK (57 tests); `.venv312\Scripts\python.exe -m compileall agent/social_bridge/service.py tests/test_social_bridge_service.py` OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `feat: add social bridge messaging`: Added controlled cross-Weixin user directory, relationship memory, authorized bridge messaging, pending/retry handling, Weixin proactive text routing, social bridge tool registration, config defaults, and focused privacy/active-send tests. Validation: `.venv312\Scripts\python.exe -m unittest tests.test_social_bridge_store tests.test_social_bridge_tools tests.test_social_bridge_service tests.test_multi_weixin_instances tests.test_multi_user_isolation` OK (47 tests); `.venv312\Scripts\python.exe -m compileall agent\social_bridge agent\tools\social_bridge channel\weixin\weixin_channel.py config.py` OK; ToolManager bridge-tool loading smoke OK; `git diff --check` OK with CRLF warnings only.
- `2026-05-22` `fix: make token usage skill read CowAgent runtime logs`: Updated `token-usage-tracker` to fall back to `data/llm_cache_usage.jsonl`, surface cache/reasoning metrics, and fixed the Bash tool so skill subprocesses receive only string env vars and resolve the active Python interpreter.
- `2026-05-22` `fix: persist real Weixin id mappings`: Stopped the channels API from exposing iLink `@im.wechat` raw IDs as display WeChat IDs, added save support for real WeChat IDs on Weixin channel cards, and covered the mapping path with focused tests.
- `2026-05-22` `d4976d2` `fix: show multi-weixin users and real ids`: Added dynamic `weixin_*` channel cards, repeated QR login for new WeChat users, admin/member role display, startup credential backfill, and real WeChat ID mapping for prompt-cache/user labels.
- `2026-05-22` `631f159` `feat: sync local cow skills`: Synced the seven locally installed CowWechat skills into `skills/`, added per-skill `INSTALL.md` files, documented required user-provided API keys, omitted pycache/local state, and sanitized local-only paths.
- `2026-05-22` `852e909` `feat: add wechat quick progress lane`: Added `/q` progress and quick-reply fast lanes, `/鐘舵€乣/`/鍙栨秷`/`/璺宠繃` local controls, per-session progress snapshots, cooperative Agent cancellation, and fast-lane unit tests.
- `2026-05-22` `c309e61` `fix: make weixin onboarding greeting deterministic`: Added a narrow pre-model onboarding greeting path so a clean workspace with `BOOTSTRAP.md` returns the full first-run welcome for pure greeting messages.
- `2026-05-22` `3fc82be` `docs: update latency telemetry notes`: Updated local handoff and Git notes after latency telemetry validation.
- `2026-05-22` `3d39398` `feat: add latency telemetry for weixin replies`: Added hashed request latency logs for session queue wait, channel handling, AgentBridge execution, and each LLM stream turn so slow Weixin replies can be attributed to queueing, model latency, cache misses, tools, persistence, or send time.
- `2026-05-22` `7598c2d` `feat: improve prompt cache telemetry`: Added prompt cache usage visibility and persisted cache hit metrics for the current deployment.
- `2026-05-22` `4ed1edf` `feat: add safe github code update skill`: Added natural-language `code-update` skill plus guarded `git_code_update` tool for fast-forward-only updates that refuse dirty worktrees and protected config/secret paths.
- `2026-05-22` `51e861b` `docs: document github remote layout`: Documented that Codex/UI pushes should use the user's `origin` remote while the original project is kept as fetch-only `upstream`.
- `2026-05-22` `cd29392` `fix: preserve private memory isolation in shared scope`: Prevented shared memory access from exposing other users' private memory entries and added isolation tests.
- `2026-05-22` `40ee608` `chore: include latest web console state`: Captured the final current web console cache-view state before GitHub upload.
- `2026-05-22` `af5f161` `chore: snapshot current CowWechat workspace`: Captured the current local workspace, including image generation tooling, access control/user isolation helpers, scheduler tests, and GitHub-upload ignore hardening.
- `2026-05-21` `3d47ee5` `docs: note browser dependency installation`: Documented browser dependency installation.
- `2026-05-21` `c135c23` `chore: document browser and vision optional deps`: Documented Playwright/browser-use/agentmesh and image/QR/HTML helper dependencies in optional requirements after installing them locally.
- `2026-05-21` `f44c612` `feat: cover OpenAI multimodal capability config`: Added OpenAI multimodal capability configuration and focused tests.
- `2026-05-21` `b6d43bb` `feat: add OpenAI responses wire mode`: Added Responses API wire-mode support across OpenAI-compatible chat, streaming, tools, vision, voice gateway handling, config, and focused tests.
- `2026-05-21` `ac69a90` `deploy: document windows weixin deepseek setup`: Initial local Windows Weixin deployment documentation and safe ignore policy.

## Rollback Notes

- To roll back the GitHub upload snapshot, inspect `git show af5f161` and `git show 40ee608`, then revert in reverse order if needed.
- To roll back the CowWechat skill sync, revert `631f159`; local installed skills under `~/cow/skills` are not changed by reverting the repository.
- To roll back latency telemetry only, revert `3d39398` after confirming no runtime-only config files are staged.
- To roll back the `/q` fast-lane behavior only, revert `852e909` after confirming no runtime-only config files are staged.
- Local runtime config is ignored; rolling back code does not change `config.json`, `.env*`, Weixin credentials, or local virtual environments.
- The active service can be checked with `PYTHONUTF8=1 .venv\Scripts\python.exe -m cli.cli status`.

